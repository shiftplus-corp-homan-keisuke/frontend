# Session9: 高度なコンポーネント設計と総合演習

## セクション概要

このセッションでは、STEP03 の最終段階として、これまでに学んだすべての知識を統合し、より高度なコンポーネント設計と実践的な演習に取り組みます。children プロパティのさらなる活用法を学び、複雑なアプリケーションの設計と実装を通じて、React の真の力を体験していきます。

このセッションは、STEP03 の集大成として位置づけられており、状態管理、イベント処理、フォーム操作、そしてコンポーネント設計のすべてを統合した総合的な学習内容となっています。実際の開発現場で求められるスキルを身につけ、自信を持って React アプリケーションを構築できるようになることが目標です。

特に、children プロパティの高度な活用法と、複数のコンポーネントが連携する複雑なアプリケーションの設計パターンを習得することで、より実践的で再利用可能なコードを書けるようになります。

## children プロパティのさらなる活用：再利用可能コンポーネントの構築

children プロパティを再度活用して、さらに別の再利用可能なコンポーネントを構築してみましょう。

今度は、メッセージを表示するコンポーネントを作成したいと思います。

このメッセージは常に paragraph 要素となります。

また、常にここにステップ番号が含まれます。

例えば、h3 要素の中に、

このような形で、そして何らかの内容が含まれます。

つまり、私たちが渡すコンテンツ、例えばここにあるメッセージのようなものです。

そこで、再び children プロパティを使用して、そのようなコンポーネントを作成しましょう。

### StepMessage コンポーネントの作成

このコンポーネントを「StepMessage」と呼びます。

```jsx
function StepMessage({ step, children }) {
  return (
    <div className="message">
      <h3>Step {step}</h3>
      {children}
    </div>
  );
}
```

今のところ、これをコピーしてみましょう。

すべてをコピーします。

前述したように、基本的に常に現在のステップを表示し、そしてここに受け取るコンテンツを表示したいと思います。

そこで、ここで 2 つの props を受け取る必要があります。

最初はこの step です。

そして再び、children プロパティです。

各コンポーネントは、覚えておいてください、

開始タグと終了タグの間にコンテンツを渡すと、

自動的にこの children プロパティを受け取ります。

つまり、コンポーネントを使用するときに、この機能が有効になります。

### StepMessage コンポーネントの実装詳細

そこで、今度は step を取得し、その step の直後に、受け取った children を単純に表示しましょう。

```jsx
function StepMessage({ step, children }) {
  return (
    <div className="message">
      <h3>Step {step}</h3>
      {children}
    </div>
  );
}
```

この部分が、前の講義で話した空の穴です。

そして、この空の穴は、もちろん私たちが渡すものなら何でも埋めることができます。

そして再び、私たちが渡すものは、

StepMessage の開始タグと終了タグの間にあるものです。

ボタンでは、その穴は基本的にボタンのすべてのコンテンツでした。

つまり、ボタンはここで、

渡されたものを正確に表示するだけです。

しかし、ここでは、さらに、

常にこの部分も表示します。

つまり、ステップ自体を含む H3 です。

### StepMessage コンポーネントの使用

さて、ここのメッセージを StepMessage に置き換えてみましょう。

```jsx
<StepMessage step={step}>Learn React ⚛️</StepMessage>
```

今度は、step は、もちろん、

他のプロパティと同じように渡す必要があります。

span ではなく、step プロパティです。

そして今、ここで開始タグと終了タグの間に、

基本的に children プロパティの内容を定義します。

つまり、これが children プロパティの値になります。

これをすべて取り除くと、

何が起こるか見てみましょう。

まあ、実際には何も表示されませんでした。

ああ、もちろんです。

つまり、ここで JSX を書いただけで、return 文がありませんでした。

また、ここで警告が表示されています。

H3 要素は P 要素の中に入れることができません。

そこで、この P を div に置き換えましょう。

```jsx
function StepMessage({ step, children }) {
  return (
    <div className="message">
      <h3>Step {step}</h3>
      {children}
    </div>
  );
}
```

保存してください。

そして、そこにあります。

美しいです。常に現在のステップを表示します。

そして、もちろん、以前にあったメッセージも表示されます。

なぜなら、ここで渡したからです。

これは以前にあったものと全く同じです。

そして今、もちろん、これをアプリのどこでも再利用できます。

### 複数の StepMessage コンポーネントの使用

ステップの後のどこか他の場所で、

アプリで StepMessage を使いたいとしましょう。

ステップ番号 1 にしましょう。

そして、ここで、コンテンツは「Pass in content」かもしれません。

別の段落に絵文字を入れてみましょう。

```jsx
<StepMessage step={1}>
  <p>Pass in content 👋</p>
</StepMessage>
```

ここに渡すコンテンツの種類は全く関係ありません。

そうすると、ここにステップ 1 のような表示があります。

そして、好きなだけ多くのコンポーネントを作ることができます。

コピー＆ペーストしてみましょう。

例えば、ステップ 2 にして、

「Read children prop」にして、

そして別の絵文字にしてみましょう。

```jsx
<StepMessage step={2}>
  <p>Read children prop 📖</p>
</StepMessage>
```

保存してください。

そして今、再び再利用可能なコンポーネントを作成しました。

アプリケーションのどこでも使用でき、

好きなコンテンツを与えることができます。

そして、ご覧のように、常に、

与えた番号でここにステップを表示し、

そしてコンポーネント自体に渡したコンテンツを表示します。

### Button コンポーネントとの組み合わせ

ここに戻りましょう。

そして、ここで、

再びボタンを使用できると考えました。

なぜ使えないでしょうか？

そうすると、これらの異なるプロパティを持つことがさらに意味を持ちます。

なぜなら、今度は基本的に異なる方法でカスタマイズできるからです。

そこで、ここで異なる背景色と

異なるテキスト色が欲しいとしましょう。

これらは文字列のようにする必要があります。

```jsx
<Button
  bgColor="#7950f2"
  textColor="#fff"
  onClick={() => alert(`Learn How To ${children}`)}
>
  Learn React ⚛️
</Button>
```

ああ、そして onClick イベントハンドラーが不足しています。

そこで、ここでインライン関数を定義しましょう。

そして、改めて強調しますが、関数を定義することに重点を置いています。

つまり、ここで関数を渡す必要があり、関数を呼び出すのではありません。

ここで何かをアラートしてみましょう。

それは実際には重要ではありません。

「Learn How To」と言って、

そしてここでこれを使用できます。

各ステップに関連するようにするためです。

ボタンを開いて、終了タグがあり、

そして今、ここに好きなものを渡すことができます。

そして、もちろん、渡すものは

通常のテキストでも構いません。

保存してください。

そして、そこにボタンがあります。

まだ動作します。

つまり、onClick ハンドラーを正常に渡して、

そして使用しています。

スタイリングが少しおかしく見えますが、

それは、ここでスタイルを設定したからだと思います。

実際に見てみましょう。

確認するためだけに。

実際、これらのボタンは、

buttons クラスを持つ要素の子要素であるため、

これらのスタイルが適用されます。

そうですよね？しかし、それは実際には重要ではありません。

もちろん、これを

buttons クラスを持つ div でラップすることもできます。

それは動作するかもしれませんし、

他のスタイリングに影響を与えるかもしれません。

まあ、それは実際には動作しませんでした。

しかし、buttons だからです。はい、いいですね。

実際にはそれの方が良く見えます。

### children プロパティの柔軟性の理解

そして、これで、再び、

ここでボタンを本当にカスタマイズできることが理解できます。

これら 3 つのような通常のプロパティでいくつかの部分を、

そして children プロパティでコンテンツ自体を。

つまり、常に文字列だけを渡したい場合は、

これはもちろん通常のプロパティでも構いません。

しかし、時々他のコンテンツが必要な場合、

ここのような実際の JSX のように、この span 要素のように。

まあ、そうすると本当に

children プロパティを活用すべきです。

そして今、このメッセージを本当に理解するために、

この重要な children プロパティでの作業方法を本当に理解するために、

アコーディオンコンポーネントの演習に戻って、

そこでも使用してみましょう。

## 演習 2: アコーディオンコンポーネント（v2）の改良実装

これまでに学んだことをすべて活用して、

以前に構築を開始したアコーディオンを

もう少し良く、もう少し実世界的にしてみましょう。

そして、ここにあります。

以前と同じように、アイテムの 1 つをクリックすると、

開きますが、別のアイテムをクリックすると、

これだけが開いたままになり、

他のすべては閉じられます。

### アコーディオンの動作原理

基本的に、これが意味することは、今度は各アイテムが

開いているかどうかを制御しなくなったということです。

代わりに、今度はアコーディオンが

どのアイテムが開いているかを管理しており、

そしてそのアイテムだけが、基本的に、開くことが許可されます。

例えば、アイテム番号 3 が

現在開いているものである場合、番号 1、2、そして 4 は

閉じられている必要があります。

つまり、再び、これらのアイテムはすべて

どれが現在開いているアイテムかを知る必要があり、つまり

今度は状態をアイテムから

ここ、アコーディオンに移動する必要があります。

### 状態の上位移動（Lifting State Up）の実装

今、パート 1 からのコードがまだここにあります。

これをフォークしてみましょう。

基本的に他の部分を変更せずに残すことができるようにです。

しかし、バージョン 1 の上で作業することも完全に可能です。

そうすれば、それだけを持つことになります。

そこで、今度はこの状態の部分を削除しましょう。なぜなら、再び、

今度は各アイテムが開いているかどうかを制御しなくなったからです。

そうすると、たくさんのエラーが発生しますが、

それは問題ありません。

今のところは、少なくとも。

### 新しい状態管理の実装

そこで、ここで、話していた状態の部分を作成しましょう。

これは curOpen になります。

そして、ここで現在開いているアイテムの番号を保存します。

```jsx
function Accordion({ data }) {
  const [curOpen, setCurOpen] = useState(null);

  return (
    <div className="accordion">
      {data.map((el, i) => (
        <AccordionItem
          curOpen={curOpen}
          onOpen={setCurOpen}
          title={el.title}
          num={i}
          key={el.title}
        >
          {el.text}
        </AccordionItem>
      ))}
    </div>
  );
}
```

setCurOpen と useState を使用して、null から始めます。

そうすると、最初は何も開いていません。

つまり、再び、この curOpen 状態変数は基本的に

番号、つまりここにあるこの番号を保持します。

現在開いているアイテムのものです。

そして、その curOpen を AccordionItem に渡し、

そしてそこから、

現在開いているかどうかを計算できます。

そうしてみましょう。

curOpen を渡しましょう。

curOpen という props を使用してそれを渡し、

そしてこの setter 関数も渡す必要があります。

そうすると、ここのアイテムで、

クリックされたアイテムに現在のアイテムを設定できます。

そして、ここで、以前に使用した規則に従いましょう。

on プレフィックスで始めることです。

onOpen という名前にしましょう。そして setCurOpen を渡します。

### AccordionItem コンポーネントの更新

そして今、ここで、それらの props を受け取りましょう。

curOpen と onOpen です。

```jsx
function AccordionItem({ num, title, curOpen, onOpen, children }) {
  const isOpen = num === curOpen;

  function handleToggle() {
    onOpen(isOpen ? null : num);
  }

  return (
    <div className={`item ${isOpen ? "open" : ""}`} onClick={handleToggle}>
      <p className="number">{num < 9 ? `0${num + 1}` : num + 1}</p>
      <p className="title">{title}</p>
      <p className="icon">{isOpen ? "-" : "+"}</p>

      {isOpen && <div className="content-box">{children}</div>}
    </div>
  );
}
```

ここで、今のところこれをコメントアウトして、

そこにあるエラーを取り除きましょう。

そして、ここで、前述したように、基本的に

このアイテムが現在開いているかどうかを計算できます。

つまり、再び、isOpen と言えます。

以前の変数名を復活させて、

そして、このアイテムの番号が

現在開いているものと等しい場合。

その場合、isOpen は true になり、

そして、まあ、開いているものとして表示されます。

これを null から 1 に変更してみましょう。

そして、それは動作します。

これが 1 であることを覚えておいてください。

そして、これは 0、1、2 です。

2 と言えば、そのアイテムが開き、

そして 0 では、最初のアイテムが開きます。

素晴らしい！つまり、状態変数を作成し、

UI で使用しました。今回は、それを

アイテムに渡し、そしてそれを使用して

アイテムが現在開いているものかどうかを計算しました。

### イベントハンドラーの実装

よし、これを null に戻しましょう。

なぜなら、もちろん、今度は状態を更新したいからです。

これらのそれぞれをクリックすることによって。

それはここにあります。すべてを取り除いて、

そして onOpen 関数を使用しましょう。

そして、ここはとても簡単です。

やりたいことは、番号を渡すことだけです。

例えば、ここでアイテム番号 1 にいる場合、

クリックすると、onOpen が番号 1 で実行されます。

そして、onOpen は基本的に setIsOpen であることを覚えておいてください。

これは setCurOpen と呼ばれるべきです。

よし、番号 1 で実行し、

そうすると状態 curOpen が 1 になり、

そして、ここで再び何が起こるかというと、

1 は 1 と等しく、そのアイテムが

開き、他のすべては閉じたままになります。

例えば、ここのアイテム 0。

まあ、アイテム 0 は 1 と異なるので、

isOpen は false です。

そして、それは美しく動作します。

素晴らしい！そして、これはかなり混乱する可能性があることを理解しています。

なぜなら、状態を上下に渡しているからです。

しかし、これは基本的に状態のリフトアップのアイデアです。

以前は、各アイテムにここに状態がありましたが、

突然、すべてが現在の状態について知る必要があり、

そのため、状態を最も近い親にリフトアップしたと言えます。

### データフローの分析

よし、この演習が終わったら、

ここでデータフローを詳細に分析するために

時間を取ってください。

すでにこれらすべてについて学んだので、

ここでは少し速く進んでいます。これは単なる演習です。

よろしいですか？次に、やりたいことは、

children プロパティについての知識を活用することです。

### children プロパティの活用

つまり、ここでテキストを渡す代わりに、このように、

実際にテキストをコンテンツとして定義する方がはるかに良いです。

基本的に、コンテンツとして。

そこで、そのテキストをここに渡しましょう。el.text です。

そして、これを閉じましょう。

そして、実際に、今テキストは一時的に消えています。

つまり、もうここでこの props は必要ありませんが、

children プロパティが必要です。これを

リストの最後または最初に追加したいと思います。

そして、ここで、それはもう text ではなく、children です。

はい、それは素晴らしく動作します。

そして、これで、

各アイテムに表示されるべきコンテンツの種類に関して

はるかに柔軟になります。

### 手動でのアイテム追加

そこで、ここに手動で 1 つ追加してみましょう。

それが、以前に見せたデモの 23 の由来です。

つまり、このループの外で、今度は 1 つだけ作成しましょう。

1 つのテストです。テスト 1 です。

そして、これは実際には文字列だと思います。

番号は、22 にしましょう。

ここで、再びタイトルを繰り返しましょう。

そして、コンテンツについては、ここに来てこれをコピーしましょう。

手で書きたければ書くことができますし、

本当に、好きなものを書くことができます。

これを P 要素でラップし、これを UL 要素でラップします。

これらのそれぞれを、単純に 1 つの LI 要素にします。

つまり、これは実際には必要ありませんが、

これは単に使い方を示すためです。

よし。そして、今ここをクリックすると、

この全体の JSX をコンテンツとして渡すことができることが理解できます。

そして、それは、再び、children プロパティのおかげです。

つまり、ここでは、もちろん、テキストだけがありますが、はい、

ここでは、この全体の HTML の構造があります。基本的に。

そして、もちろん、ここでのアコーディオンの全体の機能は

以前と同じように動作します。

つまり、同時に開くことができるのは 1 つだけです。

### 開いているアイテムを閉じる機能の追加

今、1 つの小さなことが欠けています。

それは、ここで、開いているものをクリックしても、

閉じないということです。そうですよね？

つまり、おそらくあなた自身でそれを見ることができるでしょう。

そして、今やる必要があることは、基本的に

すでに開いているときは状態を null に設定することです。

つまり、ここをクリックして、すでに開いている場合、

新しい状態、つまり新しい curOpen は、

null になるべきで、そうでなければ

番号になるべきです。

そして、それは実際にそれを修正します。

素晴らしい！つまり、その演習も終了しました。

そして、それが有用だったことを願っています。

そして今、前述したように、

コードを分析するために時間を取ってください。

そして、ここでこのコンポーネントのデータフローを。

そして、それが終わったら、

このセクションの必須のコーディングチャレンジの準備ができています。

### 完成したアコーディオンコンポーネントのコード

```jsx
// app.js
import { useState } from "react";
import "./styles.css";

const faqs = [
  {
    title: "Where are these chairs assembled?",
    text: "Lorem ipsum dolor sit amet consectetur, adipisicing elit. Accusantium, quaerat temporibus quas dolore provident nisi ut aliquid ratione beatae sequi aspernatur veniam repellendus.",
  },
  {
    title: "How long do I have to return my chair?",
    text: "Pariatur recusandae dignissimos fuga voluptas unde optio nesciunt commodi beatae, explicabo natus.",
  },
  {
    title: "Do you ship to countries outside the EU?",
    text: "Excepturi velit laborum, perspiciatis nemo perferendis reiciendis aliquam possimus dolor sed! Dolore laborum ducimus veritatis facere molestias!",
  },
];

export default function App() {
  return (
    <div>
      <Accordion data={faqs} />
    </div>
  );
}

function Accordion({ data }) {
  const [curOpen, setCurOpen] = useState(null);

  return (
    <div className="accordion">
      {data.map((el, i) => (
        <AccordionItem
          curOpen={curOpen}
          onOpen={setCurOpen}
          title={el.title}
          num={i}
          key={el.title}
        >
          {el.text}
        </AccordionItem>
      ))}

      <AccordionItem
        curOpen={curOpen}
        onOpen={setCurOpen}
        title="Test 1"
        num={22}
        key="test 1"
      >
        <p>Allows React developers to:</p>
        <ul>
          <li>Break up UI into components</li>
          <li>Make components reusuable</li>
          <li>Place state efficiently</li>
        </ul>
      </AccordionItem>
    </div>
  );
}

function AccordionItem({ num, title, curOpen, onOpen, children }) {
  const isOpen = num === curOpen;

  function handleToggle() {
    onOpen(isOpen ? null : num);
  }

  return (
    <div className={`item ${isOpen ? "open" : ""}`} onClick={handleToggle}>
      <p className="number">{num < 9 ? `0${num + 1}` : num + 1}</p>
      <p className="title">{title}</p>
      <p className="icon">{isOpen ? "-" : "+"}</p>

      {isOpen && <div className="content-box">{children}</div>}
    </div>
  );
}
```

```jsx
// index.js
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";

import App from "./App";

const rootElement = document.getElementById("root");
const root = createRoot(rootElement);

root.render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

```css
/* styles.css */
* {
  padding: 0;
  margin: 0;
  box-sizing: border-box;
}

body {
  font-family: sans-serif;
  color: #343a40;
  line-height: 1;
}

.accordion {
  width: 700px;
  margin: 100px auto;
  display: flex;
  flex-direction: column;
  gap: 24px;
}

.item {
  box-shadow: 0 0 30px rgba(0, 0, 0, 0.1);
  padding: 20px 24px;
  padding-right: 48px;
  cursor: pointer;
  border-top: 4px solid #fff;
  border-bottom: 4px solid #fff;

  display: grid;
  grid-template-columns: auto 1fr auto;
  column-gap: 24px;
  row-gap: 32px;
  align-items: center;
}

.number {
  font-size: 24px;
  font-weight: 500;
  color: #ced4da;
}

.title,
.icon {
  font-size: 24px;
  font-weight: 500;
}

.content-box {
  grid-column: 2 / -1;
  padding-bottom: 16px;
  line-height: 1.6;
}

.content-box ul {
  color: #868e96;
  margin-left: 16px;
  margin-top: 16px;

  display: flex;
  flex-direction: column;
  gap: 12px;
}

/* OPEN STATE */
.open {
  border-top: 4px solid #087f5b;
}

.open .number,
.open .title {
  color: #087f5b;
}
```

## チャレンジ 1: チップ計算機アプリケーションの完全実装

再びセクションを

素晴らしいコーディングチャレンジで終了しましょう。

セクションで学んだことをすべて練習できるようにです。

そして、これらのチャレンジがいかに重要かを

十分に強調することはできません。

ですから、お願いします、お願いします、今このビデオをスキップしないでください。

### チップ計算機の機能紹介

つまり、この非常にシンプルなチップ計算機を構築します。

ここで、請求書がいくらだったかを入力できます。

100 ドルとしましょう。

そして、サービスにどれだけ満足したか、不満だったかを言うことができます。

あなたとあなたの友人が。

つまり、2 人が夕食に出かけたとしましょう。

そして、基本的に一緒に

チップがいくらであるべきかを決めます。

例えば、あなたが良かったと言えば、それは 10%になることができますが、

あなたの友人は 0%と言います。

そうすると、あなた 2 人の平均は 5%になります。

これは実際にここの 5 ドルです。

そして、最終的な請求書は 105 ドルです。

これは 100 ドル + 5 ドルのチップです。

そして、もちろん、あなたの友人が絶対に素晴らしいと思った場合、

ここであなた 2 人の平均は 15 になり、

したがって 15 ドルのチップになります。

そして、ここで値を変更すると、

もちろん、ここで更新され続けます。

また、この Reset ボタンもあります。クリックすると、

すべてを初期値に戻します。

### コンポーネント設計の要件

今、これを構築するときに、

これらの各部分に対して 1 つの別々のコンポーネントを作成してほしいと思います。

つまり、ここのこれは 1 つのコンポーネントであるべきです。

例えば、BillInput と呼ぶことができます。

そして、これに対して 1 つのコンポーネントを持つべきです。

例えば、SelectPercentage と呼ばれ、

そして、ここで再びそのコンポーネントを再利用します。

つまり、異なるテキストで

しかし、同じコンポーネントであるべきです。

そして、ここで、このテキストを表示するための

何らかの Output コンポーネントがあり、

そして、このボタンだけのための Reset コンポーネントがあります。

よろしいですか？

### 状態管理の設計

今、ここのこれら 3 つの入力は、

もちろん制御された要素です。

そして、それらの状態値を

コンポーネント自体に追加することから始めることができます。

しかし、実際には

この状態を親コンポーネントで必要とすることにすぐに気づくでしょう。

なぜなら、

ここでこの出力を計算するために必要になるからです。

つまり、状態の上位移動の技術を

使用する必要があります。

そして、うまくいけば、すべてのロジックを覚えています。

それがどのように動作し、なぜそれをしなければならないのか。

そして、まだ疑問がある場合は、

いつでも戻って講義をチェックすることができます。

状態の上位移動について話したところで。

そして、もちろん、他のすべてのことも

ここでこのチャレンジで必要です。

### チャレンジの難易度について

つまり、このチャレンジはそれほど簡単ではないことを知っています。

しかし、それは多くの学生が実際に望んでいることです。

つまり、前回大多数に尋ねたとき、

本当に難しいチャレンジを望んでいました。

つまり、これは本当に難しいとは思いませんが、

少し挑戦的になることがあります。

つまり、100%単純ではありません。

したがって、何か問題に遭遇した場合、

それは全く問題ありません。

つまり、できる部分を書いて、

そして残りは、私の解決策を見てください。

それは 100%大丈夫です。

最も重要な部分は、

少なくともいくつかのコードを書き始めることと、

自分で試してみることです。

そして、それで十分な話です。

5 分、10 分、または必要な時間でここでお会いしましょう。

### チップ計算機の実装開始

よし、書くコードがたくさんあるので、

すぐに始めましょう。

つまり、TipCalculator と呼ばれる関数から始めます。

これを基本的にここで呼び出します。

つまり、このアプリにはスタイルは全く必要ありません。

そのため、CSS スターターファイルを提供しませんでした。

よし、ここでサイドバーを閉じて、

すべてのコンポーネントを書き出すことから始めましょう。

つまり、次のものは BillInput と呼ばれます。

そして、SelectPercentage を作成しましょう。

そして、もちろん、ここでのコンポーネント名は

完全に異なることができます。

そして、function Output があります。

そして、最後に Reset です。

よし、今度は 1 つずつ構築してみましょう。

そして、実際にここの TipCalculator から始めることができます。

なぜなら、ここで起こることは

基本的に他のすべてを含むことだからです。

つまり、ここで div を作成しましょう。

そして、BillInput が欲しいです。

2 つの SelectPercentage が欲しいです。

そして、もちろん、後でここに

props を渡します。

再び、データなしで静的な部分を

構築しているだけです。

つまり、Output があり、

Reset ボタンがあります。

よし、これから始めましょう。

### BillInput コンポーネントの実装

よし。

そして、これを label に入れます。

お会計はいくらでしたか？

そして、最後に最初の入力があります。

type は text です。

そして、placeholder も追加しましょう。

これは常に良いです。

そして、はい、ここで UI に何かが既にあります。

このクラスを取り除くことを考えていました。

そうすると、基本的にすべてのスタイリングを

完全に取り除きます。

この CSS に来ることができます。

そして、このようにします。

フォントファミリーを目に少し良くするためだけに。

### SelectPercentage コンポーネントの実装

しかし、とにかく、ここで、再び div を返しましょう。

そして、select 要素があります。

そして、既に知っているように、select の中に

異なるオプションがあります。

つまり、0 は不満で、

0%です。

そして、これを数回コピーしましょう。

5、10、20 です。

普通は 5%、

良かったは 10%、

そして 20%は絶対に素晴らしいです。

今、ここでもテキストが欲しいです。

そして、props としてここに渡すこともできますが、

ここで直接書く方がはるかに良いと思います。

つまり、サービスはいかがでしたか？

そして、これを閉じます。

そして、基本的に開始タグと終了タグの間の

コンテンツを含めます。

ご友人のサービスの評価はいかがでしたか？

よし。

そして、今、このテキストをここで

SelectPercentage に取得する方法を覚えていることを願っています。

そして、それは正しいです。自動的に

children プロパティとしてここに渡されます。

つまり、段落または label を作成しましょう。

まさにそれで。

そこに行きます。

それははるかに良く見えます。

### Output コンポーネントの実装

つまり、ここで続けましょう。

return して、ここで h3 があります。

あなたは$X を支払います。

これはいくらかのドルです。

$A と言いましょう。プラス、

チップとして$B。

つまり、今のところここでプレースホルダーだけです。

まだ静的バージョンを構築しているからです。

### Reset コンポーネントの実装

今、最後にここで、Reset と言うボタンです。

今、よし、静的な部分を構築するのに

時間がかかりました。

そして今、状態を追加しましょう。

### 状態管理の実装

そして、始める最良の方法は、

ここで必要なこれら 3 つの制御された要素のそれぞれに対して

1 つの状態を作成することです。

今、最初に言ったように、

ローカル状態を作成することから始めることもできました。

ここのコンポーネントで。

つまり、基本的に以前に学んだように。

しかし、そうすると、すぐに

ここでこの入力値が必要であることを発見するでしょう。

例えば、ここの TipCalculator コンポーネントで

なぜなら、後でここで

このチップ値を計算するのはここだからです。そうすると、それを表示できます。

つまり、Output に渡してそこでレンダリングできます。

したがって、実際に、基本的に、

すぐに状態を上位移動し、

BillInput に書くことさえしません。

つまり、bill と setBill は useState と等しく、

何らかの理由で、時々、これは

インポートで実際に動作しません。

つまり、手動でやりましょう。react から useState。

いいですね。

そして、実際にここで空の文字列から始めましょう。

よし。

そして、両方を渡す必要があります。

なぜなら、もちろん、その状態を更新したいからです。

ここの入力で変更イベントが発生するたびに。

覚えていますか？

つまり、それは制御された要素の動作方法です。

常に同じレシピ、常に同じことです。

状態がこのコンポーネントに住んでいるか、

別の親コンポーネントにあるかは関係ありません。

しかし、アイデアは同じです。1 分後に再び見るでしょう。

つまり、bill の値で Bill という props を渡しましょう。

そして、再び on プレフィックスを使用しましょう。

つまり、onSetBill は setBill になります。

しかし、もちろん、

まさにこの名前で props を渡すことも同様に有効です。

つまり、ここで bill と onSetBill を受け取ります。

そして、ここでこれらの値を使用しましょう。

つまり、この入力フィールドの値は bill であるべきです。

そして、変更されたとき、

基本的に e.target.value に基づいて

bill を設定したいです。

そして、全体を number に変換する必要があります。

今、よし。

### 動作確認

今、残念ながら開発ツールは動作しません。

既に何度か経験したように、

しかし、それを見るために、

実際にここの output に渡すことができます。

つまり、bill は bill です。

なぜなら、実際にその bill 値を

ここで表示する方法を覚えているからです。

つまり、まさにそこの 80 です。

つまり、ここで受け取りましょう。

つまり、ここの最初の値です。

つまり、見てみましょう。

それは、はい、美しく動作します。

つまり、通常の制御されたコンポーネントまたは

制御された要素と同じです。

違いは、状態が

コンポーネント自体に住んでいないことです。

しかし、親コンポーネントにあります。

### SelectPercentage コンポーネントの状態管理

そして今、これらの SelectPercentage コンポーネントに対して

まったく同じロジックを使用しましょう。

つまり、2 つの状態を作成しましょう。それぞれに 1 つずつ。

つまり、それは percentage1 になります。

そして setPercentage1

そして、ここでのデフォルトは 0 です。

そして、コピー＆ペーストしましょう。

つまり、ここのこれは percentage1 の値を取ります。

つまり、このコンポーネントが受け取る percentage プロパティは

percentage1 状態と等しくなります。

一方、この他のもの、つまりこれも

もちろん percentage プロパティを取得しますが、

ここで値はこの他の状態によって与えられます。

そして、これで、同じコンポーネントを使用して

これら 2 つの異なる状態値を設定できます。

よし、そして今、onSelect ハンドラーも。

そして、これは setPercentage1 になります。

一方、再びここで onSelect は

setPercentage2 になります。

よし。

そして、ここで、children の他に、それらを受け取ることができます。

つまり、percentage と onSelect です。

そして、今やることは、value が

受け取る percentage と等しく、

そして onChange

そして onSelect です。

つまり、すぐに number に変換できます。

e.target.value。

常に同じレシピです。

つまり、今すぐに層を見ることはできませんが、

動作しているようです。

両方とも。

### チップの計算（派生状態）

よし、そして今、必要なすべての状態があります。

そして、これに基づいて、今度はチップを計算できます。

つまり、どうやってそれをしますか？

ここでチップのための新しい状態を作成すべきですか？

まあ、実際には必要ありません。

そして、そうすることは意味がありません。

なぜなら、チップは完全に

bill と percentages から計算できるからです。

そして、派生状態の存在下にいます。

つまり、チップは単純に bill

かける 2 つの percentages の平均です。

つまり、それは percentage1 + percentage2、

2 で割って、

そして全体を 100 で割ります。

なぜなら、ここで percentages はこれらの実際の値だからです。

つまり、0 と 100 の間

しかし、0 と 1 の間で計算する必要があります。

なぜなら、まあ、それが percentages を計算する方法だからです。

そして、ここで、この括弧にあまり同意しません。

ここに置きましょう。

つまり、これで平均 percentage を計算し、

0 と 1 の間の数値に変換します。

そして、単純に bill と掛けることで、

チップの金額を取得します。

そして、今度はここでそれを

output に渡す準備ができています。

つまり、ここで受け取って渡します。

実際にはここです。

つまり、tip は tip と等しいです。

そして、ここで既に表示できます。

そして、ここで 2 つを一緒に追加するのと同じくらい簡単です。

つまり、100 と言いましょう。

そして、1 つは不満で、

もう 1 つは素晴らしかったです。

そして、平均は 10%で、まさにその 10 ドルです。

はい、そして 20 になり、

一緒に 120 になります。

ドル記号をそこに置きましょう。

そして、はい、これは正常に動作しているようです。

### Reset ボタンの実装

今やることは、この Reset ボタンを動作させることです。

しかし、それは幸い非常に簡単です。

つまり、function handleReset です。

そして、ここで本当にやる必要があることは

すべての値を

それらの初期状態に戻すことです。

つまり、setPercentage1 を 0 に戻します。

そして、ここで同じことです。

そして、ここで渡します。

つまり、onReset は handleReset のようになるべきです。

そして、もちろん onClick イベントハンドラー、onReset です。

そして、見てみましょう。そして美しいです。

つまり、すべて正常に戻ります。

### 条件付きレンダリングの追加

そして、今やるべきことは、

実際にチップがないときは

ボタンとこの出力を表示しないことです。

つまり、それを表示するのは意味がありませんよね？

つまり、ここのこれら 2 つです。

つまり、JavaScript モードでラップして、

そして bill > 0 &&と言いましょう。

今、ここで問題が発生します。なぜなら、1 つの JSX があるからです。

基本的に 2 つのトップレベル要素を持っています。

これは許可されていません。

つまり、1 つの親要素しかあることができません。

そして、再び、ここで fragment は非常に良いオプションです。

これらは必要ありません。保存してください。

そして、はい、今それらは消えています。

そして、ここで書き始めるとすぐに、チップ値があり、

そこで output とボタンを見ることができます。

素晴らしい、そしてそれは実際にそれです。

### 完成したチップ計算機のコード

```jsx
// app.js
import "./styles.css";
import { useState } from "react";

export default function App() {
  return (
    <div>
      <TipCalculator />
    </div>
  );
}

function TipCalculator() {
  const [bill, setBill] = useState("");
  const [percentage1, setPercentage1] = useState(0);
  const [percentage2, setPercentage2] = useState(0);

  const tip = bill * ((percentage1 + percentage2) / 2 / 100);

  function handleReset() {
    setBill("");
    setPercentage1(0);
    setPercentage2(0);
  }

  return (
    <div>
      <BillInput bill={bill} onSetBill={setBill} />
      <SelectPercentage percentage={percentage1} onSelect={setPercentage1}>
        サービスはいかがでしたか？
      </SelectPercentage>
      <SelectPercentage percentage={percentage2} onSelect={setPercentage2}>
        ご友人のサービスの評価はいかがでしたか？
      </SelectPercentage>

      {bill > 0 && (
        <>
          <Output bill={bill} tip={tip} />
          <Reset onReset={handleReset} />
        </>
      )}
    </div>
  );
}

function BillInput({ bill, onSetBill }) {
  return (
    <div>
      <label>お会計はいくらでしたか?</label>
      <input
        type="text"
        placeholder="Bill value"
        value={bill}
        onChange={(e) => onSetBill(Number(e.target.value))}
      />
    </div>
  );
}

function SelectPercentage({ children, percentage, onSelect }) {
  return (
    <div>
      <label>{children}</label>
      <select
        value={percentage}
        onChange={(e) => onSelect(Number(e.target.value))}
      >
        <option value="0">不満 (0%)</option>
        <option value="5">普通 (5%)</option>
        <option value="10">良かった (10%)</option>
        <option value="20">とても良かった (20%)</option>
      </select>
    </div>
  );
}

function Output({ bill, tip }) {
  return (
    <h3>
      You pay ${bill + tip} (${bill} + ${tip} tip)
    </h3>
  );
}

function Reset({ onReset }) {
  return <button onClick={onReset}>Reset</button>;
}
```

```jsx
// index.js
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";

import App from "./App";

const rootElement = document.getElementById("root");
const root = createRoot(rootElement);

root.render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

```css
/* styles.css */
body {
  font-family: sans-serif;
}
```

### チップ計算機の学習ポイント

つまり、ここで難しい部分は

すべての状態が TipCalculator に配置されていることでした。

しかし、ここのこれら 2 つのコンポーネントが

その状態を必要とし、またその状態を更新することでした。

つまり、ここですべてのこれらのプロパティで

たくさんの状態の受け渡しがあります。

はい、そしてここでも、チップ自体について。

つまり、これが実際に派生状態である必要があることを理解すること。

そうでなければ、このチップを

同期を保つために

bill とこれら両方の percentages で

あちこちで計算しなければならないでしょう。

つまり、それは多くの作業になるでしょう。

そして、代わりに派生状態として持つことで

コンポーネントが再レンダリングされるたびに

状態が更新されると、この値が再び計算され

そしてここで UI にレンダリングすることもできます。

素晴らしい、しかし、それで十分な話です。

ビデオはすでに十分長く実行されています。

そして、はい、あなたが

このコーディングチャレンジの少なくとも一部に取り組んだことを願っています。

そして、質問がある場合は、Q&A に投稿してください。

もしよろしければ、解決策も一緒に。

つまり、同じ動作するアプリケーションに対する

異なるアプローチを見るのは常に良いことです。

そして、これで、実際にセクションの終わりに到達しますが、

パート 1 の終わりでもあります。基本的に。

やりたければ、

次に来る完全な実践プロジェクトがあります。

つまり、再び本当にクールなプロジェクトです。

そして、それをスキップしないことを強くお勧めします。

そして、できるだけ早くそこでお会いしましょう。
