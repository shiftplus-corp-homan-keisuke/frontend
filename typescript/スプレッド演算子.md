# スプレッド演算子（...）による配列の結合

## コード例

```javascript
const animalBox = ["🐰", "🧸", "🐱"];
const birdBox = ["🦩", "🦚"];

// 2つの箱の中身をぜんぶ出して、新しい大きな箱に入れる
const bigBox = [...animalBox, ...birdBox];

// bigBoxの中身を見てみると…
// ['🐰', '🧸', '🐱', '🦩', '🦚']
// 全部入った！
```

## 解説

スプレッド演算子（`...`）は、配列の要素を個別に展開する機能です。

- `...animalBox` → `'🐰', '🧸', '🐱'` に展開される
- `...birdBox` → `'🦩', '🦚'` に展開される
- 結果として新しい配列 `['🐰', '🧸', '🐱', '🦩', '🦚']` が作成される

この方法により、元の配列を変更することなく、新しい配列を作成できます。

## 視覚的な説明

![スプレッド演算子の図解](./image/スプレッド演算子/image.png)

## 🔥 練習問題

問題 1：配列の合体
2 つの配列 vegetables と fruits があります。スプレッド演算子を使って、これらを 1 つの saladIngredients 配列にまとめてください。

```ts
const vegetables = ['レタス', 'きゅうり', 'トマト'];
const fruits = ['アボカド', 'オレンジ'];

// ↓ここにコードを書いてください
const saladIngredients = /* ??? */;

console.log(saladIngredients);
// 期待する結果: ['レタス', 'きゅうり', 'トマト', 'アボカド', 'オレンジ']
```

問題 2：配列のコピー
originalPencils 配列があります。スプレッド演算子を使って、この配列の完全なコピーを copiedPencils として作成してください

```ts
const originalPencils = ['赤', '青', '黄'];

// ↓ここにコードを書いてください
const copiedPencils = /* ??? */;

// コピーが正しくできているか確認
copiedPencils.push('緑'); // コピーした配列だけを変更
console.log('オリジナル:', originalPencils); // => ['赤', '青', '黄'] のままであるべき
console.log('コピー:', copiedPencils);   // => ['赤', '青', '黄', '緑'] になるべき
```

問題 3：配列への要素の追加
mainDishes 配列があります。この配列の先頭に 'スープ'、末尾に 'デザート' を追加した新しい fullCourse 配列をスプレッド演算子を使って作成してください。

```ts
const mainDishes = ['魚料理', '肉料理'];

// ↓ここにコードを書いてください
const fullCourse = /* ??? */;

console.log(fullCourse);
// 期待する結果: ['スープ', '魚料理', '肉料理', 'デザート']
```

問題 4：オブジェクトのマージ
キャラクターの基本情報 profile と戦闘能力 stats があります。スプレッド演算子を使って、これらを 1 つの character オブジェクトにまとめてください。

```ts
const profile = { name: 'うさぎ', level: 10 };
const stats = { hp: 100, attack: 50 };

// ↓ここにコードを書いてください
const character = /* ??? */;

console.log(character);
// 期待する結果: { name: 'うさぎ', level: 10, hp: 100, attack: 50 }
```

問題 5：オブジェクトのプロパティ上書き
user オブジェクトの isLoggedIn プロパティを true に変更した新しいオブジェクト loggedInUser を作成してください。元の user オブジェクトは変更してはいけません。

```ts
const user = { id: 123, name: 'ゲスト', isLoggedIn: false };

// ↓ここにコードを書いてください
const loggedInUser = /* ??? */;

console.log('変更前:', user);         // => { id: 123, name: 'ゲスト', isLoggedIn: false }
console.log('変更後:', loggedInUser); // => { id: 123, name: 'ゲスト', isLoggedIn: true }
```

問題 6：関数の引数として配列を展開
数値の配列 numbers があります。Math.max() 関数は複数の数値を引数にとり、その中の最大値を返します。スプレッド演算子を使って、numbers 配列の中の数値を Math.max() に渡して、最大値を見つけてください。

```ts
const numbers = [15, 88, 7, 42];

// ↓ここにコードを書いてください
const maxNumber = /* ??? */;

console.log(maxNumber);
// 期待する結果: 88
```
