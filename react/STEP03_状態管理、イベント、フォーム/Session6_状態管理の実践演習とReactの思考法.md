
# Session6: 状態管理の実践演習とReactの思考法

## セクション概要

このセッションでは、これまで学んだ状態管理の知識を実践的な演習を通じて深めていきます。単純な状態の使い方から一歩進んで、より複雑な状態管理のパターンを学び、実際のアプリケーション開発で必要となるスキルを身につけます。

私の経験上、Reactの状態管理の仕組み自体を理解するのはそれほど難しくありません。

しかし、実際の開発現場で様々な状況において「どのように」状態を使うかを理解するのは、実はかなり難しいものです。

これこそが、React開発者として身につけるべき最も重要なスキルの一つです。

そのため、このセッションでは追加の演習を用意し、より多くの状況と実践の機会を提供して、一緒に構築していきます。

また、セッションの後半では「Reactの思考法」について理論的な基盤を築きます。これは、プロフェッショナルなReactアプリケーションを構築するために必要不可欠な考え方です。

## 演習1: フラッシュカードアプリケーションの実装

### 学習目標

- 一つの状態で複数の要素を制御する方法を理解する
- 条件分岐を使った動的なUI表示を実装する
- イベントハンドリングの実践的な使い方を習得する
- 複雑に見える機能を単純な状態管理で実現する方法を学ぶ

### プロジェクト概要と動作デモ

さあ、早速始めていきましょう。

これから構築するのは、フラッシュカードアプリケーションです。実際には小さなプロジェクトと言えるでしょう。

フラッシュカードとは、基本的に片面に質問があり、クリックすると反対面に答えが表示されるカードのことです。

多くの人が学習に使用している、カードを回転させるようなイメージです。

このアプリケーションの動作を詳しく説明しましょう：

- カードをクリックすると、質問から答えに切り替わります
- 再度同じカードをクリックすると、基本的にカードが裏返って元に戻ります
- 別のカードをクリックすると、そのカードが開きます
- あるカードが開いている状態で別のカードをクリックすると、最初のカードが閉じて新しいカードが開きます

これは楽しい小さな演習になります。今は非常に複雑に見えるかもしれませんが、実際には**たった一つの状態**ですべてを制御できます。

### 開発環境の準備

この講義のリンクにスターターファイルを追加しました。CodeSandboxを使用します。

ここには、すべての質問を含む配列がすでに用意されています。再び、オブジェクトの配列です。

これから構築するのは、このフラッシュカードです。

あなたがすべきことは、このCodeSandboxをフォークするか、コード全体をコピーして自分のコンピューターのコードエディターに貼り付けることです。選択はあなた次第です。

私がすることは、これをフォークすることです。これにより、基本的に自分のCodeSandboxにコピーが作成されます。

### 実装のポイント

このアプリケーションの興味深い点は、複雑に見える機能を**たった一つの状態**で制御できることです。

その状態は、現在アクティブな質問のIDを追跡します。

### ステップ1: 静的なフラッシュカードのレンダリング

まず最初に、これらの異なるフラッシュカードをレンダリングしましょう。基本的には表面の部分のみ、つまりすべての質問を含む部分です。

各カードには、ID、質問、答えがあることがわかります。しかし、もちろんデフォルトでは質問のみが表示されます。

### データ構造の確認

```javascript
const questions = [
  {
    id: 3457,
    question: "What language is React based on?",
    answer: "JavaScript",
  },
  {
    id: 7336,
    question: "What are the building blocks of React apps?",
    answer: "Components",
  },
  {
    id: 8832,
    question: "What's the name of the syntax we use to describe a UI in React?",
    answer: "JSX",
  },
  {
    id: 1297,
    question: "How to pass data from parent to child components?",
    answer: "Props",
  },
  {
    id: 9103,
    question: "How to give components memory?",
    answer: "useState hook",
  },
  {
    id: 2002,
    question: "What do we call an input element that is completely synchronised with state?",
    answer: "Controlled element",
  },
];
```

各質問オブジェクトには、一意の`id`、`question`（質問）、`answer`（答え）が含まれています。

### リストのレンダリング実装

リストをレンダリングする方法はすでに知っています。

JavaScriptモードに入り、questions配列を取得してmapメソッドを使用します。

```javascript
{questions.map((question) => (
  // ここでJSXをレンダリング
))}
```

各要素を`question`としましょう。そして、各質問に対してJSXをレンダリングできます。

多くの場合、ここで追加のコンポーネントを作成します。現在構築している「Far Away」旅行リストアプリで行ったように。しかし、必須ではありません。

ここでJSXを直接返すこともできます。今回はそれを行います。

```javascript
{questions.map((question) => (
  <div>
    <p>{question.question}</p>
  </div>
))}
```

単純にdivを作成し、その中に質問を含むparagraphを配置します。`question.question`です。それだけです。

divを閉じて、paragraphも閉じます。

これで完成です。

### CSSクラスの追加

ここで不足しているのは、クラス名です...`flashcards`です。

すでにCSSを作成しているのがわかります。コードを自分のコンピューターにコピーする場合は、このstyle.cssの内容もコピーしてください。

### keyプロパティの追加

ここで問題が発生していることがわかります。keyに関連する問題です。

そうです、それぞれに一意のkeyが必要です。ここに追加できます。

```javascript
{questions.map((question) => (
  <div key={question.id}>
    <p>{question.question}</p>
  </div>
))}
```

`key`として`question.id`を使用しましょう。これらは一意の値だからです。

ご存知のように、mapの内部でリストとしてレンダリングする各要素には、一意のkeyが必要です。

リロードすると、これらのエラーは消えます。

これで、静的な部分がすべて構築されました。

### ステップ2: インタラクティブ機能の実装

今度は、これらのコンポーネント、実際にはこれらの要素のいずれかをクリックしたときに何かが起こるようにする必要があります。

どのようにこれを行うのでしょうか？

最初に言ったように、実際に必要なのは**一つの状態**だけです。その状態は、現在アクティブな質問がどの質問IDかを追跡します。

これが混乱するように聞こえる場合は、コードを書いてみましょう。

### 状態の定義

```javascript
const [selectedId, setSelectedId] = useState(null);
```

これを`selectedId`と呼び、`setSelectedId`とします。

`useState`を使用します。

ここでは`null`で開始したいと思います。なぜなら、デフォルトでは最初に質問が開かれていない、つまり最初に選択されていない状態にしたいからです。

`null`は基本的に「何もない」ということです。

ここで、Reactから`useState`をインポートする必要があります。

### 状態使用の3つのステップ

状態使用の3つのステップを思い出しましょう。いつも戻ってくるこの考え方は、アプローチの理解に役立つと思います。

1. **状態変数の定義** - すでに完了
2. **状態変数の使用**
3. **状態の更新**

2番目のポイントについて、今それを行いましょう。

selectedIDをここで何かに使用しましょう。

しかし、今回これは少し難しいですね。実際にselectedIDを何に使用する必要があるのでしょうか？

### 状態の使用方法の理解

理解しやすくするために、ここで`null`の代わりに実際のIDの一つを使用してみましょう。

```javascript
const [selectedId, setSelectedId] = useState(9103);
```

これが状態だとしましょう。

「How to give components memory」のこのカードをクリックしたと想像してください。

これをクリックしたとしましょう。これが状態になったとしましょう。

この状態をどのように使用したいのでしょうか？

基本的に、ここで質問を表示する代わりに、答えを表示したいのです。

それを行うことができます：

```javascript
{question.id === selectedId ? question.answer : question.question}
```

`question.id`が`selectedId`と等しい場合、`question.answer`を表示します。そうでなければ、もちろん`question.question`を表示します。

これで、実際に今回は答えを取得しました。

これが`useState`フックです。すでに動作しています。

### スタイリングの追加

スタイリングについても同じことを行いましょう。ここで、実際にこれは赤い背景になります。そのために作成したクラスを使用できます。

このdivに追加しましょう。

```javascript
className={question.id === selectedId ? "selected" : ""}
```

`className`、JavaScriptモードに入り、同じことを行います。

現在の`question.id`が`selectedId`と等しい場合、クラス名は`selected`になります。そうでなければ、何もありません。

素晴らしいです。

### 動作の理解

ここで何が起こっているかを確実に理解してください。

この配列をループするとき、これらの要素のそれぞれは、これらのdivの一つですね。

そして、これらの各divで、現在の質問オブジェクトにアクセスできます。そして、それぞれに一つのIDがあります。

そこで、そのIDを選択されたIDと比較できます。現在選択されているものです。

それが同じ場合、ここに書いたことが起こります。

選択された質問の場合、つまり選択されたIDがオブジェクト内のIDと等しい場合、単純に答えを表示します。

それ以外の場合、他のすべてのケースでは、質問自体を表示します。答えではありません。

### 状態をnullに戻す

```javascript
const [selectedId, setSelectedId] = useState(null);
```

これで、状態を設定する必要があります。これが3番目のステップです。

いつ状態を更新したいのでしょうか？

これらのdivの一つをクリックしたときです。

そこで`onClick`ハンドラーを配置します。

ボタンである必要はないことがわかります。

### イベントハンドラーの実装

```javascript
function handleClick(id) {
  setSelectedId(id);
}
```

コードを理解しやすくするために、ここで関数を作成しましょう。

`handleClick`と呼びましょう。

今のところは関数名だけです。

ここでその関数を渡す必要がありますね。しかし、それだけでは十分ではありません。

ここで`setSelectedId`を呼び出しますが、名前が示すように、選択された質問になるべき質問のIDが実際に必要です。

つまり、この関数はそのIDを受け取る必要があり、ここで設定できます。

```javascript
onClick={() => handleClick(question.id)}
```

この`handleClick`関数を呼び出す必要があります。そのIDで。単純に`question.id`です。

しかし、これで完了だと思う間違いを犯してはいけません。

実際、Reactはすでにここでエラーを出しています。

ここでは実際の関数を渡す必要があり、ここにあるような関数呼び出しではないことを覚えておいてください。

これは`handleClick`を即座に呼び出しています。それは私たちが望むことではありません。

Reactがイベントが発生するとすぐに関数を呼び出せるように、関数を渡したいのです。

```javascript
onClick={() => handleClick(question.id)}
```

これで十分です、少なくとも今のところは。

素晴らしく動作しました！

別のものをクリックすると、もちろんこれがアクティブになります。

### React DevToolsでの確認

React DevToolsを確認してみましょう。CodeSandboxには実際にReactツールが統合されています。

アプリ、実際にはflashcardsに来ると、状態が今9103であることがわかります。これはまさにこのIDです。

ここをクリックすると、更新されるはずです...

あまりうまく動作しないようですが、まあ、気にしないでおきましょう。これを閉じましょう。

重要なのは、少なくともある程度動作していることです。ここを再度クリックしたときに何が起こるかを見てください。

何も起こりません。もちろんです。なぜなら、ここでは単純にそのIDを渡し、そのIDを選択されたIDとして設定しているからです。

期待通りです。

### トグル機能の実装

しかし、ここで見るように、再度クリックすると実際にカードが再び閉じます。

カード間の変更機能はすでにありますが、クリックしたときの機能はまだありません。

それを行いましょう。

それほど難しくありません。

基本的に、ここでこのIDを条件に基づいて設定したいのです。

```javascript
function handleClick(id) {
  setSelectedId(id !== selectedId ? id : null);
}
```

質問IDが現在選択されているものと異なる場合、結果はIDになります。そうでなければ、nullに戻します。

ここで問題があります。

ああ、等号が一つ多すぎます。

再び、質問IDがすでに選択されたIDと異なる場合、IDを設定します。

これはまさにこの動作ですね。

しかし、そうでない場合、基本的に質問IDがすでに選択されたIDと等しい場合、nullに戻します。

nullの場合、どれもアクティブになりません。

はい、それは動作し、理にかなっていると思います。

### 完全なFlashCardsコンポーネント

```javascript
function FlashCards() {
  const [selectedId, setSelectedId] = useState(null);

  function handleClick(id) {
    setSelectedId(id !== selectedId ? id : null);
  }

  return (
    <div className="flashcards">
      {questions.map((question) => (
        <div
          key={question.id}
          onClick={() => handleClick(question.id)}
          className={question.id === selectedId ? "selected" : ""}
        >
          <p>
            {question.id === selectedId ? question.answer : question.question}
          </p>
        </div>
      ))}
    </div>
  );
}
```

ここでの状態の使用は、以前に行ったものとはかなり異なっていました。

そのため、このセクションの最後にこの演習を含めることにしました。

ここで何が起こっているかを正確に学習してください。

その後、このセクションの最終的なコーディングチャレンジの準備ができていると思います。

### 完全なプロジェクトファイル

#### app.js

```javascript
import "./styles.css";
import { useState } from "react";

export default function App() {
  return (
    <div className="App">
      <FlashCards />
    </div>
  );
}

const questions = [
  {
    id: 3457,
    question: "What language is React based on?",
    answer: "JavaScript",
  },
  {
    id: 7336,
    question: "What are the building blocks of React apps?",
    answer: "Components",
  },
  {
    id: 8832,
    question: "What's the name of the syntax we use to describe a UI in React?",
    answer: "JSX",
  },
  {
    id: 1297,
    question: "How to pass data from parent to child components?",
    answer: "Props",
  },
  {
    id: 9103,
    question: "How to give components memory?",
    answer: "useState hook",
  },
  {
    id: 2002,
    question:
      "What do we call an input element that is completely synchronised with state?",
    answer: "Controlled element",
  },
];

function FlashCards() {
  const [selectedId, setSelectedId] = useState(null);

  function handleClick(id) {
    setSelectedId(id !== selectedId ? id : null);
  }

  return (
    <div className="flashcards">
      {questions.map((question) => (
        <div
          key={question.id}
          onClick={() => handleClick(question.id)}
          className={question.id === selectedId ? "selected" : ""}
        >
          <p>
            {question.id === selectedId ? question.answer : question.question}
          </p>
        </div>
      ))}
    </div>
  );
}
```

#### index.js

```javascript
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";

import App from "./App";

const rootElement = document.getElementById("root");
const root = createRoot(rootElement);

root.render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

#### style.css

```css
* {
  box-sizing: border-box;
}

body {
  font-family: sans-serif;
  padding: 20px;
}

.flashcards {
  display: grid;
  grid-template-columns: 1fr 1fr 1fr;
  gap: 20px;
}

.flashcards div {
  border: 1px solid #e7e7e7;
  background-color: #f7f7f7;
  border-radius: 7px;
  aspect-ratio: 2 / 1;
  display: flex;
  align-items: center;
  justify-content: center;
  text-align: center;
  padding: 20px;
  cursor: pointer;
}

div.selected {
  border: 1px solid #e03131;
  background-color: #e03131;
  color: #fff;
  font-weight: bold;
}
```

### 学習のポイント

この演習で学んだ重要なポイントを整理しましょう：

1. **単一状態による複雑な制御**: 複雑に見える機能も、適切に設計された一つの状態で制御できる
2. **条件分岐の活用**: 状態の値に基づいて表示内容やスタイルを動的に変更する
3. **イベントハンドリングの実践**: クリックイベントを使った状態の更新とトグル機能の実装

## 演習2: 日付カウンター（バージョン2）

### 学習目標

- 制御されたコンポーネントの実装を実践する
- 複数の状態を組み合わせた機能の構築
- 条件付きレンダリングの応用
- 数値変換とバリデーションの実装

### アップグレード内容

このセクションを締めくくるために、以前に開始した日付カウンターチャレンジの続きを行いましょう。

このチャレンジのパート2では、以前に構築した日付カウンターをアップグレードします。

これらのボタンをクリックして日付を進めることはまだできますが、今度はここでカウントを定義することで直接日付に移動することもできます。

例えば、今から1000日後は...この日付になります。

10,000や100,000でも、お好みで続けることができます。

1に戻しましょう。

これがカウンターのアップグレードです。

ステップも異なります。

以前のようなボタンの代わりに、今度はこのスライダーがあります。

ここをクリックすると、この7が上がりますが、7はこの素敵なスライダーを使用して定義されました。

このスライダーは実際には別のタイプのHTML入力要素ですが、すぐにお見せします。

最後に、リセット機能もあります。

リセットをクリックすると、ここで0に戻り、ステップで1に戻ります。

このボタンは、これら2つのうちの1つが変更されたときにのみ表示されます。

戻って、それを行いましょう。


これにより、基本的にその他のもののコピーが作成され、最初のものを変更することなく、ここでコードを書き続けることができます。

### スライダー要素の実装

ここで素早くお見せしたいのは、そのスライダーです。

入力を使用できます。

言ったように、これは今度は入力で、タイプはrangeです。

```javascript
<input
  type="range"
  min="0"
  max="10"
/>
```

最小値を0に設定し、最大値を10に設定できます。

見てみましょう。

これで完成です。

今度は、これはもちろん制御されていない要素です。

現在、Reactはこの値の制御を担当していません。

想像できるように、あなたがする必要があるのは、この入力要素を制御された要素にすることです。

そして、こちらのこの要素も同様です。

これにより、少し前に学んだことを実践できますね。

疑問がある場合は、その講義に戻って、そこで書いたコードを確認してください。

### テキスト入力要素の実装

結果をお見せしますので、何を構築する必要があるかがわかります。

このチャレンジで頑張ってください。準備ができたら、数分後にここでお会いしましょう。

さあ、基本的にカウンターコンポーネントを素早く強化しましょう。

実際、ここではほとんどのことが変更されません。

すでにステップ状態があります。

今度はこの入力要素と接続するだけです。

この要素の値は、もちろんそのステップ状態によって制御されるべきです。

```javascript
<input
  type="range"
  min="0"
  max="10"
  value={step}
  onChange={(e) => setStep(Number(e.target.value))}
/>
```

そして、以前と同じように、onChangeハンドラーが必要です。

現在のイベントを取得し、そのイベントに基づいてステップを設定します。`event.target.value`です。

そして、ここでこのボタンを削除できます。

見てみましょう。

はい、すでに動作しています。

実際、うまく動作していませんね。

10を文字列として追加しています。

コードにバグがあります。

### 数値変換の必要性

問題は、この値が文字列として取得されることです。

これは実際に以前にあった問題で、React DevToolsを使用して検出しました。

実際、DevToolsもここで利用できるので、同じことをここで確認できます。

状態は10です。

10は文字列です。

以前に行ったことを行うだけです。これは、この値を状態に配置する前に数値に変換することです。

```javascript
onChange={(e) => setStep(Number(e.target.value))}
```

ここでリロードしましょう。

時間がかかります。

何かに設定しましょう。

今度は動作しています。

ここで何かが正しくないようですが、何らかの理由で、重要なのはここで実際に動作していることです。

CodeSandboxでの統合があまり良くないのかもしれません。

しかし、まあ、気にしないでおきましょう。重要なのは、この部分がすでに動作していることです。

今度は、ここでも入力要素を作成するだけです。

それもそれほど難しくないはずです。

このspanはもう必要ありませんが、代わりに入力を使用します。

```javascript
<input
  type="text"
  value={count}
  onChange={(e) => setCount(Number(e.target.value))}
/>
```

タイプはtext、値はcount、そしてonChange。

基本的に以前とほぼ同じことを行いたいと思います。

現在のイベントを取得し、setCount、そして今度は変換部分を忘れません。

数値に変換します。`e.target.value`。

今度は、この要素を閉じるだけです。

保存します。

これはすべて正しく見えます。

見てみましょう。

何かが起こっています。React Toolsを閉じましょう。

とにかく、確実にするためにここでリロードしましょう。

これは動作します。

ここで値を入力すると、はい、素晴らしいです。

これは本当に、本当に素晴らしいです。

これにより、コンポーネントをもう少し制御でき、今からの日付を計算できるので、非常に役立ちます。

### リセット機能の実装

しかし、ここで行ったことを素早く確認しましょう。

これら2つの入力要素は、以前に見たのと同じ理由で、今度は制御された要素です。

状態があり、その状態、例えばここではステップを入力要素と接続します。

この入力フィールドにステップ状態の値を取るように強制します。

しかし、それだけでは十分ではありません。なぜなら、それだけでは、ここで値を変更することができないからです。

そこで、実際に変更しようとするイベント、つまり基本的にここでスライドするイベントを処理する必要があります。

スライドするたびに変更イベントが発生します。

そして、それが起こるたびに、要素の現在の値でステップを設定したいと思います。

それが`e.target.value`です。

素晴らしいです。最後に、不足しているのはそのリセット、つまりここのボタンです。

ここに下にボタンを作成しましょう。

そのボタンを配置するdivを作成しましょう。

```javascript
<div>
  <button onClick={handleReset}>Reset</button>
</div>
```

リセット、それを閉じて、divを閉じます。

クリックで何かが起こることを望むので、onClickプロパティを使用します。

ここで、実際にこれを別の関数として行いましょう。なぜなら、ここで2つの状態を設定する必要があるからです。

これを`handleReset`と呼びましょう。

もちろんそれは存在しないので、この苦情を受けます。それを修正しましょう。`handleReset`。

ここで何が起こるのでしょうか？

セッター関数を使用して、これら2つを初期状態に戻すだけです。

```javascript
function handleReset() {
  setCount(0);
  setStep(1);
}
```

`setCount`は0に戻り、ステップは1に戻ります。

はい、それは動作します。

### 条件付きレンダリング

私がお見せしたデモと唯一異なるのは、実際にリセットするものがないときにこのリセットボタンを表示すべきではないということです。

基本的に、ここでこの部分を条件付きでレンダリングしたいと思います。

JavaScriptブロックでラップしましょう。

そして、次のように言うことができます：

```javascript
{(count !== 0 || step !== 1) ? (
  <div>
    <button onClick={handleReset}>Reset</button>
  </div>
) : null}
```

countが0と等しくない場合、またはstepが1と異なる場合。

countが0と異なる場合、またはstepが1と異なる場合。

0はcountのデフォルトで、1はstepのデフォルトです。

これらのデフォルトの1つが異なる場合、この部分を条件付きでレンダリングしたいと思います。

そのために、再びandオペレーターを使用できます。

または、なぜか、今回は三項演算子を使用しましょう。

この場合、これをレンダリングし、そうでなければ何もレンダリングしません。

この周りに括弧を置くべきかもしれません。

これが最初に評価される1つの条件であることを確認するためです。

これを変更するとすぐに、ボタンが表示されます。

または、これを変更するとすぐに、同じです。

美しいです。これはまさに私たちが構築したかったものです。

チャレンジが完了し、セクションが完了しました。

時間を無駄にせず、次のセクションの次の講義で「Far Away」リストの作業を続けましょう。

### 完全なコンポーネント実装

```javascript
import { useState } from "react";
import "./styles.css";

export default function App() {
  return (
    <div className="App">
      <Counter />
    </div>
  );
}

function Counter() {
  const [count, setCount] = useState(0);
  const [step, setStep] = useState(1);

  function handleReset() {
    setCount(0);
    setStep(1);
  }

  const date = new Date("june 21 2027");
  date.setDate(date.getDate() + count);

  return (
    <div>
      <div>
        <input
          type="range"
          min="0"
          max="10"
          value={step}
          onChange={(e) => setStep(Number(e.target.value))}
        />
        <span>Step: {step}</span>
      </div>

      <div>
        <button onClick={() => setCount((c) => c - step)}>-</button>
        <input
          type="text"
          value={count}
          onChange={(e) => setCount(Number(e.target.value))}
        />
        <button onClick={() => setCount((c) => c + step)}>+</button>
      </div>

      <p>
        <span>
          {count === 0
            ? "Today is "
            : count > 0
            ? `${count} days from today is `
            : `${Math.abs(count)} days ago was `}
        </span>
        <span>{date.toDateString()}</span>
      </p>

      {count !== 0 || step !== 1 ? (
        <div>
          <button onClick={handleReset}>Reset</button>
        </div>
      ) : null}
    </div>
  );
}
```

#### index.js

```javascript
import { StrictMode } from "react";
import { createRoot } from "react-dom/client";

import App from "./App";

const rootElement = document.getElementById("root");
const root = createRoot(rootElement);

root.render(
  <StrictMode>
    <App />
  </StrictMode>
);
```

#### style.css

```css
.App {
  font-family: sans-serif;
  text-align: center;
}
```

### 制御されたコンポーネントの利点

この演習で確認したことを素早く振り返りましょう。

これら2つの入力要素は、以前に見たのと同じ理由で、制御されたコンポーネントになりました。

状態があり、その状態（例えば、ここではstep）を入力要素と接続します。

この入力フィールドにstep状態の値を取るように強制します。

しかし、それだけでは十分ではありません。なぜなら、それだけでは値を変更することができないからです。

そこで、実際に変更しようとするイベントを処理する必要があります。基本的にここでスライドするイベントです。

スライドするたびに変更イベントが発生し、それが起こるたびに、要素の現在の値でstepを設定したいと思います。

それが`e.target.value`です。

## Reactの思考法とは何か？

### Reactの思考法の核心

セクションを開始するために、Reactの思考法が実際に何についてのものかを発見しましょう。

今までに気づいたかもしれませんが、Reactアプリケーションの構築には完全に新しい考え方が必要です。

なぜなら、バニラJavaScriptでアプリケーションを構築するのとは非常に異なるからです。

Reactアプリを構築するには、useStateのようなすべての異なる関数でReact APIを実際に使用する方法を学ぶだけでなく、Reactで考えることもできる必要があります。

基本的にReactの考え方に入る必要があります。

これら両方のスキルを持つと、Reactをマスターし、プロフェッショナルなReactアプリケーションを構築する道を順調に進むことができます。

Reactで考えるとは実際に何を意味するのでしょうか？

アプリケーションを構築している間、Reactで考えるということは、コンポーネント、状態、プロパティ、一般的なデータフロー、エフェクトなど、すべてのReactツールをいつ、どのように使用するかについて非常に良いメンタルモデルを持っていることを意味します。

また、以前に学んだように、要素の変更ではなく、常に状態の遷移の観点から考えることでもあります。

### Reactの思考法のプロセス

Reactの思考法を、より構造化された方法でアプリを構築するのに役立つ全体的なプロセスとして見ることもできます。

このプロセスの最初のステップは、望ましいUIをコンポーネントに分割し、これらのコンポーネントが互いにどのように関連しているかを確立することです。

つまり、コンポーネントツリーを確立することです。

これには、コンポーネントの再利用性と構成可能性について考えることも含まれます。

その後、アプリケーションの静的バージョンの構築を開始できます。

状態やインタラクティビティなしで、これは素晴らしいことです。

なぜなら、これを行うことで、状態やインタラクティビティについて心配する前に、ほとんどのコーディングを前もって行うからです。

その部分は次に来ます。

#### 1. UIをコンポーネントに分割

ステップ3では状態について考えます。

ここで、いつ状態が必要か、どのタイプの状態が必要か、各状態をどこに配置するかを決定します。

#### 2. 静的バージョンの構築

最後に、アプリケーション全体でデータがどのように流れるかを確立することについて考えます。

これには、一方向データフロー、子から親への通信、グローバル状態へのアクセス方法について考えることが含まれます。

#### 3. 状態について考える

これらのポイント、3と4は、状態管理と総称するもので、このセクションの主な焦点です。

#### 4. データフローの確立

もちろん、これは常に文字通りに従う必要がある厳格なプロセスではありません。

実際には、これらの異なるステップ間で常に多くの行き来があり、物事は決してこのように線形ではありません。

しかし、全体的なガイドラインとしてこのようなプロセスを持つことは良いことです。

### 状態管理の重要性

さて、Reactで考える方法を本当に知ると、次のような質問に答えることができるようになります：

### Reactの思考法で答えられる質問

#### コンポーネント設計について

- UIデザインをコンポーネントに分割する方法
- コンポーネントの一部を真に再利用可能にする方法
- 再利用可能なコンポーネントからユーザーインターフェースを組み立てる方法

#### 状態管理について

また、状態についても多く考えます：

- 望むインタラクティビティのためにどの状態が必要か
- 各状態をどこに配置するか、言い換えれば、どのコンポーネントが実際に各状態を所有すべきか
- どのタイプの状態を使用できるか、または使用すべきか
- より一般的な用語では、アプリケーション全体でデータをどのように流すか

### 学習アプローチ

お気づきかもしれませんが、これらのトピックの一部については以前に話し始めましたが、これらのスキルの一部に本当に焦点を当てたセクションを1つ持ちたいと思いました。

そうすることで、Reactの考え方にますます入っていくことができます。

もちろん、これらのスキルを本当にマスターするのは、実践とコードを書くことによってのみです。

幸運なことに、このコースでそれをたくさん行います。

しかし、このビデオのように時々少し理論的なアプローチを取ることもあります。

なぜなら、React API自体以外にもこれらのことを教えることが本当に、本当に重要だと信じているからです。

### プロセスの柔軟性

そうすることで、このコースを可能な限りあなたにとって良いものにし、私の意見では、そこにある他のすべてのReactコースよりもはるかに良いものにします。

### 継続的な学習の重要性

とにかく、今度は状態管理の基本を見ることに移りましょう。

## まとめ

このセッションでは、状態管理の実践的な演習を通じて、Reactの核心的なスキルを深めました。

### 技術的な学習内容

1. **フラッシュカードアプリケーション**: 単一の状態で複雑な機能を制御する方法
2. **日付カウンター（v2）**: 制御されたコンポーネントと複数状態の組み合わせ
3. **条件付きレンダリング**: 状態に基づく動的なUI表示
4. **イベントハンドリング**: 実践的なユーザーインタラクションの実装

### 概念的な学習内容

1. **Reactの思考法**: プロフェッショナルな開発に必要な考え方
2. **状態管理の重要性**: React開発者として最も重要なスキル
3. **構造化されたアプローチ**: UIの分割から状態管理まで

### 次のステップ

これらの演習で学んだパターンは、より大きなアプリケーションでも応用できます。次のセクションでは、これらの知識を「Far Away」リストアプリケーションの開発に活用していきます。

状態管理の実践を通じて、Reactの思考法を身につけ、プロフェッショナルなReactアプリケーション開発への道を歩んでいきましょう。
ここには前のチャレンジのコードがまだあります。今度はこれをフォークします。

これにより、基本的にその他のもののコピーが作成され、最初のものを変更することなく、ここでコードを書き続けることができます。

### スライダー要素の実装

ここで素早くお見せしたいのは、そのスライダーです。

入力を使用できます。

言ったように、これは今度は入力で、タイプはrangeです。

```javascript
<input
  type="range"
  min="0"
  max="10"
/>
```

最小値を0に設定し、最大値を10に設定できます。

これで完成です。

今度は、これはもちろん制御されていない要素です。

現在、Reactはこの値の制御を担当していません。

想像できるように、あなたがする必要があるのは、この入力要素を制御された要素にすることです。

そして、こちらのこの要素も同様です。

これにより、少し前に学んだことを実践できますね。

疑問がある場合は、その講義に戻って、そこで書いたコードを確認してください。

### テキスト入力要素の実装

結果をお見せしますので、何を構築する必要があるかがわかります。

このチャレンジで頑張ってください。準備ができたら、数分後にここでお会いしましょう。

さあ、基本的にカウンターコンポーネントを素早く強化しましょう。

実際、ここではほとんどのことが変更されません。

すでにステップ状態があります。

今度はこの入力要素と接続するだけです。

この要素の値は、もちろんそのステップ状態によって制御されるべきです。

```javascript
<input
  type="range"
  min="0"
  max="10"
  value={step}
  onChange={(e) => setStep(Number(e.target.value))}
/>
```

そして、以前と同じように、onChangeハンドラーが必要です。

現在のイベントを取得し、そのイベントに基づいてステップを設定します。`event.target.value`です。

そして、ここでこのボタンを削除できます。

見てみましょう。

はい、すでに動作しています。

実際、うまく動作していませんね。

10を文字列として追加しています。

コードにバグがあります。

### 数値変換の必要性

問題は、この値が文字列として取得されることです。

これは実際に以前にあった問題で、React DevToolsを使用して検出しました。

実際、DevToolsもここで利用できるので、同じことをここで確認できます。

状態は10です。

10は文字列です。

以前に行ったことを行うだけです。これは、この値を状態に配置する前に数値に変換することです。

```javascript
onChange={(e) => setStep(Number(e.target.value))}
```

ここでリロードしましょう。

時間がかかります。

何かに設定しましょう。

今度は動作しています。

ここで何かが正しくないようですが、何らかの理由で、重要なのはここで実際に動作していることです。

CodeSandboxでの統合があまり良くないのかもしれません。

しかし、まあ、気にしないでおきましょう。重要なのは、この部分がすでに動作していることです。

今度は、ここでも入力要素を作成するだけです。

それもそれほど難しくないはずです。

```javascript
<input
  type="text"
  value={count}
  onChange={(e) => setCount(Number(e.target.value))}
/>
```

このspanはもう必要ありませんが、代わりに入力を使用します。

タイプはtext、値はcount、そしてonChange。

基本的に以前とほぼ同じことを行いたいと思います。

現在のイベントを取得し、setCount、そして今度は変換部分を忘れません。

数値に変換します。`e.target.value`。

今度は、この要素を閉じるだけです。

保存します。

これはすべて正しく見えます。

見てみましょう。

何かが起こっています。React Toolsを閉じましょう。

とにかく、確実にするためにここでリロードしましょう。

これは動作します。

ここで値を入力すると、はい、素晴らしいです。

これは本当に、本当に素晴らしいです。

これにより、コンポーネントをもう少し制御でき、今からの日付を計算できるので、非常に役立ちます。

### リセット機能の実装

しかし、ここで行ったことを素早く確認しましょう。

これら2つの入力要素は、以前に見たのと同じ理由で、今度は制御された要素です。

状態があり、その状態、例えばここではステップを入力要素と接続します。

この入力フィールドにステップ状態の値を取るように強制します。

しかし、それだけでは十分ではありません。なぜなら、それだけでは、ここで値を変更することができないからです。

そこで、実際に変更しようとするイベント、つまり基本的にここでスライドするイベントを処理する必要があります。スライ