# 高度なuseReducerフック

このセクションでは、高度な`useReducer`フックについて深く掘り下げていきます。

この特別なフックは、状態を管理するためのもう一つの強力な方法を提供してくれます。そして、Reactでは状態が非常に重要なので、`useReducer`は非常に重要なフックです。

また、後でReduxを理解するためにも、`useReducer`をマスターすることが重要です。

そのため、`useReducer`フックに特化したセクションを設けました。

さて、このセクションはかなり長くなりますが、それは`useReducer`を使ってこの新しいプロジェクト全体を構築するからです。

これまでに学んだ他のすべてのスキルを強化します。

時間を節約したい場合は、このセクションの最初の5つか6つのビデオを見るだけで十分です。

ということで、早速始めましょう。

# もう一つのフック：useReducer

この講義と次の講義で、`useReducer`フックを実際にどのように使用するかについて、まず高レベルの概要から始めましょう。

そして、いつものように、また新しいプロジェクトを作成する時が来ました。

フォルダに移動して、バージョン5の`NPX Create-React-App`を実行しましょう。

そして、このセクションで構築するアプリは、`React Quiz`と呼ばれます。

さて、ですね？そして、Create-React-Appが必要なすべてのパッケージのインストールを完了するまで待ちましょう。

さて、ではいつものクリーンアップの時間です。

まず、これをリネームします。もう10番目のプロジェクトですね。

そして、いつものように、この辺りのものをすべて削除します。

よし、ここからスターターを取得する。今回は5つのスターターファイルがある。記録かもしれないな。

次に、CSSファイルを置き換えて、フォルダをVS Codeにドラッグ＆ドロップしましょう。そして、いつものように、不要なものはすべてここから削除します。

残りは大丈夫です。アップコンポーネントが必要で、CSSファイルを含める必要があります。これは、いつものように、ここにあるスターターファイルの一部です。

では、appファイルとdate countファイルを開くことから始めましょう。これらはすぐに必要になります。

そして、ここで、すべてを削除し、appコンポーネントをゼロから始めましょう。

`export default function app`です。そして、ここに`div`を作成しましょう。

そして、この`div`で、`DateCounter`コンポーネントをすぐに呼び出します。

`DateCounter`です。そして、それを`DateCounter`からインポートする必要があります。

では、見てみましょう。統合ターミナルを開き、`NPM start`です。

アプリケーションの起動に成功した場合、これが表示されるはずです。

では、アプリも閉じましょう。なぜなら、最初の2つの講義では、`DateCounter`のみを扱うからです。

この`DateCounter`は、以前のチャレンジの1つで実装したものと同じです。

つまり、基本的に、これらの値の1つをクリックすると、日付が増加します。

ここでその数値を更新することもでき、日付も増加することがわかります。

そして、このスライダーで値を変更できます。これは基本的にステップです。

ここをクリックすると、ここのステップ値だけ増加します。

さて、ですね。それで、今からやることは、基本的にこれらの2つの`usestate`フックを`Usereducer`フックに置き換えることです。

だから、このコードが何をするのか、そしてそれがどのように機能するのかを正確に理解することが非常に重要です。

それで、もしあなたがチャレンジをしなかったり、本当に覚えていない場合は、今数分かけてこのコードを本当に確認してください。

ここにあるすべてのイベントハンドラーと、それらがこれらの2つの状態をどのように更新するか、そして状態が互いにどのように相互作用するかです。

この講義では、すべてがどのように機能するかを本当に確信した場合にのみ進んでください。

さて、ですね。しかし、とにかく、最後に`usereducer`フックについて話しましょう。

`usereducer`フックは、基本的に、`usestate`フックの代わりに状態を管理するための、より高度で複雑な方法です。

`usereducer`フックは、いわゆる`reducer`関数と連携して機能します。これは、常に前の状態と、引数としていわゆるアクションを受け取り、次の状態を返す純粋な関数です。

しかし、それについて話す代わりに、実際に使ってみましょう。そして、ここにあるこの最初の`count`状態を置き換えることから始めます。

では、再び`count`と書き、次に`dispatch`と書きます。そして、これらの意味を1分で説明します。そして、次に`usereducer`です。

そして、このフックは、初期状態だけでなく、先ほど話した`reducer`関数も受け取ります。

では、すでにここにそれを書き、1分で機能不全を作成します。それが最初の引数です。

そして、2番目の引数は再び初期状態です。そして、それは再びゼロです。では、この状態をコメントアウトし、次にコンポーネントの外で、その`reducer`関数を作成しましょう。

それは単なる`function reducer`です。そして、言ったように、この`reducer`関数は、引数として現在の状態とアクションを取ります。

そして、最初に行うことは、これら2つをコンソールにロックすることです。さて、ですね。

さて、いくつかエラーが表示されました。では、ここのこのコードをすべてコメントアウトしましょう。

実際にはもうこれらは必要ありません。そして、`usereducer`フックもアプリケーションに含める必要があります。

よし、ここでリロードしてみよう。そして、ほら。

さて、`reducer`関数がありますが、この関数は実際にいつ呼び出されるのでしょうか？まあ、それがこの`dispatch`が実際に登場する場所です。

`usereducer`フックは現在の状態を返すことがわかります。`useState`フックと同じです。

しかし、状態更新関数も返す代わりに、`reusereducer`フックは2番目のものとして、この`dispatch`関数を返します。

そして、この`dispatch`関数も状態を更新するために使用できます。ただ、少し異なる方法で機能します。

では、この関数をここで呼び出しましょう。そして、数値1を渡しましょう。

そして、実際には`increment`イベントハンドラーで行いましょう。つまり、ここにあるこのボタンをクリックしたときに呼び出されるものです。

では、このボタンを実際にクリックしましょう。そして、ここで出力が得られます。これを少し大きくしましょう。

そして、ここから得られる出力、つまりコンソールログは0と1です。つまり、状態は0で、アクションは1です。

なぜでしょうか？まあ、それは`reducer`関数が、最初に言ったように、現在の状態にアクセスできるからです。

現在はゼロです。つまり、初期状態です。そして、アクションにもアクセスできます。

現在は、単に`dispatch`関数に渡したこの1です。つまり、これは基本的に`reducer`のアクションになります。

そして、`reducer`のアイデアは、これら2つ、つまり現在の状態とアクションを取り、それに基づいて次の状態を返すことです。

では、そうしましょう。やりたいことは、この場合、状態とアクションを返すことだけです。

そして、ここで返されたものは何であれ、新しい状態になります。つまり、現在、ここにある値は1になるはずです。

リロードしてみよう。そして、ほら。

状態が実際に正常に更新されました。さて、ここをクリックしても、もちろん何も起こりませんが、それを修正しましょう。

では、再び`dispatch`関数を呼びましょう。`usereducer`の用語では、アクションをディスパッチすると言います。

ここで、-1のアクションをディスパッチします。すると、その-1が現在の状態に追加され、値が減少します。

そして、それはうまく機能します。素晴らしい。さて、この時点では、これはあまり役に立ちませんよね？

つまり、同じ機能を得るために、多くの追加作業をしなければなりませんでしたよね？

しかし、信じてください。ここを進むにつれて、はるかに役に立つようになります。

次に、状態をどのように設定するかについて考えましょう。なぜなら、ここにある数値を入力すると、実際に状態がその数値に更新されることを望んでいるからです。

それがここにある定義されたカウントです。しかし、これはこれまでやってきたのと同じようには本当に機能しません。

したがって、もし単に`E.target.value`をディスパッチする場合、それは単に現在の状態に追加されるだけですよね？

では、ここに何かを書きましょう。そして、今度は0を書きます。そうすれば、ここに10が表示されます。しかし、0を書いたとき、実際にはそれを状態に追加しました。

なぜなら、ここでは単にアクションを現在の状態に追加しているからです。そして、今度は別の0を書くと、それはすでにここにある状態に追加されます。

それは全く望んでいません。したがって、今度はここでアクションについて考え始める時が来ました。

つまり、この場合、3つのアクションがあります。カウントを減らすこと、増やすこと、そして設定することです。

そして、これらのアクションに名前を付ける必要があります。そこで、この値を渡すだけでなく、アクションとこの値を含むオブジェクトを渡すことにします。

では、`type`と書き、これを減らすために`Dec`と呼び、次に`Payload`を1に設定します。

さて、ですね。このオブジェクトこそが、`reducer`関数を扱うときにアクションと呼ぶものです。

そして、理論上、ここにあるこのオブジェクトは、私たちが望むどんな形でも持つことができます。しかし、常に`type`プロパティと`payload`プロパティだけを持つのは、一種の標準です。

実際には`playload`ではなく、本当に`payload`です。これは、`usereducer`フックを使用するときに常に表示される標準です。

そして、後で`Redux`でも同じことが言えます。そのことを聞いたことがある場合です。では、ここでも同じことをしましょう。

タイプ。これは`Inc`になり、ここにあるペイロードは1になります。よし？そして、今、これを行う代わりに、これらの異なるタイプを考慮に入れる必要があります。

では、`action.type`が`Inc`の場合、現在の状態と`action.payload`を返しましょう。

そして、もしそれが減少であれば、マイナスです。そして、`action.type`が`set count`であれば、後で作成しますが、実際には、単に`action.payload`を返します。

しかし、今からこれを実験してみましょう。そうすれば、もっと意味がわかるようになります。

では、リロードしましょう。そして、まずこれが機能するかどうか見てみましょう。そして、美しい。

本当に素晴らしい。そして、今度はここに`set count`というタイプの実際のアクションオブジェクトもディスパッチしよう。

そして、ここにあるペイロードは、このフィールドに入力された値になります。そして、今度はこれが実際に機能します。

つまり、私がここに入力した値は何であれ、`E.target.value`になり、これがここにあるこのアクションのペイロードになります。

そして、このアクションを私たちの`reducer`にディスパッチします。そして、そのアクションにはその値が含まれています。

ここにある700とそのタイプです。そして、そのタイプに基づいて、次の状態がまさにその値になるべきだと決定します。

この場合、700です。そして、もしこれらすべてが本当に混乱しているように見えたり、聞こえたりするなら、この時点で心配しないでください。

それで、今私たちは、これが実際にどのように機能するかについての概要を得ているだけですが、`usereducer`フックが何であるか、それが本当にどのように機能するかを正確に説明します。そして、ええ、

素敵な図を用意しますので、それまでにこれらすべてが非常に意味のあるものになることを願っています。さて、ここを終えるために、これについてもう少し考えましょう。

それで、実際に、ここにペイロードを渡す必要はありますか？まあ、実際にはありません。なぜなら、`reducer`は、減らしたいときに何が起こるかを実際に知っているはずだからです。

つまり、減少と増加は実際には1を加算または減算するだけです。そして、そのロジックをここで、`reducer`に直接追加する必要があります。

私たちの`reducer`は、これらのタイプのアクションをどのように実行するかを自分で知っているべきです。

そして、ここからペイロードを削除し、このアクションをディスパッチするのを少し簡単にすることができます。ここにある`payload`プロパティはオプションです。

そしてもちろん、この場合、まだそれが必要です。つまり、基本的に、まだこの値を私たちの`reducer`に渡す必要があります。

なぜなら、そうでなければ、もちろん、次の状態を設定する方法がないからです。さて、ですね。そして、これはまだ機能します。

そして、これで、ここにあるこの`count`状態を、単純な`useState`から`usereducer`に変換する最初の部分を終えました。

それで、ここで何が起こったのかを簡単にまとめてみましょう。この新しいフックは、ゼロであった初期状態だけでなく、ディスパッチ関数に渡す現在の状態とアクションに常にアクセスできる`reducer`関数も受け取ります。

この`dispatch`関数はどこから来たのでしょうか？それは`usereducer`が返すものの1つです。

これは常に現在の状態を返します。ここでは`Count`と呼ぶことにしました。そして、`dispatch`関数も返します。これを使って、このようなアクションをディスパッチできます。

慣例として、タイプを含むアクションをディスパッチし、オプションでペイロードもディスパッチします。目標は、`reducer`に何らかの値を渡すことです。

そして、私たちの`reducer`関数は、現在の状態と、次の状態を計算するためにアクションに含まれるすべての情報を取ります。

通常、アクションタイプに基づいて、`reducer`は何らかの決定を下します。例えば、`Inc`、つまりインクリメントであれば、単に1を加算します。

デクリメントであれば、現在の状態から1を減算します。そして、`set count`であれば、ペイロードとして渡された値を新しい状態として単に設定します。

そして、それらの値が返されます。そして、この返された値が次の状態になります。そして、いつものように、コンポーネントが再レンダリングされ、DUIも更新されます。

よし、それでこれが少しでも意味をなしたことを願っているが、もしそうでないなら、まあ、このセクションとコースの残りの部分を通してこれを練習する機会はたくさんあるだろう。

次に、このステップもリデューサーに組み込む。では、次のビデオでそれをすぐにやろう。

# 関連する状態の管理

では、今度はステップ状態も、構築してきたReducerに組み込みましょう。

通常、Reducersは、ここでやってきたように単一の値だけでなく、より複雑な状態を管理する必要がある場合に使用します。

つまり、通常、状態は単一の値ではなく、オブジェクトになります。では、これもコメントアウトして、`initial state`というオブジェクトを定義しましょう。

そして、これはまた、かなり一般的な名前です。そして、ここでは`count`をゼロと定義し、`step`を1と定義します。

以前と同じデフォルト値です。そして、ここには今度はゼロだけでなく、その初期状態を渡します。

そして、ここにある状態はもはや`count`とは呼ばれませんが、まあ、「Just state」と呼びましょう。

そして、その状態オブジェクトエラーをすぐに分割代入することもできますが、ここでやりましょう。

では、`state`から`count`と`step`を分割代入するとしましょう。そして、今度は再び、以前にあったこの関数がありますが、それはもはや存在しません。

よし。そして、今ここにあるものも、もはやあまり意味がありません。

では、とりあえずここにある`console.log`に戻りましょう。これももう必要ありません。

そして、すぐにリロードして、ここにあるこれらのボタンの1つをクリックしましょう。そうするとアクションがディスパッチされます。

そして、ここで最初の問題が発生します。何らかの理由で、実際にはそこから何かを返す必要があるかもしれませんが、もう一度試してみましょう。

ええ、それは実際には機能しません。では、そこから何かオブジェクトを返してみましょう。

これをやる必要さえありません。これは単にエラーを回避するためです。そして、ええ、今度は機能します。

ここでやりたかったことは、今度は状態とアクションを再び示すことです。それで、今`Reducer`で取得する状態は、`use Reducer`に渡したこの初期ステップと同じです。

そして、同じ形状のオブジェクトを返す必要があります。つまり、`count`と`step`も含まれています。そして、今度はここの`Reducer`関数をそれに合わせて調整しましょう。

実際、このようなReducer関数内で`switch`文を使用するのは非常に一般的です。

つまり、複数の`if`の代わりに、通常は`switch`を使用するだけです。そして、評価したい値は、もちろん`action type`です。

そして、考えられるすべてのケースに対して1つのケースを行うだけです。それで、アクションタイプが減った場合、何を返したいでしょうか？

さて、以前は単に状態に1を足して返していましたが、今ではもうできません。

さて、今度はこの形状のオブジェクトを返す必要があります。覚えていますか？では、どうすればよいでしょうか？

さて、まず、`state.count`に1を足して`count`を設定しましょう。そして、`step`も設定する必要があります。

しかし、明示的に書き出す代わりに、ここにある現在の状態オブジェクト全体を展開します。

そして、ここにあるこの部分で、基本的にそこの`count`プロパティを上書きします。そして、これはまさに、過去にオブジェクトを更新してきた方法と全く同じです。

つまり、基本的に、以前のオブジェクトのすべての情報と、上書きしたいものすべてを含む、全く新しいオブジェクトを作成します。

さて、ここにはこの警告が表示されます。なぜなら、デフォルトも必要だからです。そして、ここで、基本的に`switch`が認識しないアクションを取得した場合、単に「未知のアクション」という新しいエラーをスローします。

まあ、これはまだハッピーではないが、まあ、とりあえずここで続けよう。

それで、そこに何か問題があるとはあまり思いません。では、他のケースもやってみましょう。

インクリメントするには、マイナス1です。エラーはまだ消えません。

しかし、その理由は、ここで私たちはすでに返しているからです。そして、このコードは到達不能になります。

よし、そして最後に、この他の`set count`ケースがある。それで、ここで私は基本的にすでに持っていたコードを`switch`文に変換し、`count`と`step`の形状を持つオブジェクトも返すようにしている。

よし、状態を展開し、次に`count`は`action payload`と等しくなる。

よし、今のところ、これは以前と全く同じように機能する。ここでスイッチしたことを除けばだ。

それで、ここにプラスがあり、ここにマイナスがある。では、ええ、これで動く。そして、ここに入力すると、状態も更新される。

素晴らしい、そして今度は実際にここのステップについても同じことをやろう。それで、ここには今、`set step`というタイプの新しいアクションもディスパッチしたいよね？

そして、ペイロードは全く同じになる。だから、繰り返しになるが、数`E target value`に変換する。よし？`set step`だ。

そして、今度はここにそのための新しいケースを追加する必要がある。そして、この場合、状態全体を返し、今回は`step`を`action.payload`に設定したい。

それで、状態だ。リロードしよう。そして、ええ、美しい。

それは機能します。そして、今度はやらなければならない唯一のことは、実際にこのステップを考慮に入れて、ここで増減することです。

それで、それは十分に簡単です。つまり、それは単に`state step`です。そして、ここでもちろん同じことです。

そして、これは2になるはずです。そうです、8です。などです。

さて、よし、これを取り除きましょう。そして、まあ、この時点まで、おそらくこれらすべてがまだそれほど面白そうに思えないかもしれません。

つまり、これまでにやったことはすべて、`use data`フックでも簡単に達成できたでしょう？

しかし、今度は実際に何か違うことをしましょう。これらも取り除きましょう。

よし、言っていたように、何か違うことをしよう。それはこのリセット関数だ。

以前やったことは、単純に2つの状態セッターを取得し、両方を呼び出すことだった。しかし、今度は、それらすべてを同時に行う1つの大きな状態遷移を行うことができる。

そして、それは`useReducer`フックの大きな利点の1つになるだろう。

特に、これらの2つよりもさらに多くの状態がある場合はそうだ。では、実際に何を意味するのかお見せしよう。

ここでは、例えば`reset`というタイプの新しいアクションをディスパッチするだけでいい。

そして、`Reducer`にデータを渡す必要はない。したがって、ペイロードを指定する必要もない。

なぜなら、`Reducer`関数内でそれを実行できるからだ。では、そのケースもここに加えよう。

`reset`です。そして、ここで行うべきことは、`count`をゼロに、`step`を1にリセットする新しいオブジェクトを返すことだけです。

そして、ここでは基本的にこれらの2つの状態を同時に更新できます。そして、さらに良くすることもできます。

ここからこれを切り取ります。これは実際にはその外に存在できます。次に、ここで初期状態を返すだけです。

よし。そして、「リセット」をクリックすると、「バン」、ほら。

私たちの状態全体が、ちょうど初期状態にリセットされました。すべて一度に、ここにある1つのディスパッチだけでです。

つまり、ここにあるこれらのイベントハンドラーで行っていることは、実際にはディスパッチだけです。そして、これらすべてのディスパッチをここに移動できます。

JSXにです。それは実際には非常に意味がありますが、今はしません。

しかし、ええ、ここにあるこれらすべてのイベントハンドラーを置き換えることができます。なぜなら、それらすべて、それらがすべて行うことは、アクションをディスパッチすることだからです。ロジック自体です。

つまり、それらが何をするかの本当のロジックは、今、この1つの`Reducer`関数にすべて集中しています。そして、それが`Reducer`関数の美しさと大きな利点です。

つまり、基本的に、アプリケーションで発生する可能性のあるすべての状態更新が、この1つの中央の場所にあります。

そして、これにより、すべての異なるコンポーネントやすべての異なる関数に移動することなく、アプリケーション全体を非常に簡単に理解できます。

つまり、もちろん、ここでは1つのコンポーネントしかなく、すべてが本当にシンプルです。しかし、これがより大きなアプリケーションでどれほど素晴らしいものになるかを見始めることができるかもしれません。

そして、実際、このセクションの大きなアプリケーションを構築し始めると、それが実際に機能しているのを見ることになります。

しかし、これらの利点はすべて後でより明確になります。今のところ、これらの2つの講義では、`use Reducer`フックのメカニズムに焦点を当てたかっただけです。

つまり、それがどのように機能するか、特にここにあるこの`Reducer`関数がどのように機能するかです。

しかし、これが今、非常に混乱しているように見えることは知っています。なぜなら、このかなり複雑なフックについて初めて学んだときに、私がどのように感じたかを完全に覚えているからです。

しかし、今、私たちは高度なReactの部分にいるので、これらのより難しいことについて学ぶ時が来ました。

しかし、とにかく、次の講義では、ここにあるデータと状態の流れを本当に示すいくつかの素晴らしい図があります。

そして、それをお楽しみに。

# `useReducer`による状態管理

状態更新ロジックをすべて`reducer`関数という1つの中心的な場所に集中させるために、`useReducer`フックを使用する方法を学びました。

では、`reducer`の概念と、特定の状況で`reducer`がアプリケーションを大幅に改善できる理由と方法について、さらに深く掘り下げていきましょう。

ここまで、私たちは`useState`フックを使ってすべての状態を管理してきましたよね？

しかし、コンポーネントと状態の更新がより複雑になるにつれて、`useState`を使用してすべての状態を管理することは、状況によっては十分ではありません。

例えば、一部のコンポーネントは、多くの状態変数と、コンポーネント全体、あるいは複数のコンポーネントにまたがる複数のイベントハンドラーに分散された多くの状態更新を持っています。

そして、これはすぐに圧倒的で管理が難しくなる可能性があります。また、複数の状態更新が同時に、つまり同じイベントへの反応として起こる必要があることも非常に一般的です。

例えば、ゲームを開始したい場合、スコアをゼロに設定し、`is playing`ステータスを設定し、タイマーを開始する必要があるかもしれません。

そして最後に、ある状態を更新することが、1つ以上の他の状態に依存することが多く、これも多くの状態がある場合に困難になる可能性があります。

そして、これらのすべてのケースで、`useReducer`は本当に役立ちます。これらは`reducer`が解決しようとする問題であり、それではどのように解決するか見てみましょう。

まず第一に、`useReducer`は状態を設定および管理するための代替方法であり、複雑な状態や関連する状態に最適です。

さて、過去2回の講義で`useReducer`をすでに使用しましたが、その様子はこんな感じでした。`reducer`関数とその初期状態で`useReducer`を呼び出し、状態とディスパッチ関数を返します。

それで、最初から始めると、`useReducer`を使用するとき、通常は`useReducer`フックから返される状態オブジェクトに関連する状態を格納します。

さて、それはプリミティブ値であることもありますが、通常はオブジェクトを使用します。さて、すでに知っているように、`useReducer`が機能するためには`reducer`関数と呼ばれるものが必要です。

この関数は、状態の更新を担当するすべてのロジックを配置する場所であり、イベントハンドラーからこの1つの中央の場所にすべての状態更新ロジックを移動することで、状態ロジックをコンポーネント自体から完全に分離できます。

これにより、コンポーネントが非常にきれいになり、読みやすくなります。したがって、`useReducer`で状態を管理する場合、最終的に状態オブジェクトを更新するのはこの`reducer`関数になります。

ある意味では、`useState`の`setState`関数に似ていますが、スーパーパワーを備えています。さて、実際には、`reducer`は単に、現在の状態とアクションを入力として受け取り、それらの値に基づいて次の状態、つまり更新された状態を返す関数です。

さて、Reactでは状態は不変であることを覚えておいてください。これは、`reducer`が状態を変更することが許可されていないことを意味します。

そして実際、`reducer`では副作用は全く許されません。つまり、`reducer`は常に新しい状態を返す純粋な関数でなければなりません。

そして、繰り返しになりますが、現在の状態と受け取ったアクションに基づいています。そして、アクションについて言えば、アクションは単に、状態をどのように更新すべきかを記述するオブジェクトです。

通常、アクションタイプといわゆるペイロードが含まれます。これは基本的に入力データです。そして、このアクションタイプとペイロードに基づいて、`reducer`は次の状態をどのように正確に作成するかを決定します。

そして、パズルの最後のピースはこれです。実際に状態の更新をどのようにトリガーするのでしょうか？さて、それが`dispatch`関数の出番です。

`useReducer`は、状態の更新をトリガーするために使用できる関数である、いわゆる`dispatch`関数を返します。

つまり、`setState`を使用して状態を更新する代わりに、今度は`dispatch`関数を使用して、イベントハンドラーからアクションを送信します。

`dispatch`を`reducer`に呼び出しています。そして、すでに知っているように、`reducer`はこのアクションを使用して次の状態を計算します。

よし、これらが`useReducer`フックを効果的に使用するために組み合わせる必要があるすべての要素だ。アクションオブジェクト、ディスパッチ関数、リデューサー、そして状態オブジェクトだ。

しかし、今度はこれらの要素が実際にどのように組み合わさって状態を更新するのかを本当に見るために、図を見てみよう。

あるコンポーネントのイベントハンドラーにいて、今、ある状態を更新する必要があるとしよう。それで、どうする？まあ、その通りだ。

`useReducer`から取得した`dispatch`関数を呼び出して、アクションを`reducer`にディスパッチする。

そして、このアクションは、前に学んだように、`reducer`のための情報を含むオブジェクトだ。つまり、`reducer`が状態をどのように更新すべきかについての情報だ。

この場合、アクションタイプは`updateDay`で、ペイロードは23だ。これはおそらく、`reducer`が`day`状態を23に設定することを意味するだろう。

さて、オブジェクトはこの正確な形状、つまりタイプとペイロードを持つ必要はないが、ほとんどの開発者に採用されている標準だ。

さて、基本的に、`reducer`はこのアクションを現在の状態とともに受け取り、ブランドの新しい状態オブジェクトを返します。これは、`reducer`の文脈では通常、次の状態と呼ばれます。

そして、いつものように、状態を更新すると、コンポーネントインスタンスの再レンダリングがトリガーされます。さて、`reducer`関数がなぜ実際に`reducer`と呼ばれるのか疑問に思っているなら、答えは、配列の`reduce`メソッドと全く同じ考えに従っているからです。

`reduce`メソッドがすべての配列値を1つの値に蓄積するのと同様に、Reactの`reducer`はすべてのアクションを時間の経過とともに1つの状態に蓄積します。

さて、舞台裏では、`dispatch`関数は`reducer`にアクセスできます。なぜなら、それを`useReducer`フックに渡したからです、よね？

`dispatch`は、この全体を本当に調整しており、`reducer`に現在の状態へのアクセスも与えています。

そして、これをさらに良く理解するために、`useReducer`のメカニズムを、はるかに単純な`useState`メカニズムと比較してみましょう。

`useState`を使用すると、セッター関数が返されます。それを単に`setState`と呼びましょう。

そして、状態を更新したい場合、必要な新しい更新された状態値を渡すだけで、Reactは単に状態を更新します。

そして、それは再レンダリングをトリガーします。つまり、`useReducer`よりもはるかに単純で簡単ですが、`useReducer`は、この講義で前に見た問題を解決するので、多くの状況で素晴らしい選択肢です。

セットアップが少し複雑になりますが。しかし、`useReducer`の大きな利点と、それをいつ使用すべきかについては、このセクションの後半で詳しく説明します。

さて、ディスパッチアクションとリデューサーの記述というこの考え全体が、最初は非常に混乱を招くことを理解しています。

昔、私がどれほど混乱していたかを覚えているので、わかります。そして、私が最初にこれについて学んだときに、これらすべてを本当に明確にしてくれた本当に役立つアナロジーを今お見せしましょう。

何らかの理由で銀行口座から5,000ドルを引き出す必要があると想像してください。

さて、これは大きな金額なので、ATMから引き出すことはできません。したがって、物理的に銀行に行く必要があります。

さて、銀行に着いたら、どうやってその5,000ドルを手に入れるのでしょうか？銀行の金庫室にまっすぐ歩いて行き、現金をつかみ、家に帰りますか？

まあ、そうは思いませんよね？通常、そうではありません。実際にどのように機能するかというと、銀行に行き、そこで机に座ってあなたを助けてくれる準備ができている人を見つけることです。

さて、銀行に到着したとき、あなたはすでにいくらの現金を引き出したいのか、そしてどの口座番号からなのかを知っています。

そして、その人のところにまっすぐ歩いて行き、口座番号923577から5,000ドルを引き出したいと伝えます。例えばです。

次に何が起こるかというと、通常、その人はコンピュータに何かを入力し、口座に現金があるかどうかを確認し、もしあれば、銀行の金庫室に行き、お金を取り出し、最終的にあなたに渡します。

結局のところ、それはあなたのお金ですよね？しかし、この現実のバージョンと、あなたが自分で現金をつかんだ以前のバージョンの物語の大きな違いに注目してください。

この現実のバージョンでは、あなたは何をすべきか、そしてそれをどのようにすべきかをその人に伝え、彼はあなたの代わりにあなたのためにお金を手に入れました。

そして、あなたは直接お金を取らなかった。そして、それは大きな違いです。それで、これはもしかして聞き覚えがありますか？

さて、そう願っています。そして、今このアナロジーを`useReducer`に戻し、これらの各ピースが`useReducer`メカニズムで何を表しているかを特定しましょう。

そして、最も重要なもの、つまり状態から始めましょう。このアナロジーで状態は何だと思いますか？

さて、状態は銀行の金庫室によって表されます。なぜなら、ここに関連データ、つまりお金が保管され、更新されるからです。

つまり、更新する必要があるのは金庫室であり、それが私たちの状態です。素晴らしい。では、それをさておき、金庫室からお金がどのように取り出されるかを考えてみましょう。

状態が実際にどのように更新されるかについてです。では、最初から始めましょう。銀行に行く顧客は、このアナロジーで何を表していると思いますか？

まあ、銀行に行って現金を要求する顧客は、明らかにディスパッチャーです。なぜなら、状態の更新を要求しているのは彼らだからです、よね？

そして、彼らはその人に会い、5,000ドルを引き出すように要求することでそうしています。では、ここでのリデューサーとアクションは何でしょうか？

さて、リデューサーは銀行で働いている人になるでしょう。なぜなら、実際に更新を行うのはその人だからです。

この場合、金庫室に行ってあなたのお金を取りに行くのはその人です。しかし、その人はいくらのお金を取るべきか、そしてどの口座から取るべきかをどのようにして知るのでしょうか？

彼らはあなたがそう言ったから知っています。あなたのリクエストメッセージで正確にです。そして、そのリクエストメッセージは明らかにアクションです。

この例では、アクションは、アクションタイプが「引き出し」であり、ペイロードがあなたが行いたい引き出しに関するデータである、このようにモデル化できます。

要約すると、あなたは明確な行動を念頭に置いて銀行に行き、その行動をリデューサー、つまりそこで働いている人にディスパッチしました。彼はその行動を受け取り、指示に従ってあなたの口座から正しい金額のお金を取りました。

状態から。彼はあなたにお金を渡し、このサイクルを終えました。あなたは直接金庫室に行って自分のお金を取ったわけではありません。

代わりに、金庫室に対するさまざまなアクションを実行する方法をあなたよりもはるかによく知っている仲介者として、その人を持っていました。

つまり、彼は入金、出金、口座の開設と閉鎖、ローンの申請など、多くのことを知っています。

そして、彼は、あなたが詳細について心配する必要なく、これらすべてを行います。つまり、`reducer`関数と全く同じです。`reducer`関数も、すべての状態更新ロジックをあなたから切り離して抽象化します。

そうすることで、クリーンで理解しやすいコンポーネントを持つことができます。よし、これでディスパッチャー、リデューサー、アクション、そして状態の関係が明確になったことを願っています。

そして、このセクションを通して、これらすべてを練習する機会はたくさんあります。

# 「Reactクイズ」アプリ

`useReducer`フックがいかに非常に役立つかを、うまくいけばお見せできるような、全く新しいアプリの構築を始めましょう。

そして、これが私たちが構築するアプリケーションで、「The React Quiz」と呼ばれています。

名前が示すように、これは基本的にReactのいくつかの概念に関する非常に単純なクイズです。これは、実際に偽のAPIから質問をロードすることから始まります。

そして、この時点で、これらの50の質問を受け取ったので、ここでクイズを開始できます。

ここに4つの異なる選択肢がある質問があり、上には進捗状況とこれまでに獲得したポイント数が表示されます。

それで、もちろん、正しい選択肢をクリックできます。この場合、Reactは最も人気のあるフレームワークです。

そして、すぐに正しいとマークされます。また、ここにある「次へ」ボタンが表示されるので、次のボタンに移動できます。

そして、今度は間違った選択肢を選択しましょう。そして、もちろん、正しい選択肢はまだ正しいとマークされています。

さて、もちろん、クイズ全体をここで説明するつもりはありませんが、最終結果、つまりクイズの終わりに到達したときにどのようになるかを見ていきます。

さて、ここにもタイマーが実行されていることに注意してください。そして、このタイマーが終了すると、自動的に負けるか、あるいはクイズを自動的に終了し、その時点までに集めたポイントしか得られません。

画面に表示されるすべての状態、つまりどの質問が選択されたか、ポイント数、現在どの質問にいるか、タイマー、そして本当にすべての状態は、1つの大きなリデューサーによって管理されています。

なぜなら、このセクションはそれがすべてだからですよね？さて、この講義で私がやりたいことは、ここでアプリの骨格を設定することだけです。

つまり、基本的に、ヘッダーとアプリ自体の構造を設定するだけです。そして、次の講義では、質問のフェッチを開始し、アプリケーションの構築を開始できます。

それで、もうこのコンポーネントは必要ありません。代わりに、Appに戻りましょう。そして、ここで、その日付カウンターを削除します。

よし。それで、私たちの親divには`app`というクラス名が付き、その`app`の中にヘッダーがあります。

つまり、基本的に、ロゴとアプリケーションの名前を持つこの部分です。さて、前のセクションの1つで述べたように、今度はコンポーネントごとに1つのファイルを作成し始めます。

それで、このプロジェクト開発をもう少し現実的なものにします。さて、ヘッダーの場合、実際にはすでにそのコンポーネントを作成しています。

これは、ロジックも状態もなく、プロップさえも受け取らない非常に単純なコンポーネントです。つまり、画像、ロゴ、タイトルを持つ非常に単純なプレゼンテーションコンポーネントです。

そして、それをここに含めましょう。このようにです。そして、もちろん、そのコンポーネントをそのファイルからインポートする必要があります。

現在のフォルダにあり、次に`header`です。そして、ほら。ヘッダーがあり、その下に、基本的にメインコンテンツがあります。

そして、そのメインコンテンツのために、`main`コンポーネントを作成しましょう。そして、その前に、ここにマークアップを記述するだけです。

`main`というクラスの`main`要素が欲しいとしましょう。そして、例えば、進捗状況の段落が欲しいとしましょう。1または15としましょう。

これは単なるダミーコンテンツです。そして、質問自体です。

よし、ではこれだけあるとしよう。しかし、これをここにあるメインHTML要素の中ではなく、独自のコンポーネントの中に置きたい。

そうすれば、ここにあるアプリコンポーネントは、きれいで整頓されたままだ。つまり、異なるコンポーネントはあるが、クラス名を持つこれらの要素はない。

では、新しいコンポーネントで新しいファイルを作成しよう。これは`Main.js`と呼ぶことにしよう。なぜなら、`Main`がコンポーネントの名前になるからだ。

そして、今度は、コースの冒頭でお渡ししたスニペットを初めて使ってみよう。覚えているかな。

ユーザースニペットに行き、その中の一つであるはずだ。ええ、私が言っているスニペットは`RFC`スニペットだ。

もし何らかの理由で、コースの冒頭でこれを自分のスニペットに追加しなかったなら、ここからコードをコピーするか、コースの冒頭のセットアップ講義に戻ることができる。

なぜなら、繰り返しになるが、今度はそのスニペットを使い、非常に便利になるからだ。

`RFC`と書くだけで、Enterキーを押すと、ファイルの名前さえも付いた新しいコンポーネントが作成される。

つまり、ファイルから名前を取得し、それに応じてコンポーネントに名前を付けた。そして、今やるべきことは、ここにあるマークアップを変更することだけだ。

さて、ここでは`div`から`main`要素に変更したいのです。それで、この開始タグと終了タグを変更しなければならなかったのが、何度か非常に迷惑だったことを思い出してください。

そして、それを修正するために、ここにあるこの拡張機能をインストールしましょう。`Auto Rename Tag`です。

すでにインストールされていますが、どうぞインストールしてください。そして、この`div`を`Main`に変更すると、両方が同時に変更されるのがわかります。

それで、ここでは、`Main`のクラス名も同様です。そして、今度は、コンテンツとして何を配置するでしょうか？

まあ、基本的に、ここにあるこのコンテンツをこのコンポーネントに留めておきたいのです。つまり、それを`main`に渡すだけです。

そして、そのためには、`children`プロップを使用します。覚えていますか？

つまり、基本的にここでコンポーネントコンポジションを行っています。そして、この時点ですでにこれを何度も行っているので、これは何も新しいことではないはずです。

そして、これにより、やるべきことは、ここにあるこれを`Main`に置き換えることだけです。そして、繰り返しになりますが、それは`tier`を自動的に置き換えました。

そして、`Main`を`Main`からインポートするだけです。さて、ですね、今は同じように見えますが、今度はもう少し良い構造になりました。

そしてまた、コンポーネントツリーもこのように、はるかに完全だと思います。まあ、実際にはまだ更新されていません。

ええ、それで、ツリーでも、アプリ、ヘッダー、メイン部分があることがわかります。

# 偽APIからの質問の読み込み

よし、では偽のWebサーバーで偽のAPIをセットアップし、それを使って質問データをアプリケーションにロードしよう。

今回は、私が自分で質問を作成したので、実際のAPIは使用しない。そのため、それを持つAPIはない。

しかし、それでも、どこかから質問を読み込んでいるふりをしたい。そして、そのため、`json-server`というnpmパッケージを使って偽のAPIを作成できる。

では、ターミナルに行き、基本的にここに新しいタブを作成しよう。

これにより、アプリを実行しているこのプロセスは実行し続け、ここにもう1つのターミナルがある。

正しいフォルダにいることを確認して、`npm install json-server`を実行しよう。

その間に、このセクションの冒頭で含めるのを忘れたスターターデータからデータを取得する。

それで、ファイルをコピーしたとき、おそらくすでにこの`questions.json`を取得しているだろう。しかし、繰り返しになるが、私は実際、このファイルをここに以前に配置するのを忘れていた。

では、ここに来て、`react-quiz`にこのファイルを貼り付けよう。あるいは、実際には、ここに新しいフォルダを作成したい。

このフォルダを`data`と呼ぼう。そして、あなたのファイル構造でも同じことをしてほしい。

そこに新しいファイルを作成し、この`questions.json`ファイルをそこに移動しよう。

よし、そのファイルがそのフォルダにあることを確認してください。そして、その間に、このインストールが完了しました。

そして、次にそのパッケージを呼び出すことができるようにするために、つまり、`json-server`コマンドを実行するために、このファイルに新しいnpmスクリプトを追加する必要があります。

ここにあるこれらのnpmスクリプトは、基本的にここに記述するコマンドです。つまり、`npm start`と記述するたびに、ここにある`start`コマンドが存在するため、それができます。

よし、では今度は、`server`という名前で独自のnpmスクリプトを作成しよう。それで、ここで何が起こるべきかというと、`json-server`パッケージを呼び出して、実行し、特定のファイルを監視したいのです。

そして、そのファイルは私たちの質問データになります。そして、実際には、ここにあるそのデータを見てみましょう。基本的に、`questions`プロパティを持つ1つのオブジェクトにすぎません。

そして、そのプロパティには1つの配列があります。そして、その配列は私たちの通常のオブジェクトの配列です。

つまり、各オブジェクトには、質問、選択肢、正解、ポイント数があります。そして、これが私たちが今監視したいファイルです。つまり、基本的にAPIを作成するためのファイルです。

そして、フォルダである`data`と、`questions.json`を実行するだけです。そして最後に、ポートも指定する必要があります。8000としましょう。

そして、それは私たちを始めるのに十分なはずです。では、`npm run server`を実行しましょう。ここで、キーワードを実行する必要があります。

`npm server`ではなく、`npm run server`です。よし、これでAPIが実行されているはずですが、なぜかポートが3000であることがわかります。

それで、おそらくすべて小文字です。それを保存して、ここを閉じましょう。

それは以前、Reactアプリケーションと同じURL、つまり同じポートで実行されていました。

では、それをリロードして、これを再試行しましょう。そして、ええ、今度はポート8000で実行されています。

それで、このURLをコピーして、これを8000に変更するだけで、まあ、ここで私たちのデータが見えるはずですが、どうやら何か問題があったようです。

では、見てみましょう。まあ、今度はすでに使用中だと考えています。では、他のnpmプロセスもここで終了して、`npm start`をもう一度実行してみましょう。

そして、ここで、`npm run server`を実行しましょう。よし、まだうまくいかないので、他のポートに変更するだけでいい。

これはそれほど重要ではありません。だから、もしあなたの場合、このようにうまくいったなら、そのポートを使って、8000を9000に変更してください。

そして実際、ここにあるエンドポイントを作成しているので、`questions`も追加する必要があります。

そして、ここにあるこのフィールドの名前が`questions`だからです。だから、もしこれがこのようであったなら、ここにもこれら2つを追加する必要があります。

それで、これはもう機能せず、私たちのデータはこのURLにあることになります。しかし、もちろん、それはあまり意味がありませんので、これを使うだけにしましょう。

そして、これが実際には単なる配列であることに気づくでしょう。それで、ここにある配列はまさにこれです。

では、このURLを取得し、アプリケーションにフェッチしましょう。マウント時にそのデータをロードしたいので、`useEffect`フックを使いましょう。

そして、言ったように、マウント時に実行するだけです。それで、今度はこのURLからフェッチしたいと思います。そして、ここでは非同期関数をわざわざ使う必要はありません。

しかし、このプロミスを処理するために`then`メソッドを使うだけにしましょう。これは私たちにレスポンスを与え、それをJSONに変換する必要があります。

そして、それは順番に別のプロミスを返します。それで、別の`then`ハンドラーをそこにチェーンします。そして、これは私たちのデータを与えるはずです。

今のところ、それをコンソールにログするだけです。そして、ここで起こりうるエラーを、コンソールにもログすることでキャッチしましょう。

ここに`console.error`を使いましょう。このようにです。

よし、では実際に試してみよう。リロードして、コンソールに来て、美しい。

ここにデータがある。しかし、もちろん、いずれは、そのデータをここにあるUIに表示する必要がある。

そして、そのためには状態が必要になる。そして、想像できるように、今度は`useReducer`フックを使ってその状態を作成する。

では、そうしよう。それで、ここに状態オブジェクトとディスパッチ関数を取得する。覚えているかな？

そして、`useReducer`フックを使用します。これは、インポートにここにある編集でした。そして、ここに初期状態を渡します。まだありませんが、1秒でこのオブジェクトを作成します。

そして、実際、まず、まだないリデューサー関数を渡します。そして、両方をコンポーネントの外で、ここに作成しましょう。

`initialState`と`reducer`です。これは、覚えているように、現在の状態とディスパッチされたアクションを受け取ります。

よし。そして、今度は`initialState`を作成することから始めましょう。そして、ここに`questions`を作成しましょう。デフォルトでは空の配列になります。

さて、これ以外に、ユーザーに質問がフェッチされていることを伝えるための読み込み状態も欲しいのです。

しかし、今回は別の方法で行います。つまり、いつものように`isLoading`状態を作成するのではなく、代わりに今回は`status`状態を作成します。

そして、この`status`は、基本的に、時間の経過とともに変化するアプリケーションの現在の状態の文字列になります。

つまり、最初は、アプリケーションは読み込み状態になります。しかし、時間の経過とともに、異なる状態になることができます。

では、すぐにここにそれらを書きましょう。読み込み状態、エラー状態、データが到着してクイズを開始する準備ができた状態、クイズが実際に実行されているアクティブ状態、そしてクイズが終了した終了状態です。

よし。そして、これはアプリケーションが存在しうるこれらのすべての異なる状態を処理するための、少し良い方法です。

つまり、`isLoading`、`isError`、`isReady`、`isActive`状態を持つ代わりに、単にステータスを持つだけです。

そして、ステータスの中で、現在何が起こっているのかをアプリケーションに伝えます。これは`useReducer`とは関係ありません。

これは単なる別のテクニックです。しかし、とにかく、`reducer`関数に来て、以前にも見た`switch`文を設定しましょう。

これは基本的にあなたが常に従うレシピで、常に同じです。少なくとも、ここのセットアップはそうです。

`action.type`を切り替えます。基本的に、異なるタイプをテストしたいのです。そして、`dataReceived`のケースから始めましょう。

これが、実際にデータがあるときにここでディスパッチするアクションになります。ここです。

そして、実際、まずそれをやりましょう。それで、今度はデータをコンソールにログするのではなく、リデューサーにアクションをディスパッチします。

そして、私たちの通常のイベントをここで作成しましょう。それは単にタイプを持つだけです。そして、それは`dataReceived`タイプです。

これは、イベントとして考えることもできます。つまり、基本的に、ここでこの`dataReceived`イベントを作成しているようなものです。

私たちの`reducer`は、それに応答します。それが最初の部分です。そして、ここにはペイロードも必要です。なぜなら、実際にいくつかのデータを送信したいからです。

つまり、`reducer`にいくつかの情報を送信して、`reducer`がこれを使用して次の状態を計算できるようにします。そして、ここにあるそのペイロードは、受信したばかりのデータになります。

よし。そして、ここにあるように、新しい状態オブジェクトを返す必要があることを思い出してください。そして、現在の状態をすべて取得します。

そして、質問を、受信したばかりのデータに設定します。それが`action.payload`です。しかし、本当に素晴らしいのは、質問に関連するこの他の状態を設定できることです。

そして、それが状態です。状態と質問は、多くの場合、同時に変更されます。そして、それがリデューサーを持つことが非常に役立つ理由です。

なぜなら、今、ここで、同じ場所で、ステータスを`ready`に設定することもできるからです。

つまり、基本的に、これら2つの状態変数、つまりこれら2つの状態を、この1つのディスパッチですべて一度に更新しました。

つまり、ここにあるこの1つのイベントをディスパッチするだけで、質問とステータスの両方を更新しました。そして、この単純なイベントをディスパッチするだけで、新しい状態に移行したと言えます。

よし、しかし、とにかく、デフォルトのケースも作成しよう。そして、ここで、以前と同様に、「アクションが不明です」という新しいエラーをスローするだけだ。

これを保存して、何が起こるか見てみよう。もうコンソールにログは記録されないが、状態にはすでに質問があるはずだ。

UIではまだ見ることができないが、開発者ツールを見てみよう。そして、ええ。

状態に質問があり、ステータスも正常に準備完了に変更されました。つまり、この新しい状態に移行し、状態オブジェクトのこれらの2つのプロパティを変更しました。

これは素晴らしい機能です。そして、今度はエラーの場合に備えて別のイベントを作成しましょう。

例えば、`case dataFailed`、それは可能な名前の1つに過ぎません。そして、ここで、状態全体を返し、次に単にステータスを`error`に設定しましょう。

そして、後でもちろん、UIのJSXでさまざまなものを表示するためにステータスを使用します。

エラーがある場合は、そのエラーを表示します。そして、準備ができている場合、つまりステータスが準備できている場合は、質問を表示します。

よし、しかし、今度は実際にそのアクションをディスパッチしよう。つまり、今回のタイプは`dataFailed`だ。

そして、ここで、実際にはペイロードを渡さないでおこう。なぜなら、受け取るエラーには実際には興味がないからだ。

だから、私たちがやることは、状態に「今、状態はエラーです」と伝えることだけだ。それで、これをテストするために、データをロードするプロセスをここで終了しよう。

`Control+C`でこれを終了できる。そして、もちろん、そのエンドポイントからデータをフェッチすることはできなくなる。

そして、すでにいくつかのエラーが表示されるのがわかる。そして、ここでも、ステータスがエラーに設定されているのがわかる。

それも機能しているが、もちろん、APIを機能させて戻ろう。

そして、今またロードすると、状態に質問が表示されます。さて、よし、多くのことをやったので、やったことすべてを簡単にまとめておきましょう。

`json-server`パッケージをインストールして、基本的にこの偽のAPIを作成しました。そして、`server`という名前でnpmスクリプトを作成し、そのパッケージを実行し、実際にはこの質問データを配列として持つファイルを監視しました。

この配列はここにあるものです。そして、アプリケーションの内部では、初期レンダリングでそのデータをフェッチするために`useEffect`フックを使用しました。

次に、そのデータを状態に格納するために、今回は`useReducer`フックを使用しました。そして、初期状態オブジェクトと`reducer`を作成しました。

それはかなり標準的なものです。質問配列があり、状態があります。そしてもちろん、アプリケーションを構築し続けるにつれて、ここに多くの情報を追加します。

つまり、3つか4つの状態を追加しますが、今のところはこれら2つだけを扱っています。そして、データが正常にフェッチされるとすぐに、ここでアクションをディスパッチします。

ここで`reducer`に、アクションのタイプは`dataReceived`であると伝えます。そして、ペイロードとして、実際のデータを渡します。

そして、`reducer`はそのアクションを受け取り、ここにあるこのケースでそれを処理します。ここで、ペイロード、つまりAPIから受け取ったデータを質問配列に割り当てます。

しかし、それだけではありません。なぜなら、同時に、ステータスをここに`ready`に設定できたからです。すべて一度にです。

つまり、1つのディスパッチ関数の呼び出しだけでです。さて、この`reducer`の利点は、もちろん、まだ明確ではありません。

なぜなら、まだこれらの2つのケースとこれらの2つの状態変数しかないからです。しかし、このセクションを進むにつれて、それは本当に明確になり、本当に役立つでしょう。

# ローディング、エラー、および準備完了ステータスの処理

`reducer`を設置したので、アプリケーションが持つことができるさまざまな状態を処理する時が来ました。

最初はアプリケーションが読み込み中であることを思い出してください。それが始まりです。そして、データを受信すると、状態は`ready`に変わります。

そして、そのとき質問のリストを表示したいと思います。そして、エラーが発生する可能性もあります。その場合、状態は`error`に変わります。

そして、今度はこれら3つの状況に対して異なるUIを表示したいと思います。そして、これらの状況はすべて、ここにあるこのメインコンポーネント内に常に表示されます。

では、これを削除しましょう。そして、今度は条件付きでいくつかのコンポーネントを表示します。そして、ここでは`state.status`を使用できますが、それは少し手間がかかりすぎるかもしれません。

そこで、実際には状態オブジェクトを分割代入しましょう。さて、それを外で、つまりここにこれらの変数を作成することもできますが、その場で実行できます。

つまり、基本的にここにネストされた分割代入を使用することで、質問と状態の状態をここに分割代入できます。

そして、今、状態はもちろんもはや存在しませんが、ステータスがあります。そして、今、例えば、それが読み込み中であるかどうかを確認できます。

そして、もしそうなら、何かを表示したいのです。さて、この場合、ローダーを表示したいのですが、実際にはすでにここにあるそのローダーコンポーネントがあります。

繰り返しになりますが、これは非常に単純なプレゼンテーションコンポーネントです。そのため、ゼロから作成する必要はありませんでした。

プロップさえも受け取らず、エラーコンポーネントについても同じです。

全く同じで、プロップも受け取らず、状態もありません。それで、数分節約できると思い、ビデオで手動で書くことはしませんでした。

それで、このステータスが読み込み中であれば、その読み込みコンポーネントを表示したいのです。そして、それをここに含めましょう。

しかし、この行を複製するだけで、実際にはローダーと呼ばれています。そして、エラーもすでにインポートしましょう。

それで、ここにはローダーがあります。そして、ここで何か問題があります。状態が定義されていません。

しかし、私がどこにも状態を使っていない間に、リロードしてみましょう。そうすれば直ります。

よし。そして、今度は同じことをもう一度やればいい。なぜなら、これらの異なる状態は、もちろん、互いに排他的だからだ。

だから、三項演算子やネストされた三項演算子さえ必要ない。これかこれか、あるいは次のもののいずれか1つだけが真になる。

そして、ええ。だから、ここにエンドを使うだけでいい。よし。しかし、ここではエラーを使い、すでに試してみよう。

では、ローディングが少し見えるかどうか見てみましょう。見えましたが、非常に短かったです。

それで、確認のために、ここでいくつかのスロットリングをしましょう。アプリを少し遅くします。これは少し小さくできます。

ええ、どうぞ。それで、質問をロードしています。そして、今度はネットワーク接続がないふりをして、オフラインにしましょう。

そして、リロードすると、もちろん、ページ全体が機能しません。したがって、その実験は実際には機能しません。

しかし、ここでエラー状態を確認するために、偽のAPIをもう一度終了しましょう。そうすれば、いくつかのエラーが表示されるはずです。

そして、確かに。今、このディスパッチはここにあるこのイベントタイプで呼び出され、それが私たちのステータスをエラーに設定しました。

したがって、このエラーコンポーネントが表示されました。しかし、もちろん、サーバーを再起動しましょう。そうすれば、機能するはずです。

はい。よし。

そして、処理できる別の状態またはステータスがあります。それは`ready`ステータスです。

`ready`とは、基本的に質問が到着し、クイズを開始する準備ができたことを意味します。`status ready`です。

そして、ここにはさらに別のコンポーネントを作成したいと思います。では、それを確認してみましょう。

ここ、デモで、これが最初に表示したいものです。アプリが準備できたときです。

アプリをロードすると、読み込みインジケーターがちらっと表示され、ここがスタート画面になります。

このテキスト、質問の数、そして開始ボタンだけです。非常に単純なものです。

では、そのコンポーネントの新しいファイルをここに作成し、`startscreen.js`と名付けます。

そして、繰り返しになりますが、`rfc`というスニペットを使用します。ちなみに、これはReact Functional Componentの略です。

そして、ここにいくつかのテキストを配置しましょう。「Reactクイズへようこそ」から始めましょう。

そして、質問の数を含む段落です。今のところ、Xを配置するだけです。

「Reactの習熟度をテストしてください。」そして、この`div`は実際には`start`というクラス名を持つべきです。

そして、ここには代わりに`h3`を使いましょう。そして、最後に、「始めましょう」というボタンも追加します。

よし。それで、今のところはそれで十分だ。そして、それをここに含めよう。

それで、「スタート」、そしてここでVSコード。今回は実際にインポートを提案してくれる。

ファイルへのパス名でわかる。そして、それをクリックすると、インポートがうまく自動的に追加される。

これを保存すると、ほら。それで、基本的に私たちはその準備状態に反応、あるいは対応した。

さて、ここで欲しいのは、実際には質問の数です。そして、それは基本的にここにある質問配列の長さです。

質問配列の長さは15なので、そこに15が表示されることを望みます。では、その値をどこで計算するのでしょうか？

それを状態オブジェクトにも配置する必要があるでしょうか？まあ、それは実際には必要ありません。

いつもやっているように、ここにあるアプリコンポーネントで派生状態を計算するだけで済みます。なぜなら、実際にはこれは単なる派生状態だからです。

質問配列自体から単純に計算できるからです。では、`num questions`は単に`questions`としましょう。

ここは`questions`であるべきです。見てみましょう。ええ、どこでも`questions`です。

だから、単に`questions.length`です。そして、もちろん、配列全体をこのコンポーネントに渡すこともできますが、その必要は全くありません。

そして、他の場所でもこれが必要になります。それで、質問の数を質問の数と等しく渡しましょう。

そうすれば、ここでそれを受け取ることができます。分割代入して、次に`num questions`です。

そして、最後に、ここにも`num questions`です。そして、美しい。

今、私たちは15の質問を持っています。このようにです。そして、今必要なのは、ここにもういくつかのクラス名を追加することだけです。

`btn`と`btn ui`クラスです。そして、いつものように、CSSファイルでそれらを確認してください。

よし。そして、これで、この講義の目標を実際に達成しました。

そして、次の講義では、ここをクリックして最終的にゲームまたはクイズを開始します。

# 新しいクイズの開始

新しいクイズを開始する機能の実装の時間です。

では、どうすればよいでしょうか？さて、ここにあるこのボタンをクリックした後、基本的に最初の質問をここにあるこのメインコンポーネント内に表示したいのです。

つまり、基本的に、ここです。そして、このステータス状態を使用して、メインに何がレンダリングされるかを基本的に決定してきたので、今やるべきことは、そのステータスを何か別のものに変更し、その質問をここに表示することだけです。

そして、実際にはこれから始めましょう。では、もしステータスがアクティブであれば、ここで質問コンポーネントを表示したいとしましょう。

そのコンポーネントはまだ存在しないので、作成しましょう。

ここにさらに別のコンポーネントファイルがあります。それを閉じて、もう一度素敵なスニペットを使います。

では、とりあえず`question`と書きます。そして、もちろん、それをインポートする必要があります。

よし。まあ、それは正しくないが、ええ、今度は正しい。それで、ここで、`active`はアプリケーションが持つことができる5つの異なるステータスの1つであることを思い出してください。

そして、今、私たちはここでそのアクティブ状態を基本的に処理しています。ここです。

繰り返しになりますが、私たちはこの異なる状態を使用して、アプリのこのメイン部分に何が表示されるかを決定しています。

そして、それは流れのようなものです。まず、状態は読み込み中であり、次にこれを表示します。

エラーがあった場合、これを表示します。しかし、すべてがうまくいった場合、次のものを表示します。

そして、ユーザーがここで収集すると、次のものを表示します。つまり、ここにあるこの質問です。

しかし、問題は、このステータスをどのようにしてアクティブに設定するか、言い換えれば、どのようにしてゲームを開始するかです。

まあ、やるべきことは、基本的に、ステータスを`start`または`active`に設定する、`reducer`に新しいアクションタイプを作成することだけです。

では、ここで新しいアクションタイプを作成し、これを`start`と呼びましょう。そして、ここで、いつものように新しい状態を返したいと思います。

それは現在の状態と、アクティブに設定されたアンチステータスで構成されます。よし、そして今、非常に簡単です。

作成したばかりの`start`のアクションタイプを持つこのボタンからアクションをディスパッチするだけです。

では、それをここでやりましょう。ここで、そのアクションをディスパッチしたいと思います。そのためには、そのディスパッチ関数にアクセスする必要があります。

つまり、それはプロップでなければなりません。それで、それをスタート画面にここに渡しましょう。

それで、ご覧の通り、私たちは基本的に`dispatch`関数を渡しています。以前はイベントハンドラー関数や`set state`関数などを渡していたようにです。

もし`new state`をまだ使っていたら、おそらくここに新しいイベントハンドラーを作成し、そのイベントハンドラーをスタート画面に渡していたでしょう。

しかし、今は`reducer`ですべての状態遷移を処理しているので、もう何もする必要はありません。

そして、私たちがやるべきことは、ディスパッチャー関数を渡すことだけです。なぜなら、そうすれば非常に簡単だからです。

ここでやるべきことは、ディスパッチしたいアクションでそのディスパッチ関数を呼び出すことだけです。

つまり、ここで新しい関数を作成し、次にディスパッチし、イベントのタイプは、先ほど定義したように`start`になります。

そして、これで十分なはずです。だから、もしここをクリックすると、質問コンポーネントがこれの代わりに表示されるはずです。

では、見てみましょう。そして、バン、そこにあります。それで、このコンポーネントを終えました。そして、実際、この講義でやりたかったことを終えました。

それで、質問の実際の表示は次の講義に任せます。

# 質問の表示

では、現在の質問をレンダリングして表示しましょう。

もちろん、ここですべての質問を同時にレンダリングするのではなく、1つずつレンダリングします。そのため、現在の質問がどれであるかを知る方法が必要です。

つまり、基本的に、どの質問が現在の質問であるかを追跡する方法が必要です。そこで、ここにある初期状態に新しい状態を追加しましょう。

そして、これを`current`または`current index`と呼ぶこともできますが、単に`index`と呼びたいと思います。

そして、このインデックスはゼロから始まります。なぜなら、このインデックスを使用して、質問配列から特定の質問オブジェクトを取り出すからです。

そして、この質問配列の最初の要素は要素番号ゼロなので、インデックスはゼロから始まります。

そして、将来のある時点で、次の質問を表示したい場合、このインデックスを変更することでそれができるとすでに想像できます。

つまり、将来、そのインデックスをゼロから1に変更すると、次の質問が表示されるはずです。

つまり、画面を再レンダリングするはずです。したがって、これは状態変数である必要があります。

それが、ここにあるこの変数がなぜ必要なのか、あるいはこの状態の一部が必要なのか、そしてそもそもなぜ状態である必要があるのかの理由です。

そして、繰り返しになりますが、それは更新されたときに画面を再レンダリングする必要があるからです。しかし、とにかく、今度はこのインデックスを使用して、適切な質問オブジェクトを質問コンポーネントに渡しましょう。

このコンポーネントはもちろん、その現在の質問へのアクセスを必要とします。そして、先ほど言ったように、それは`questions`、つまり現在の位置にある配列になります。これはインデックスです。

さて、ここには、状態からこのインデックスをまだ分割代入していないため、利用できません。しかし、ほら。

そして、今度はこのコンポーネントに行きましょう。そして、以前と同じように、コマンドキーを押してここをクリックすると、別のファイルにあっても、自動的にそのコンポーネントに移動します。

では、ここでその質問を受け取り、それを表示しましょう。さて、まず、このオブジェクトの形状を見てみましょう。

そうすれば、実際に何を扱っているのかわかります。では、コンソールに来て、ここをクリックすると、それが表示されるはずです。

質問の中に`question`プロパティがあります。それを最初に使いたいのです。

ここに`question.question`があります。そして、これで、すでにUIに質問が表示されます。

素晴らしい。しかし、選択肢の配列もあります。そして、次に、この配列をループし、もちろん、これらの選択肢も表示したいのです。

さて、今回はリスト要素ではなく、実際のボタンを持つことになるので、順序付きリストは使用しません。

では、これを`options`と呼びましょう。そして、ここで`question.options`をループし、次に`.map`です。

それで、それぞれが質問、あるいは実際には選択肢です。そして、それぞれに対して、`btn`と`btn-option`というクラス名のボタンを返したいのです。

よし、そしてテキストについては、これは単なる単純な文字列なので、オプション自体であるべきだ。

各選択肢は単なる文字列なので、それはすでに見栄えが良い。何があるか見てみよう。

ああ、もちろん、キープロパティが欠けているので、最適化のためには非常に重要です。この時点で既に学んだようにです。

オプションは一意なので、それぞれをキーとして使用できます。リロードすると、開始します。

そして、選択肢とともに最初の質問があります。いいですね。

さて、実際には、このコンポーネントを2つに分割したいと思います。

このようにリストがあるときはいつでも、個人的にはこのような状況ではより小さなコンポーネントを持つことを好みます。

このコンポーネントのコンテンツの論理的な分離に基づいて、今このコンポーネントを分割できると思います。

では、この部分全体を取得し、新しいコンポーネントを作成しましょう。そして、実際には非常に多くのコンポーネントを作成しているので、`components`という新しいフォルダを作成するのが一般的です。

そして、ここでコンポーネントを作成します。では、ここでこれを行い、後ですべての他のコンポーネントもそこに置きます。

`options.js`です。そして、もう一度私たちのスニペットを使います。そして、ここにすでにJSXがあるので、そこに配置しましょう。

さて、ここには質問が欠けていますが、まず、他のすべてのコンポーネントをこのコンポーネントフォルダに入れましょう。

アプリ、日付カウンター、エラー、ヘッダー、ローダー、メインの質問、そしてスタート画面です。

ここにはすでに多くのコンポーネントがあるので、それらをすべてそこに入れましょう。さて、もちろん、`index.js`ファイルに問題が発生します。

なぜなら、まだアプリコンポーネントを以前と同じフォルダから読み込もうとしているからです。しかし、今はコンポーネントフォルダにあるので、ここで変更する必要があります。

しかし、他のすべては依然として機能します。なぜなら、それらを`app.js`内でここでインポートしているからです。そして、ここで同じフォルダからそれらをインポートしているからです。

それが、これが依然として機能する理由です。さて、しかし、とにかく、ここにある私たちのオプションに戻り、ここで質問を受け取る必要があります。

そして、もちろん、それをそこに渡す必要があります。それをここにインポートする必要さえあります。

オプション、次に現在の質問、そしてそれをここにインポートするだけです。

`options`をインポートします。そして、繰り返しになりますが、それは同じフォルダです。そして、テストしてみましょう。

そして、どうぞ。さて、もちろん、ここをクリックしても何も起こりません。そして、それが次のビデオで扱うことです。

# 新しい回答の処理

この講義では、新しい回答を処理するためのロジックを実装します。

それが何を意味するのかを理解するために、デモプロジェクトをここで確認し、新しい回答をしてみましょう。

新しい回答とは、基本的にここにあるこれらのオプションの1つをクリックすることです。では、やってみましょう。

すると、基本的に3つのことが起こったことがわかります。まず、正解と不正解の答えが表示されます。次に、獲得したポイントが更新されます。

そして3番目に、ここに「次へ」ボタンが表示されました。つまり、これらのオプションのいずれかをクリックすると、画面を再レンダリングする必要があるということです。

そして、繰り返しになりますが、それは新しい状態が必要であることを意味します。そして、その状態は、基本的に、どのオプションが選択されたかを保存する必要があります。

言い換えれば、どの答えだったのか？答え0、1、2、3です。基本的に、答えは、ここにあるこのオプションのインデックス番号になります。

では、`App.js`に戻り、初期状態にさらに別の状態を追加しましょう。

これを`answer`と呼び、最初は`null`になります。つまり、最初は答えがありません。

それは理にかなっていますよね？そして次に、その答えを更新するために、`reducer`にアクションを作成しましょう。

では、これをやった後でやりましょう。順番に並べましょう。

そして、これを`newAnswer`と呼びましょう。そうすれば、後で`newAnswer`タイプの新しいアクションをディスパッチしますよね？

そして、ここでは、いつものように状態を返し、次に答えが`action.payload`に設定されます。

非常に簡単なことです。そして、今度は質問に行く必要があります。あるいは、実際にはこれらのオプションにです。なぜなら、ここでボタンのクリックが発生するからです。

ここで、新しいアクションをディスパッチする必要があります。つまり、`dispatch`関数を質問、あるいは実際には`question`に渡す必要があります。

`dispatch`と`dispatch`関数です。そして、与えた答えも必要になります。

この答えは、与えられた答えが正しいかどうかを表示できるようにするために必要です。つまり、これらの色の1つで基本的にそれをペイントすることです。

そして、繰り返しになりますが、最初にこれをここで分割代入する必要があります。答えが必要です。ええ、ディスパッチと答えです。

では、それらをここで掴み、ここに渡しましょう。プロップドリリングが少しありますが、この1つのレベルだけなら大したことではありません。

では、これらの2つのプロップをここで受け入れて、ほら。今、ここからアクションをディスパッチできます。`onClick`です。

`dispatch`とこのイベントのタイプ、まあ、見てみましょう、覚えていません。`newAnswer`です。

そして、ペイロードは与えられた答えになるはずです。`newAnswer`とペイロードは再び答えです。

さて、答えは何でしょうか？以前、答えは基本的にオプションのインデックスであると言ったことを思い出してください。

この場合、それはインデックス番号1になりますよね？そして、その理由は、正解の選択肢もこれらの同じインデックスを使用してマークされているからです。

それをお見せしたいだけです。なぜなら、Reactの部分を学ぶだけでなく、基本的にアプリケーションを構築する方法を学ぶことも重要だと思うからです。

さて、ここで私たちのアプリは別の状態、つまり終了状態に変わりましたが、気にしないでください。

私が言っていたことは、ここにある正解の選択肢もこれらのインデックスを使用しているということです。そして、ここで正解の選択肢はReactです。

それで、それはインデックス番号1です。よし、ではここで現在のインデックスを取得するにはどうすればよいでしょうか？

まあ、`map`メソッドの2番目の引数としてここでそれを取得するだけです。つまり、このメソッドの仕組みは、最初のオプションが配列の現在の要素であり、2番目の引数が現在のインデックスであるということです。

そして、これだけで十分です。では、チェックしてみましょう。今回はここをクリックしてみましょう。

そして、状態に来ると、2の答えがあるはずです。そして、まあ、それはどこにあるのでしょうか？実際にはどこにもありません。

では、リロードしましょう。アプリケーションをリロードするのを忘れていたのかもしれません。もう一度試してみましょう。

ああ、そしてどうやらそうだったようだ。今、答えがここにある。いいですね。

そして、次に、この答えに基づいていくつかの書式設定を行いましょう。そして、それがこれらのオプションがその`answer`プロップを受け取った理由です。

ここにあるその状態です。では、何をするかというと、基本的にここのクラス名を条件付きで変更します。

では、テンプレートリテラルを作成し、まず選択されたオプションのクラスを作成しましょう。

ここで、現在のインデックスが答えと等しい場合、`answer`クラスを追加し、そうでなければ何も追加しないとしましょう。

条件付きCSSを行うときは、常に三項演算子を使用する必要があります。そうすれば、反対の場合、つまりこの3番目の分岐では、文字列を全く返しません。

そして、選択した質問が実際にこの特別なクラスを取得し、少し右に移動したことがわかります。そして、これを選択した場合、まあ、それがその特別なクラスを取得します。

それが最初の事です。そして、今度は、オプションが正解のオプションであるかどうかに応じて、正しい色でそれらをペイントする必要があります。

では、もう一度JavaScriptモードに入りましょう。そして、現在のインデックスが`question.correctOption`と等しい場合、クラス`correct`を、そうでなければ`wrong`を記述しましょう。

そして、美しい。それは機能しています。そして、繰り返しになりますが、ここで状態を確認しましょう。何が起こっているのかわかります。

繰り返しになりますが、これらのオプション、あるいはこれらの質問には、実際にはここにプロパティとして正解の選択肢があります。

ここでは1番です。そして、この場合、このオプションの1番はインデックスと等しいので、正解とマークされ、他のすべては不正解とマークされました。

よし？そして、今度は実際に何度もクリックできるが、もちろん、それは許されるべきではない。

つまり、ユーザーがこれらのオプションの1つをクリックすると、ロックされ、それ以上クリックできなくなります。

では、`disabled`プロップでボタンを無効にしましょう。そして、ここで、基本的に、答えがあったかどうかを知りたいのです。

つまり、どの答えでもあれば、このボタンが無効になります。答えの初期状態が`null`であることを知っているので、答えが`null`でないかどうかを確認するだけで済みます。

`null`でない場合は、答えがあったことを意味し、ボタンを無効にしたいのです。では、もう一度試してみましょう。

ああ、しかし、ここで何か問題があります。すべてのオプションがすでにペイントされています。それは非常に悪いです。なぜなら、ここで正解の選択肢を明かしてしまうからです。

そして、実際、ここでやったことはあまり正しくありませんでした。つまり、それぞれを正解と不正解に分類することです。

私たちは、答えが全くなかった場合にのみ、そうしたいのです。つまり、この状況で、基本的にです。私たちはそれを再利用する必要があります。

では、それを特別な変数に入れましょう。`hasAnswered`としましょう。よし、そして、それをここで使いましょう。

そして、ここもです。さて、ここは少し分かりにくくなるので、三項演算子を使い、次に別の三項演算子を使います。

つまり、ネストされた三項演算子があり、かなり醜いです。なので、実際にはこれを外で行うこともできますが、このままにしておきましょう。

この背後にあるロジックを理解していれば、読みやすいです。そして、今度は正しいようです。

では、ここで何かを選択しましょう。そして、ええ、今度は他のものをクリックできません。そして、すべてが正しい方法でマークされています。

それで、これを選択しました。それで、これは特別なクラスを取得しましたが、それは間違っています。それで、それは黄色で、これだけが青です。なぜなら、それは正しいからです。

正しいクラスを持っています。素晴らしい、それで3つのうちの1つを処理しました。

繰り返しますが、正解が表示されるはずです。次に、ユーザーのスコアが更新され、ここにある「次へ」ボタンが表示されるはずです。

では、今度は2番目の部分、つまりユーザーのスコアが更新されるべきであることに素早く対処しましょう。

つまり、それは再び画面上で更新される必要があるものであり、それは別の状態変数が必要であることを意味します。

では、これを`points`と呼び、ユーザーは0から始めます。よし、そして今、これらのポイントはどこで更新するのでしょうか？

さて、新しい答えを受け取ったのと全く同じ場所であるのが理にかなっています。ここで、ポイントも更新します。

では、どうすればよいでしょうか？もちろん、答えが正しかった場合にのみポイントが与えられるべきだからです。

だから、まずどの質問が現在の質問であるかを把握する必要があります。次に、答えが正しい場合、そしてその場合にのみ、現在のポイントにポイントを追加したいのです。

それは混乱するように聞こえます。では、やってみましょう。まず、現在の質問がどれであるかを把握しましょう。

実際には状態に保存されていないからです。インデックスしか知りませんが、質問自体は知りません。

では、`state.questions`を`state.index`で取得しましょう。そして、ここで本当に、`reducer`に入る現在の状態を利用して、次の状態を計算しています。

本当に現在の状態に依存しています。これで、どの質問が現在の質問であるかがわかります。そして、今、現在の質問が受け取った答えと等しいかどうかを確認できます。

その受け取った答えは、再び`action.payload`です。そして、今、それが再び`question.correctOption`と等しいかどうかを確認しましょう。

以前にもここで使用したこのプロパティです、ですよね？もしそうなら、現在のポイントにいくつかのポイントを追加したいのです。

そして、それは`state.points`プラス何かです。今のところ1つにしましょう。そうでなければ、ポイントは同じままになります。

そして、再び`state.points`を返します。よし、今は1つ追加していますが、それは最終的に追加したいものではありません。

なぜなら、私たちの質問オブジェクトのそれぞれに注目してください。実際には、いや、もっと多くのスペースを与えましょう。

それぞれがこの`points`プロパティを持っています。それは、それぞれが実際に異なる`points`値を持っているからです。

したがって、簡単なものは10ポイントしか与えませんが、より難しい場合は、まあ、これかもしれません。

まあ、実際にはそうではありません。いくつか難しいものがあります。ええ、このようなものです。

これは30ポイントを追加するので、実際にはその値を取得する必要があります。つまり、現在の質問のポイントです。

よし、ここのこれが少し分かりにくいかもしれないことは理解しているが、これは本当に、どのような種類のデータがあるかを理解し、そのデータを使って作業することだけだ。

そして、`reducer`で初めてかなり複雑な状態更新ロジックがあることに気づくだろう。

そして、これは完全に問題なく、実際には奨励されている。可能な限り、次の状態を計算するためのロジックの多くを`reducer`に直接入れるべきだ。

イベントが実際に最初に処理される場所よりも、`reducer`にこのロジックがある方が良い。

つまり、基本的に、イベントを処理するオプションで、ここでこの計算を行うこともできるだろう。しかし、それは`useReducer`フックのロジックに反するだろう。

このようにする方がはるかに良い。よし、それでこれは機能するはずだ。チェックしてみよう。

この質問が10点満点であることがわかっています。そして、もし正解を選んだら、ポイントが実際に10に更新されたことに気づくでしょう。

つまり、0 + 10です。そして、もし不正解を選んだら、ポイントは0のままです。

素晴らしい。もちろん、後でこれらのポイントをUIに表示するつもりですが、それは少し後回しにしましょう。なぜなら、次に、「次へ」ボタンを表示し、実際に次の質問に進むロジックを処理する必要があるからです。

つまり、ここで終わったら、クリックして次のものに進みたいのです。そして、それが次のビデオで行うことです。

# 次の質問へ移動する

さて、答えが出たらすぐに次の質問に進むという簡単な作業があります。

ユーザーがこれらのオプションの1つをクリックするとすぐに、ここにボタンが表示され、それをクリックできます。

そして、自動的に次の質問に進みます。さて、前に議論したように、次の質問に進むことは、基本的にここにあるこのインデックスを増やすことを意味しますよね？

なぜなら、このインデックスに基づいて、現在の質問が読み取られ、表示されるからです。それが、ここで行っていることです。

現在のインデックスに対応する質問オブジェクトを渡しています。そして、今度はここに、`reducer`にもう一つの可能なアクションを作成しましょう。

これを`nextQuestion`と呼びましょう。ここでは、現在の状態を返し、変更したいのは、`state.index + 1`に設定される`index`プロパティだけです。

そして、それが少なくとも今のところはそれです。そして、今度は実際にそのボタンを作成する必要があります。

つまり、質問の他に、ボタンを表示したいのです。そして、実際には常にボタンコンポーネントを表示しますが、そのボタンコンポーネントは、答えがあった場合にのみボタン要素自体をレンダリングします。

逆の方法で行うこともでき、ここで条件付きレンダリングを行うこともできます。しかし、代わりに、その条件付きレンダリングをボタン内で行います。

つまり、この「次へ」ボタンが、自分自身をレンダリングするかどうかを基本的に決定できるようにします。そして、そこからアクションをディスパッチしたいので、それを渡す必要があります。

そして、今もちろん、このコンポーネントを作成する必要があります。しかし、その前に、ここに問題があります。なぜなら、今度はフラグメントが必要だからです。

よし。そして、今言ったように、ここにあるこの新しいコンポーネントを作成する必要がある。

よし。そして、すでに`dispatch`関数を受け取る。しかし、`answer`も必要だ。そして、それは先ほど言った理由による。

つまり、このコンポーネントが、答えがあった場合に、ボタンをレンダリングするかどうかを許可したいからだ。

では、現在の答えも渡そう。そして、もちろん、ここにあるこのコンポーネントもインポートする必要がある。

では、それを複製しよう。そして、ええ、それでいいはずだ。

では、ここに戻って、その`answer`プロップを受け入れよう。早期リターンを行う。

`answer`が`null`の場合、これは答えがなかったことを意味することを思い出してほしい。その場合は、ここで何も返さない。

`null`を返すだけだ。そして、そうでなければ、実際にボタンを作成する。

ここで「次へ」と言い、クラス名は`btn`と`btn-ui`にする。そして最後に、`onClick`ハンドラーは新しい関数になる。

そして、もちろん、新しいアクションをディスパッチする。そして、思い出してほしい。これは作成したばかりのもので、`next question`だ。

そして、これがトリガーする状態遷移では、私たちの`reducer`はデータを必要としない。したがって、ここではペイロードは必要ない。

よし。そして、これが機能するかどうか見てみよう。では、スタートして、クリックしよう。

そして、確かに、そこに私たちのボタンがあります。そして、今度はテストしてみましょう。

ええ、次の質問に進みましたが、まだ問題があります。それは、私たちの答えがリセットされていないことです。

したがって、以前に与えられた返信をまだ表示しています。それを修正するのは簡単ですが、重要なのは、これらの異なる質問を実際に移動できることです。

では、その問題を素早く修正しましょう。そして、必要なことは、再び、これらの状態の更新が行われる中心的な場所に来ることだけです。

では、ここで何をすべきでしょうか？まあ、答えを`null`に戻すだけで、それが解決します。

そして、もう一度、ここで実際に、これらのすべての異なる状態変数がどれほど密接に関係しているかを見ることができます。

そして、`useReducer`フックは、このアプリケーションの状態を管理するための完璧なソリューションでした。

しかし、とにかく、今度はもう一度テストしてみましょう。ええ、それが解決します。

では、ここで別のものを試してみましょう。そして、これはコンポーネントです。私たちのポイントが正しく更新されているかどうかを確認するためだけです。

ええ、そうです。それで、今度は30ポイントです。これは、私たちが今正解した各質問に対して10ポイントです。

そして、これらのポイントについて言えば、私たちは今、それらをここに表示したいのです。つまり、可能なすべてのポイントのうちの現在のポイントと、私たちが行ってきた進捗状況です。

つまり、基本的にここにあるこの部分を表示することです。それで、待たずに、次のビデオに一緒に移動してそれをやりましょう。

# 進捗の表示

アプリケーションの構築を続け、ユーザーがクイズでどれだけ進歩したかを表示しましょう。

基本的に表示したいのはこれです。質問の数に対する現在の質問と、ポイントの数に対する現在のポイントです。

そして、この素晴らしい進捗バーもです。つまり、基本的にやりたいことは、質問と「次へ」ボタンに加えて、進捗コンポーネントも表示することです。

そして、実際にはまずそのコンポーネントを構築しましょう。新しいファイルで、「Progress」とだけ呼びます。

そして、このように新しいコンポーネントをスキャフォールドし、作業に取り掛かりましょう。

さて、ここでは、より意味のあるヘッダー要素、つまりメイン部分の内部のヘッダーを使用しましょう。

それは完全に意味があり、ここで使用するのに良い要素だと思います。そして、それに「progress」というクラス名を付けましょう。

そして、そこにある各サイトに対して1つの段落から始めましょう。

ここに現在の質問が必要です。それがインデックスであることを思い出してください。そして、それをこれらの強いテクニックの中に配置しましょう。

もう一度書くだけです。それをコピーして貼り付けるよりも簡単です。そして、ここには質問の数も必要です。

これらのことはプロップとして受け取る必要があります。そして、もちろん、これは単なるIではなく、インデックスと呼ばれます。

インデックス、`numQuestions`、そう呼ばれると思います。では、すでに試してみましょう。

プログレス、そして私はここにあるこのリストからこれを選択しています。なぜなら、これはこれがコンポーネントをこのページ、つまりこのファイルに自動的にインポートすることを示すからです。

だから、繰り返しになりますが、インデックスと質問の数が必要です。よし、では見てみよう。

ええ、すでに何かあります。最初からやり直しましょう。そして、今度は質問ゼロがあることがわかります。

そして、それはもちろん、インデックスが0ベースだからですが、ユーザーはそれを知りません。だから、1から始めるともっと見栄えがしますよね？

次に、現在のポイントが欲しい。そして、繰り返しになるが、それらを太字にしよう。

それがポイントであり、可能なすべてのポイントのうちのものです。つまり、それは基本的にすべての質問のすべてのポイントの合計になります。

しかし、まだその値はありません。それで、まあ、計算に行きましょう。

ここにXと書くだけで、コンポーネントを正しく表示できるように、ポイントを取得します。

`points`...どうやらまだそれを分割代入していないようだ。しかし、状態にあるはずだ。なぜなら、以前に更新したことを覚えているからだ。

そして、ええ、今ではゼロです。よし、そして今、ユーザーが獲得できるポイントの最大量を取得しよう。

そして、繰り返しになるが、それは派生状態である。なぜなら、それを質問配列自体から計算できるからだ。

では、`maxPossiblePoints`は`questions.reduce`としよう。なぜなら、これを1つの値にまとめたいからだ。

そして、ここにあるコールバックは常に前の値と配列の現在の値を取得する。それが最初の引数で、2番目の引数は初期値だ。

そして、今やらなければならないことは、前の値、つまりゼロから始まる値を`cur.points`に追加することだけだ。

つまり、ここにある通常の通常の`reducer`だけだ。だから、この時点では何も奇妙なことはないはずだ。

そして、それを渡してコピーし、ここにあるXを置き換えよう。

では、見てみよう。そして、美しい。唯一欠けているのは... まあ、ここにあるこのタイマーは数分後にタイムアウトするので、再起動する必要がある。

だから、このプログレスバーも欲しい。もっと多くの質問を完了するとすぐに進み続ける。

ご覧の通り、それは進み続けている。そして、この要素は実際にはプログレスバーだ。

これは新しいものだ。少なくとも、私が以前に本当に使ったことのないものだ。そして、ここで、これが取ることができる最大値を定義できる。

そして、これが質問の数であることは理にかなっている。つまり、この場合、基本的に0から15まで進む。

そして、現在の値を定義することもできます。つまり、これは基本的に入力要素のようなものです。

そして、ある意味でそれを制御された要素にしています。進捗バーで状態を設定することはできませんが。

しかし、とにかく、ここにある現在の値は現在のインデックスになります。そして、ここにあるのは実際にはインデックスであり、インデックスプラス1ではありません。なぜなら、実際にはこれを空で始めたいからです。

それで、ここに来ると、最初は空にしておきたいのです。では、見てみましょう。

ここをクリックしてみましょう。そして、今度はそれが実際には動かなかったのがわかります。ここでクリックすると初めて動きます。

それで、1に移動しました。この要素を調べると、最大値は15で、もちろん、値は1であることがわかります。

それで、これをここで変更して、例えば、10でどのようになるかを見ることができます。

そして、15で完全にいっぱいになります。しかし、とにかく、ここでの動作は少し異なります。なぜなら、ここをクリックすると、すぐに次に移動するからです。

クリックした直後にです。つまり、どういうわけか、これは基本的に答えがあるかどうかを検出し、もしあれば、この値をすぐに前に進めます。

では、それをここで実装しましょう。答えを渡すだけで、使用できる非常に素晴らしいトリックをお見せします。

それで、答えがあるかどうかを再度チェックした結果のブーリアンを数値に変換できます。

これを実行します。つまり、基本的に、これが行うことは、答えがない場合、これは`false`になり、数値はそれをゼロに変換します。

しかし、答えがある場合、これはもちろん`true`になり、その`true`は1に変換されます。

そして、インデックスに1を追加します。では、見てみましょう。そして、まあ、それはすでにここに1つあります。

ああ、しかし、それは私が答えを渡さなかったからです。それで、今は答えが未定義で、`null`とは異なります。

そして、それがすでに1を与えます。もちろん、答えをプロップとして渡す必要もありますが、これはそれを修正します。

ご覧の通り、これらの予期しない小さな間違いが起こるのは完全に普通のことです。しかし、いずれにしても、今では修正されました。

そして、これが私たちが探していたものです。それで、最後までずっと進み続けます。今はやりません。

時間がかかりすぎるからです。しかし、とにかく、これでこの講義の課題を正常に完了しました。

では、これを閉じて、もう必要ありません。これも、これもです。

そして、これで、次に進みましょう。

# クイズの終了

次の課題は、実際にクイズを終了する機能を実装することです。

ユーザーが最後の質問に回答した後、ここにあるメインコンポーネント内にこれらすべてを表示するのではなく、アプリケーションが終了ステータスに移行するようにしたいのです。

そうすれば、ここに終了画面のようなものを表示できます。最初はスタート画面があり、アプリケーションが準備できたら。

アクティブな間は、進捗状況、質問、次へボタンが表示されます。そして、ユーザーが実際に終了すると、ステータスは`finished`に設定されます。

そして、この状態、その状態状態をここで処理しましょう。まだボタンなどはありませんが、ステータスが終了するのをトリガーするものです。

しかし、とにかくこれを実装しましょう。私たちは望んでいる、あるいは実際にはまずコンポーネントを作成しましょう。

`FinishScreen.js`です。そして、ここに、「You scored」という段落を返し、次にスコアを返します。

そして、これを実際に`strong`タグでラップしましょう。そして、可能なポイントの最大量から、つまり「`maxPossiblePoints`」です。

そして、パーセンテージも計算しましょう。では、ここで外でやりましょう。

派生状態として、基本的にです。では、それは「ポイント」を「`maxPossiblePoints`」で割って100を掛けたものだとしましょう。

では、それを取って、ここで使いましょう。では、それを切り捨てて、次にパーセンテージです。

しかし、もちろん、コードは、これらのどれも存在しないと文句を言っています。なぜなら、もちろん、これをプロップとして受け取る必要があるからです。

よし、これでコンポーネントはハッピーになったが、もちろんまだ表示されていない。なぜなら、ここに含める必要があるからだ。

`finish screen`だ。自動的にインポートされたか確認しよう。

されなかったと思う。ええ、実際にはされなかった。

では、もう一度試してみよう。ええ、こんな感じだ。

「ポイント」と「`maxPossiblePoints`」も渡す。

さて、よし。しかし、先ほど述べたように、私たちのコードのどこにも、ステータスを終了に変更することはできません。

しかし、これをテストするために、ここでやってみましょう。そして、それがこれらの開発者ツールを持つことの大きな利点の1つです。

それで、ここで、例えば、終了まで、好きな値を変更できます。そして、作成したばかりのコンポーネントが表示されるのがわかります。

ここに、「result」の「className」が欠けています。それで、パーセンテージ記号も付けて、ずっと見栄えが良くなりました。

そして、後でここに絵文字のようなものも含めます。しかし、それは少し後回しにしましょう。なぜなら、今のところ、実際にはステータスを終了させるトリガーをしたいからです。

それで、いつそれが起こるべきでしょうか？では、ここで再起動しましょう。そして、最後の質問の1つに行きましょう。

そして、繰り返しになりますが、今度はインデックスを、例えば13に変更するだけです。そして、これで、最後の質問にほぼ到達しました。

では、ここに何かを返信するだけにしましょう。次にもう一度。そして、今度は最後の質問ですね？

つまり、インデックスは14です。それで、今ここで何かを選択すると、ここをクリックすると何が起こるでしょうか。インデックスは単に16、あるいは実際には15になり、表示する質問がなくなります。

そして、まさにこの状況で、ここにあるこのボタンが次のインデックスに進むのではなく、ここのステータスを`finished`に変更するようにしたいのです。

そして、それを次に実装します。だから、今ここをクリックすると、再びエラーが表示されます。なぜなら、そのインデックスの質問がないからです。

よし。それで、「NextButton」に行き、特定の条件が満たされた場合にのみ、`next question`イベントをディスパッチするこの種のボタンを返したいと言おう。

そして、その条件は、インデックスが質問の数より小さいことです。そのためには、もちろん、それをここに渡す必要があります。

そして、ここは実際にはマイナス1です。なぜなら、インデックスは0ベースですが、数字はそうではないからです。

よし、ではインデックスと`numQuestions`を渡そう。

そこでそれらを受け取り、ここでそれらを渡します。`numQuestions`は`numQuestions`と等しく、次にインデックスです。

このアプリケーションでは多くのプロップが渡されているのがわかりますが、この問題に対しては素晴らしい解決策を後で学びます。

それは実際には問題ではありませんが、後で学ぶツールを使えばもう少し良くできます。

しかし、とにかく、これをもう一度シミュレートしてみましょう。そして、インデックスが14になると、終了時にそのボタンが表示されなくなるはずです。

そして、その通りです。今回は先に進めません。そして、それで以前に見たバグを防ぎました。

しかし、今度はもちろん、別のボタンが必要です。では、インデックスが質問の数マイナス1と等しい場合、この場合は14ですね。

この場合、「finish」と言います。そして、ここで何か別のものをディスパッチしたいのです。

では、「finish」というイベントとしましょう。では、ここにある`reducer`でそれを追加、あるいは基本的に処理しましょう。

`finish`です。そして、これは非常に簡単になります。少なくとも今のところはです。なぜなら、ここで行うことは、状態を「finished」に設定することだけだからです。

では、ここで再びリロードしましょう。クイズ全体を手動でやり直す気にはなれないので、14と入力して、何でもクリックしましょう。

ええ、ここに「finish」ボタンが表示されました。そして、今ここをクリックすると、ええ、美しい。

ステータスが「finished」に変わり、もちろん、ここにある`finished screen`が表示されました。よし、ここに絵文字をいくつか追加して、画面をもう少しきれいにしよう。

これは、基本的に、ユーザーがどれだけうまくやったかを表すものだ。

では、ここで`let`変数を作成しよう。このコースでは初めてだと思いますが、それは、今度はここに多くの条件があるため、これが最も簡単な方法だからです。

つまり、基本的に、パーセンテージのさまざまな値に応じて、絵文字に異なる値を割り当てます。

異なる絵文字を与えます。では、ここで、このようなメダルを目指しましょう。

次に、「`else if`」と言うことができます。あるいは、実際には、これらは互いに排他的なので、そうする必要さえありません。

だから、通常の「`if`」で十分です。80以上で、ここでこれを行うのに時間をかけたくない場合は、少し早送りできますが、100未満の場合は、絵文字、見てみましょう、

今回はパーティー絵文字を使おう。そして、あと2つか3つやろう。

50以上80未満の場合と、0より大きく50未満の場合。そして最後に、ユーザーがすべての質問を失敗した場合に1つやろう。

これには`face palm`を使いましょう。次に、ここでは単に考える絵文字のようなものを使いましょう。

まあ、そこではなく。そして、ここには単に、まあ、例えば幸せな顔ではなく、これを使おう。

そして、それをここに含めるだけです。それで、少し見栄えが良くなり、ユーザーにすぐにフィードバックを与えることができます。

素晴らしい。それで、これは今本当に素晴らしい機能だ。私が実際に実装したい機能がもう一つだけある。

それはハイスコアだ。それで、ユーザーがクイズをやって100点を取った後、次にまたやったら200点を取った場合、その200点をハイスコアとして保存したい。

そして、3回目にやったら、例えば150点を取った場合、200点がハイスコアとして残る。

そして、それをここに段落として表示したい。それで、「highscore」の「className」を使い、括弧の間に「ハイスコアは今のところX点です」と書くだけだ。

そして、ここでは実際にはフラグメントを返そう。なぜなら、不要な`diff`でここにあるマークアップを汚したくないからだ。よし。

さて、いつ、どこで、どのように、このハイスコアを計算するのが最善でしょうか？まあ、そうするのに最適な場所は、ユーザーがゲームを終えたまさにその時だと思います。

そして、それがこの`finish`イベント、ですよね？これが起こると、ゲームは終了します。そして、ここでハイスコアを完璧に計算できます。

これは別の状態です。もちろん、再レンダリング間でそれを覚えておく必要があるからです。それで、ここでもゼロから始めます。そして、ええ、ゲームが終了したらハイスコアを更新しましょう。

「`state.points`」（ゲーム終了時の現在のポイント）を「現在のハイスコア」と比較するだけで済みます。

そして、それが「現在のハイスコア」よりも大きい場合は、ここに新しい値を「ハイスコア」に割り当てたいのです。

つまり、それが「`state.points`」になります。しかし、そうでなければ、「tier」は実際には疑問符ですが、そうでなければ、「ハイスコア」は単に「`state.highscore`」のままになります。

では、手動でリロードして、状態が新しいハイスコアを取得するようにしましょう。そして、今度は手動で、例えば20に設定します。

そして、ゲームを始めましょう。いくつかやってみましょう。そうすれば、もっとポイントが稼げます。

今、すでに30ポイントあります。では、最後のものに進みましょう。

全部スキップしたので、ユーザーはこれをやらないことを願っていますが、ええ、今度は誰でもクリックするだけにしましょう。

ああ、それはわざとではありませんでした。しかし、とにかく、私たちは30ポイントです。ハイスコアは20です。そして、何が起こるか見てみましょう。

そして、ハイスコアが実際に50に更新されたのがわかりました。なぜなら、もちろん、それらの50は現在のハイスコアよりも多かったからです。

よし、ではこれをここから取り出そう。状態から分割代入して、それを終了画面に渡せるようにしよう。

`highscore`は`highscore`と等しくなり、ここにも書きましょう。

「`highscore`」、そしてここでそれを受け入れる、「`highscore`」。しかし、今度はそれをもう一度テストするつもりはありません。なぜなら、それが機能すると確信できるからです。

さて、ここには最後のことが1つあります。それは、クイズを再開するためのこのボタンです。

今のところ、それはまだありません。それで、繰り返しになりますが、それは示しませんが、基本的に、最終的には、クイズをやり直すためのボタンが1つ欲しくなります。

そして、それが次のビデオで実装するものです。そこでは、アプリケーションはほとんど完成です。

# クイズの再起動

短い休憩の後、ユーザーがクイズを再開できる簡単な機能を最終的に構築しましょう。

そして、私たちがすでに用意しているリデューサーを使えば、これはほとんど簡単すぎます。

このセクションでは、これまで何度も関連する状態を更新してきたので、実際にこれを自分で実装できると思います。

つまり、この機能全体を、今自分で構築できるかもしれません。

それは本当に素晴らしいチャレンジになるかもしれません。ここにあるこのボタンと同じクラスのボタンを追加するだけです。

あるいは少なくともこれと同じクラスです。つまり、基本的にはここにあるものと同じです。

そして、新しいアクションを作成し、基本的にそこでこの日付をリセットします。

それはそれほど難しくないはずです。そして、今すぐそれをやって、終わったらここに戻ってきてください。

それが楽しかったことを願っています。では、このボタンをここにコピー＆ペーストするだけです。

これをこの右下に配置し、もちろん、`dispatch`関数にアクセスする必要があります。

ここのテキストは「クイズを再開」になります。そして、ここで、「restart」というタイプのアクションをディスパッチしましょう。

非常に創造的です。よし、そしてReducerに戻り、`case restart`とします。

そして、ここで、これを2つの異なる方法で行うことができます。初期状態にリセットしたいすべての変数を定義できます。

例えば、このように、スコアをゼロに戻し、基本的に、質問配列を除くすべてをここで行います。

これを再フェッチしたくないのは当然です。そして、まあ、おそらく、ここにあるこれも`ready`にリセットしたいのですが、これらの他のものは、ここにある値と全く同じになるはずです。

では、これを試してみましょう。ここにある初期状態全体をここに展開し、質問を再び追加します。

`questions`は`state.questions`と等しいと言い、それらを失わないようにします。

そして、最後にステータスは`ready`になるべきです。しかし、繰り返しになりますが、これをやった方が良いでしょう。

状態全体を返し、次に`points`をゼロに、`highscore`をゼロに、`index`もゼロに上書きします。

そして、あと1つだけありました。それは何でしたか？答えは`null`に、答えは`null`にです。

そしてもちろん、ステータスは`ready`です。どちらも機能しますが、基本的には初期状態に似たものにリセットしたいことをより明確にするために、こちらを好みます。

これは本当に簡単でしたよね？そして、私はこのパターンが本当に、本当に好きです。

ここにある状態ロジック、そのすべてが、すべての異なるコンポーネントから切り離されています。

そして、これは後でコードに戻ってきて、何が起こっているのかを理解したいときに特に役立ちます。

次にやるべきことは、基本的にこれらのケースを読み通すことだけです。そうすれば、アプリケーションで何が起こるか、そして状態がどのように遷移するかを理解できます。

つまり、アプリケーションの多くを理解していなくても、`reducer`を読むだけで、いくつかのデータが受信される可能性があること、それが失敗する可能性があること、何かが開始されること、新しい答えがある可能性があること、次の質問がある可能性があることなどがわかります。

繰り返しになりますが、`reducer`を読むだけでです。そして、このため、私たちの状態更新は実際にははるかに宣言的です。

つまり、これらの異なるアクションを、ここで書き出す状態遷移にマッピングするだけです。

本当に、本当に素晴らしいパターンですが、それについてはコースの終わりまでに、ここで学んだことをすべてまとめるときにもう一度話します。

では、実際にこれをテストしてみましょう。では、スタートしましょう。そして、もう一度ごまかして、14番のインデックスに自動的に進みます。

では、ここをクリックしましょう。ああ、実際にはこれはデモバージョンです。つまり、本当にごまかしですよね？

なぜなら、それはすでに機能しているからです。だから、ええ、再び14。

そして、わずか20ポイントしかありません。ハイスコアを更新することはないと思いますが、実際には更新します。

もちろん、それもゼロにリセットされるからです。そして、ここには以前と同じものはありませんでしたが、今度はクイズを再開できます。

では、見てみましょう。そして、`dispatch`は関数ではありません。いいですね、渡していなかったようです。

もちろん、ここにはそれが必要です。では、もう一度やり直しましょう。

そして、もしよろしければ、もちろんクイズ全体をやり直すことができます。私が本当にそうしてほしいと思っています。

だからこそ、15問の質問を追加しました。少し練習できるようにするためです。

しかし、とにかく、今ここをクリックすると、いいですね。質問は同じままですが、他はすべてリセットされます。

素晴らしい、それで実際にはそれで終わりです。その部分も終えました。今やらなければならないことは、ここにあるこのタイマーを動かすことだけです。

このタイマーが切れると、基本的にクイズは終了します。ここを見ると、更新し続けているのがわかります。

では、5秒に設定しましょう。ここで何が起こるか見てみましょう。

では、待ちましょう。そして、終了画面に自動的に移動するのがわかります。

そして、今からこの機能を私たちのアプリケーションに実装しましょう。

# useEffectでタイマーを設定する

このプロジェクトの最後の講義へようこそ。今回は、私たちの旧友である`use effect`を使用して、タイマー機能を実装します。

そして、この新機能は、私たちがすでに持っているリデューサーと非常によく連携します。ゲームが始まるとタイマーも始まり、タイマーがゼロになると、状態を再び`finished`に設定することでゲームを停止させます。

つまり、この動作をリデューサーで非常に美しくモデル化できるようになります。では、残っている現在の時間を表示し、実際に最初にタイマーを開始するタイマーコンポーネントを作成することから始めましょう。

そのコンポーネントはここ、一番下にあります。つまり、UIの一番下、このボタンの横にあります。

つまり、これら2つのコンポーネントをフッターの中に入れたいのです。タイマー、つまり次に構築するコンポーネントが欲しいのです。

そして、繰り返しになりますが、これら2つをフッターHTML要素の中に入れたいのです。さて、これは醜いです。ご存知の通り、私はこのように見るのが本当に好きではありません。

そして、フッターコンポーネントを素早く作成しましょう。そして、これがやることは、フッター要素を返すことだけで、渡された子が含まれます。

よし、ここでコンポーネントコンポジションをもう少しやろう。もちろん、タイマーと次のボタンの両方をここで呼び出すこともできたが、そうすると、すべてのプロップをここに渡して、他のコンポーネントに渡すだけになってしまう。

そして、それは実際には必要ない。それで、フッター。そして、タイマーもすぐにここに作成しよう。

そして、とりあえずタイマー文字列を返そう。そうすれば、これら2つのコンポーネントをここにアプリケーションに含めることができる。

タイマーとフッターです。これが構築するすべてのコンポーネントです。

ご覧の通り、たくさんあるので、他のインポートとは少し分けておきたいと思っています。

しかし、とにかく、今度は何が得られるか見てみましょう。そして、ここに私たちのタイマーが基本的にあります。

それはまだ実際にはタイマーではありませんが、それでは今からそれを変更しましょう。まず、`timer`というクラスが必要です。

そして、何かが見えるように、ここにいくつかの時間を置くふりをしましょう。これはすでに見栄えが良くなっています。

さて、よし。そして、ここで、マウント時に副作用を作成するために、再び`useEffect`フックを使用しよう。

それで、ここにあるこのコンポーネントがマウントされると、基本的にタイマーを初期化したい。そして、ここでそれをやっている。

このコンポーネントでタイマーを開始している。なぜなら、このタイマーコンポーネントは、ゲームが始まるとすぐにマウントされるからだ。

もちろん、アプリコンポーネントでタイマーを開始することはできなかった。なぜなら、タイマーはアプリケーション全体がマウントされるとすぐに実行され始めるからだ。

しかし、それは私たちが望むものではない。だから、ゲームが始まるときにマウントされるコンポーネントの1つに、このエフェクトを配置する必要がある。

もちろん、それを回避する方法はあるだろうが、このエフェクトがこのタイマーコンポーネントにあることも理にかなっていると思う。

しかし、とにかく、JavaScriptで実際にタイマーを作成するにはどうすればよいのでしょうか？さて、まず、もちろん、これはここで関数である必要があります。

しかし、次に`setInterval`関数を使用します。そして、この関数は、ここで渡す関数を、定義できる数ミリ秒ごとに実行するだけです。

では、私たちの関数を渡しましょう。これは、ちなみに、ここで矢印関数にすることもできますが、このようにやってみましょう。

今のところ、1秒に1回、「tick」という文字列をコンソールにロックしたいのです。そして、それは1000ミリ秒ごとです。

さて、これで既にタイマーが開始されているはずです。コンソールに来ると、1秒ごとに、実際には2つの`tick`が表示されているのがわかります。

しかし、繰り返しになりますが、それは開発モードでエフェクトが2回実行されるためです。これは本番環境では起こりませんが、今はそうなります。

しかし、もちろん、ここでやりたいことは、コンソールに何かをロックすることではなく、何らかの値が欲しいのです。

毎秒減らすことができる状態値が欲しいのです。では、とりあえずここから`console.lock`を削除し、ページをリロードして、ページに追加の作業を作成しないようにしましょう。

そして、ここに戻って、状態に最後の状態を追加しましょう。

そして、それが残りの秒数になります。そして、逆の方法でやることもできます。つまり、ゼロから始めて、特定の数に達するまで毎秒1つ追加します。

しかし、ここでは、いくつかの数字から始めます。今のところ、10秒とだけ言いましょう。そして、毎秒1秒ずつ削除します。

そして、それがゼロに達したら、つまり、残りの秒数がなくなったら、ゲームを終了します。

では、説明したことを実装しましょう。そして、もう一度、ここにあるリデューサーを再び利用し、ここに最後のイベントを追加します。

そして、これは「tick」と呼びます。なぜなら、時計のように、これは毎秒起こるからです。

これがこの関数でディスパッチするイベントです。そして、なぜここでそれをすぐにやらないのでしょうか？私たちはすでにここにいるので？

「tick」というタイプのイベントをディスパッチしましょう。次に、このディスパッチ関数にアクセスできるようにする必要があります。

`interior`は`dispatch event`ではありません。そして、今VS Codeは文句を言っています。あるいは実際にはESLintです。なぜなら、`dispatch`は今や依存関係だからです。

では、Reactが必要とするなら編集しましょう。そして、もちろん、前の講義で行ったように、それを渡すことを忘れないでください。

そして、今度はここで状態遷移を再びモデル化する準備ができました。

状態全体が欲しいのです。そして、`seconds remaining`は、`state.second remaining - 1`と等しくなるようにしたいのです。

1分前に言ったように、毎秒1秒ずつ引きます。そして、その値をタイマーに渡して、この値を表示できるようにしましょう。

ここにある`seconds remaining`、状態から取り出して、ここにある`seconds remaining`。そして、もちろん、それをここでプロップとして受け入れる必要もあります。

そして、ええ、それをここに配置します。そして、これで準備完了のはずです。

では、見てみましょう。そして、ええ、素晴らしい機能です。さて、もちろん、今のところ、これをゼロ未満にすることも許可しています。

では、次にそれを修正しましょう。つまり、基本的に、毎秒、これがゼロに達したかどうかを確認したいのです。

そして、もしそうなら、ゲームを終了します。まさに最初に言っていたようにです。

この機能は、すでに持っている状態構造全体に、本当に本当にうまく収まります。

では、今度は再び`status`を使い、`state.seconds remaining`がゼロと等しいかどうかを確認できます。

もしそうなら、ここで`finished`を返し、そうでなければ、それは同じままになります。

つまり、それは再び`state.status`です。ここにあるこれは、この機能の全体の心臓部です。

つまり、基本的に、秒がゼロになったかどうかを確認できます。そして、もしゼロになったら、ステータスを`finished`に設定するだけです。

これにより、ゲーム全体が停止し、アクティブ状態でここにあるものの代わりに、この`finished screen`がレンダリングされます。

では、リロードしましょう。そして、見てみましょう。

それで、小さな数字を使ったのは、これがすぐに機能するかどうかをすぐに確認できるようにするためでした。

そして、はい、機能します。そして、クイズを再開しても、以前と同じ値から始まります。

それは、ここで再起動したときに、ここにある初期状態全体を再び取得したからです。

覚えておいてください。しかし、クイズを再開したときに、ここで何か気づきましたか？

では、もう一度試してみて、今これがどれくらい速く進むか見てみましょう。そして、もしもう一度やると、さらに速くなるでしょう。

実際、クイズを開始する必要さえなく、これが今、終了画面に移動します。

明らかに何かがうまくいっていません。これがいかに速く進むかを見てください。そして、その理由は、私たちのタイマーが実際には決して止まらないからです。

つまり、クリーンアップ関数がありません。そのため、ここにあるタイマーは、このコンポーネントがアンマウントされた後も実行し続けます。

もちろん、それを修正する必要があります。そして、これはクリーンアップ関数の素晴らしいユースケースです。

つまり、このメソッドから返されるタイマーのアイデアを保存し、ここから返すクリーンアップ関数で使用できるようにする必要があります。

インターバルをクリアするには、基本的に`setInterval`で開始したタイマーのIDを渡すだけで済みます。

`setInterval`タイマーはそれぞれ一意のIDを返します。そして、そのIDを使ってタイマーをクリア、つまりキャンセルできます。

そして、今度はこれがレンダリング間、そしてさらに重要なことに、このコンポーネントがアンマウントされた後に実行されます。

そして、タイマーは実際に停止します。以前は、クイズを再起動するたびに、新しいタイマーが追加されていました。

そして、同時に多くのタイマーが実行されており、それらがすべてこのアクションをディスパッチしていました。

そして、そのため、私たちの時間は非常に速く減少していました。しかし、これはそれを修正するはずです。

では、試してみましょう。そして、実際には、今は一度に1秒ずつしか進んでいません。

そして、どうぞ。それは予想通りでした。しかし、今度はタイマーが以前と同じ速度であるかどうかをもう一度見てみましょう。

そして、ええ、そうです。そして、それはここにあるこのクリーンアップ関数が実際に機能していることを意味します。

さて、ここですべてのレンダリング、あるいは実際にはすべてのコンソールログを取得していることに注目してください。それは、もちろん、アプリケーション全体、つまりすべてのコンポーネントが毎秒再レンダリングされるからです。

では、前に学んだことを簡単に復習しましょう。それは、1つのコンポーネントが再レンダリングされると、そのすべての子コンポーネントも再レンダリングされるということです。

状態は、このグローバルな`app`コンポーネントにあります。したがって、状態が再レンダリングされると、これらのすべての子コンポーネントも再レンダリングされます。

そして、もちろん、ここでこのレンダリングを行う`question`コンポーネントにも当てはまります。これにより、毎秒このロックがここに作成されます。

では、ここでそれを素早くクリーンアップしましょう。そして、これは、何千ものコンポーネントがある本当に大規模なアプリケーションではパフォーマンスの問題になる可能性があります。

その場合、最も親のコンポーネントが毎秒再レンダリングされるべきではありません。しかし、この場合、これは実際には問題ありません。

さて、もちろん、ここにある10秒では全く十分ではありませんよね？そして、今やりたいことは、持っている質問の数から秒数を計算することです。

では、そうしましょう。残り秒数は10から始まりますが、繰り返しになりますが、質問の数から計算したいのです。

しかし、最初はその数がわかりませんよね？ですから、これを`null`に設定し、代わりにゲームを開始するときにここでこの数を計算しましょう。

このアクションがディスパッチされる時点では、すでに質問配列があるので、ここで残りの秒数を計算できます。

`state.questions.length`、つまり質問の数、そして質問ごとに30秒としましょう。

さて、このような魔法の数字をここに配置すべきではありません。代わりに、このような値に対して、常に外に定数を作成する必要があります。

そうでなければ、ここに30が表示されるのは本当に奇妙で不明確になります。では、質問あたりの秒数と呼ばれる定数を作成しましょう。

そして、これらを大文字にするのは一種の慣習です。質問あたり`secs`は30です。

よし、それで今度はうまくいくはずだ。そして、ええ、今度はクイズを完了するのに十分な時間があると思う。これは、繰り返しになるが、あなたがやることを願っている。

あるいは、これらのビデオの1つの外で、すでにやったかもしれない。しかし、今、これをもう少しきれいにフォーマットしよう。

例えば、分と秒を分ける。ここで分の変数を作成しよう。

`Math.floor`を使おう。そして、秒を60で割るだけだ。そして、これが分の量を与え、次に`Math.floor`で切り捨てる。

そうすれば、分の他に、秒も表示できる。そして、これは`seconds remaining`であり、次にそれを60で割った余りだ。

それは、繰り返しになるが、かなり標準的なものだ。そして、ここにそれを表示しよう。分と秒だ。

保存して、今度はあまり見栄えが良くない。なぜなら、理想的には、状況によっては末尾にゼロも欲しいからだ。

今ではなく、10未満の場合だが、それもかなり簡単だ。

分が10未満で、もしそうなら、そこに0を置くだけです。そして、そこにあります。

そして、まあ、秒についても同じことです。秒です。そして、今のところ、そうではありませんが、1秒後、あるいは実際には20秒後にそれを見るかもしれません。

しかし、ええ、おそらくそのために待つ必要はありません。なぜなら、実際にはこれでアプリケーションを完成させたからです。

機能は完全です。今、私たちがやるべきことは、タイトルを修正することだけかもしれません。では、ここに来て、「The React Quiz」と呼びましょう。

そして、そこにあります。美しい。そして、今、私たちは本当にこのアプリケーションを終えました。

それで、おめでとうございます。ポートフォリオにさらに1つの本当に素晴らしいReactアプリケーションを追加しました。

これが楽しいものであったことを願っています。私はあなたと一緒にこれを開発するのが本当に好きでした。

リデューサーと`useReducer`フックについて多くを学びましたが、繰り返しになりますが、React開発全般についてさらに学びました。

そして、もし望むなら、このプロジェクトをさらに進めることができます。少なくとも、実装できる機能のアイデアが3つあります。

まず、ここにあるスタート画面で、さまざまなことができます。例えば、ユーザーが特定の数の質問のみを選択できるようにしたり、質問の難易度でフィルタリングできるようにしたりすることです。

さて、私たちができるもう一つのことは、クイズのハイスコアを偽のAPIにもアップロードすることです。

そして、後でアプリケーションをリロードすると、それがハイスコアを再フェッチし、状態に戻して、その値を失わないようにすることができます。

そして最後に、私が持っていたもう一つのアイデアは、今やっているように、現在の答えだけでなく、ここにすべての答えを配列に保存することです。

そして、そうすることで、ユーザーは過去を行ったり来たりして、そのように答えを確認できます。

もしよろしければ、このプロジェクトでさらに楽しむことができます。そして、もしそうするなら、GitHubのリンクなどを共有してください。あるいは、この講義のQ&Aで最終的なアプリケーションだけでも構いません。

そして、このセクションを終えるために、`useReducer`について学んだことを簡単に復習し、`useState`フックと比較します。

では、すぐそこでお会いしましょう。

# セクションのまとめ：useState vs. useReducer

このセクションを締めくくるために、`useReducer`について学んだことを、`useState`と比較しながら、両者の動作といつどちらを使うべきかという観点から復習しましょう。

すでに知っているように、`useState`は互いに独立した単一の状態に最適です。

数値、文字列、配列、単純なオブジェクトなどです。一方、`useReducer`は、互いに関連し依存する複数の状態がある場合、または非常に複雑な状態がある場合に使用します。

さて、`useState`でいくつかの状態がある場合、その状態を更新するロジックは、通常、イベントハンドラーまたはエフェクトに直接配置されます。

そして、これらはコンポーネント全体に分散されているか、あるいはリフトされた状態を使用している場合は子コンポーネントにさえ配置されている可能性があります。

そして、そこに`useReduce`の大きな利点の1つがあります。なぜなら、`useReduce`を使用すると、すべての状態更新ロジックを1つの場所、つまり`reducer`関数に集中させることができるからです。

そして、これにより、すべてのロジックがコンポーネントからうまく分離されます。次に、状態を更新する必要があるときはいつでも、`reducer`にアクションをディスパッチするだけです。

`reducer`は、さまざまなアクションに対して状態の更新をどのように実行するかを正確に知っています。つまり、`reducer`は、明確に定義された名前を持つアクションに状態遷移をマッピングします。

これにより、はるかに宣言的になります。そして、最初の講義のゲームの例に戻ると、ゲームを開始するためにこれらのような3つの別々の状態を設定する代わりに、

`startGame`アクションをディスパッチするだけで、`reducer`が残りを処理してくれます。

そして、これは本当に素晴らしいパターンです。このアプローチの唯一の欠点は、理解して実装するのが少し難しい場合があることです。

少し時間がかかるかもしれませんが、それだけの価値はあるかもしれません。さて、比較を終えるために、`useState`ではもちろんアクションをディスパッチしません。

`setState`関数を呼び出して直接状態を更新するだけです。これにより、アクションをディスパッチする場合と比較して、状態の更新ははるかに命令的に感じられます。

しかし、良い面として、これはもちろん理解して使用するのがはるかに簡単です。なぜなら、`reducer`関数を記述する必要がないからです。

そして、ほとんどの場合、これは完全に問題ありません。そして、`useState`が正しい選択となります。

そして、これが実際に、次の最後のスライドにつながります。そこでは、コンポーネントに状態を追加する必要があるときにいつでも、`useState`と`useReducer`のどちらかを選択するための簡単なフレームワークを提供します。

まず、最も明白な質問は、状態が1つだけ必要かどうかです。もしそうなら、答えは非常に簡単です。

`useState`フックを1つだけ使用して、それで終わりです。さて、状態が1つ以上必要な場合、次に尋ねる質問は、

私の状態は頻繁に一緒に更新される必要がありますか？以前のゲームの例のようにです。そして、答えが「はい」なら、

`useReducer`の良いユースケースがあるかもしれません。しかし、おそらく最初に尋ねるべきもう1つの質問があります。

リデューサー関数を使って、少し複雑な`useReducer`フックを実際に書く気がありますか？

そして、もしそうでなければ、`useState`フックを使い続けます。

しかし、もしリデューサーを書く時間があれば、これは間違いなく`useReducer`の良いユースケースです。

いいですね。では、続けていきましょう。もし私たちの状態が頻繁に一緒に更新されない場合、次のステップは、関連する状態が3つまたは4つ以上必要かどうか、そしてその状態にオブジェクトが含まれているかどうかを把握することです。

これを複雑な状態と呼ぶことができます。したがって、この場合、そして実際にリデューサーを書く気があれば、`useReducer`が再び正しい選択肢です。

最後に、私たちの状態はそれほど複雑ではないかもしれませんが、コンポーネントとその子コンポーネントを大きすぎたり混乱させたりするイベントハンドラーが多すぎると感じるかもしれません。

その場合、`useReducer`を検討することもできます。そうでなければ、質問が再び「いいえ」であれば、`useState`が再び正しい選択肢です。

つまり、一般的に、`useReducer`について学んだ後でも、`useState`はReactの状態を管理するためのデフォルトの選択肢であり続けるべきです。

しかし、`useState`が、私たちが話してきたこれらの問題のいずれかを引き起こす場合、`useReducer`を検討する時です。

そして、それが実際にはそれです。それで、この時点で、`useReducer`フックを理解することに関して、あなたはおそらくReact開発者の上位5%に入っています。

本当に素晴らしい進歩を遂げています。そして、最後に、`useReducer`フックを自分で練習する時が来ました。

# チャレンジ #1: `useReducer`で銀行口座を作成する

```tsx
import { useReducer } from "react";
import "./styles.css";

/*
INSTRUCTIONS / CONSIDERATIONS:

1. Let's implement a simple bank account! It's similar to the example that I used as an analogy to explain how useReducer works, but it's simplified (we're not using account numbers here)

2. Use a reducer to model the following state transitions: openAccount, deposit, withdraw, requestLoan, payLoan, closeAccount. Use the `initialState` below to get started.

3. All operations (expect for opening account) can only be performed if isActive is true. If it's not, just return the original state object. You can check this right at the beginning of the reducer

4. When the account is opened, isActive is set to true. There is also a minimum deposit amount of 500 to open an account (which means that the balance will start at 500)

5. Customer can only request a loan if there is no loan yet. If that condition is met, the requested amount will be registered in the 'loan' state, and it will be added to the balance. If the condition is not met, just return the current state

6. When the customer pays the loan, the opposite happens: the money is taken from the balance, and the 'loan' will get back to 0. This can lead to negative balances, but that's no problem, because the customer can't close their account now (see next point)

7. Customer can only close an account if there is no loan, AND if the balance is zero. If this condition is not met, just return the state. If the condition is met, the account is deactivated and all money is withdrawn. The account basically gets back to the initial state
*/

const initialState = {
  balance: 0,
  loan: 0,
  isActive: false,
};

function reducer(state, action) {
  if (!state.isActive && action.type !== "openAccount") return state;

  switch (action.type) {
    case "openAccount":
      return {
        ...state,
        balance: 500,
        isActive: true,
      };
    case "deposit":
      return { ...state, balance: state.balance + action.payload };
    case "withdraw":
      return { ...state, balance: state.balance - action.payload };
    case "requestLoan":
      if (state.loan > 0) return state;
      return {
        ...state,
        loan: action.payload,
        balance: state.balance + action.payload,
      };
    case "payLoan":
      return { ...state, loan: 0, balance: state.balance - state.loan };
    case "closeAccount":
      if (state.loan > 0 || state.balance !== 0) return state;
      return initialState;
    default:
      throw new Error("Unkown");
  }
}

export default function App() {
  const [{ balance, loan, isActive }, dispatch] = useReducer(
    reducer,
    initialState
  );

  return (
    <div className="App">
      <h1>useReducer Bank Account</h1>
      <p>Balance: {balance}</p>
      <p>Loan: {loan}</p>

      <p>
        <button
          onClick={() => dispatch({ type: "openAccount" })}
          disabled={isActive}
        >
          Open account
        </button>
      </p>
      <p>
        <button
          onClick={() => dispatch({ type: "deposit", payload: 150 })}
          disabled={!isActive}
        >
          Deposit 150
        </button>
      </p>
      <p>
        <button
          onClick={() => dispatch({ type: "withdraw", payload: 50 })}
          disabled={!isActive}
        >
          Withdraw 50
        </button>
      </p>
      <p>
        <button
          onClick={() => dispatch({ type: "requestLoan", payload: 5000 })}
          disabled={!isActive}
        >
          Request a loan of 5000
        </button>
      </p>
      <p>
        <button
          onClick={() => dispatch({ type: "payLoan" })}
          disabled={!isActive}
        >
          Pay loan
        </button>
      </p>
      <p>
        <button
          onClick={() => dispatch({ type: "closeAccount" })}
          disabled={!isActive}
        >
          Close account
        </button>
      </p>
    </div>
  );
}
```

伝統を続け、チャレンジでセクションを締めくくりましょう。

そして、今回はこのような非常に簡略化された銀行口座を作成してほしいと思います。

これは、`use Reducer`フックについて最初に話したときに与えたアナロジーに似ています。

違いは、ここでは本当にシンプルに保つために、口座番号がないことです。

それで、最初は、口座開設という1つのアクションしか取れません。

そして、すぐに私たちの銀行口座に500ドル、あるいはユーロが入金されます。

そして、そこからこれらの異なるアクションを実行できます。例えば、いくらかのお金を入金できます。

それで、残高が150増えたことがわかりますが、もちろん、後で入力フィールドを使用して、50ではない何か他のものを入金することもできます。

それで、もちろん、お金を引き出すことができます。そして、ローンをリクエストすることもできます。

そうすると、私たちの口座も増え、ここにある私たちのローンも増えることがわかります。

それが5,000ドルです。しかし、もう一度ここをクリックしても、それはもう機能しません。

つまり、同時に1つのアクティブなローンしかありません。その後、ある時点でローンを返済することもできます。

もう一度ここをクリックして、最後にアカウントを閉じることもできます。しかし、それが機能するためには、ローンがあってはならず、残高がゼロである必要があります。

だから、今クリックしても、それは機能しないが、すべてのお金を引き出せば、口座を閉じることができる。

そして、基本的には初期状態に戻ります。いつものように、スターターファイルにこれらすべてがあります。

ここにはすでにすべてのJSXを記述しました。そして、今回は従うことができるいくつかの指示や考慮事項も書きました。

つまり、基本的に、ここにあるこの銀行口座が従うべきルールです。よし、ここにはすでに初期状態もあります。これを使用する必要があります。

そして、残高とローンがあることがわかります。これら2つです。そして、`is active`状態もあります。

最初は`false`であることがわかります。そして、ここにあるこれらのボタンはすべて無効になります。

それで、今のところ、ここにあるすべての`disabled`は単に`fault`に設定されていますが、おそらく後でこの`is active`を使用することになるでしょう。

そして、ここにはもちろん、独自のイベントと/または関数も記述します。さて、ですね。それで、これがそれほど難しくないことを願っています。

ここにあるこれらの指示を読んでください。そして、何か問題や疑問があれば、その部分に到達するまでビデオを少し進めることができます。

よし？では、このチャレンジに数分間時間を取って、非常に重要な`useReducer`フックを自分で練習してください。

よし、ではここで作業に取り掛かろう。そして、今回はこれをフォークして、スターターファイルを上書きしないようにする。

そして、私が今ここで実装しようとしているものと似たようなことをしたことを願っている。

ここにReducer関数と、もちろん初期状態があり、これは状態を返す。それをバランスローンと`is active`にすぐに分割代入する。

そして、このように`dispatch`関数も取得する。よし。

次に、現在の状態とディスパッチされたアクションを受け取るReducerを作成する必要がある。ここにいくつかの問題がある。

そして、今度は典型的なスイッチをここに作りましょう。アクションタイプに対して、すでに知っているすべてのケースを一度に作成しましょう。

口座開設、入金、その他すべてがあります。口座開設です。今のところ、ここに何かを返すだけにしましょう。

次に、入金、引き出し。そして、ここには、この書式設定については何の配慮も必要ありません。

ローンをリクエストします。次にローンを支払います。それはロードではなく、ローンです。

そして最後に、口座を閉鎖する機能も欲しい。そして、いつものように、新しいエラーをスローするデフォルトのケースでだ。

このようにです。さて、ですね。さて、ここにあるJSXに戻りましょう。そうすれば、取得した変数を使用できます。

ここで、残高を表示し、ここでローンを表示します。そして、すぐにユーザーインターフェイスが更新されます。

そして、ここでやりたいことは、あるいは、まず`is active`も`falses`の代わりに使いましょう。

そして、実際にはもちろん、口座がアクティブでないときにボタンが無効になるようにしたいのです。ただし、ここにあるこのボタンを除いてです。

そうでなければ、そもそも口座を開設できませんでした。よし？

それで、口座を開設するためにここで行うことは、`open account`というタイプのイベントをディスパッチし、ここにあるこの男を閉じる必要があります。

そして、この場合何が起こるべきか見てみましょう。まあ、いつものように、状態オブジェクト全体を返します。

そして、残高は初期金額であるため500に設定する必要があり、`is active`は`true`に設定されます。

そして、ここにあるこれらのボタンはすべてアクティブになります。そして、見てみましょう。ええ、いいですね。

すでに機能しているので、続けましょう。次の2つは非常に似ているので、実装しましょう。

状態と、残高は単に現在の残高プラス、アクションで入ってくるものです。

つまり、`action.payload`です。繰り返しになりますが、`action payload`はいくつかのデータを渡すためのものです。

そして、ここに150を渡しますが、`Reducer`関数でその150をハードコーディングしたくありません。

つまり、アクションとしてそれを渡す必要があります。それで、引き出し。

マイナスが付くだけで同じです。そして、ここ、ええ、試してみましょう。

入金タイプのディスパッチと150のペイロード。そして、先ほど言っていたように、もちろん、ここにインプットフィールドを作成することもできます。

そして、入力フィールドから来た値を預けました。それで、この例で本当に夢中になることができますし、それをはるかに複雑に、そしてより完全にすることができます。

もしそうするなら、それをQ&Aに投稿してください。なぜなら、それを見ることに本当に興味があるからです。

しかし、とにかく、ここにあるこのボタンをクリックすると、毎回銀行口座に150を入金します。

そして、ここで、この行全体をコピーしましょう。いいえ、実際にはそれは機能しません。

だから、気にしないでください。ここにあるタイプは`draw`で、50で、それは確かに機能します。

これらは簡単なものでした。今度は難しいものを処理しましょう。しかし、実際にはその前に、何か別のことをする必要があります。

では、実際にこの全体をリロードしましょう。そうすれば、残高は500に戻り、口座はもはや開いていません。

そして、ここ、あるいは実際にはここで、それを`faults`に設定しましょう。

そして、今ここをクリックすると、実際にはすでに何かを口座に入金できます。たとえアクティブでなくてもです。

そして、もちろん、Reducer関数内でもそれを防ぐ必要があります。この種のことをブロックするためにUIだけに頼ることはできません。なぜなら、後でアプリケーションのどこかで、このようなアクションをディスパッチするかもしれないからです。

ボタンからではなく、例えば何らかのエフェクトからかもしれません。そして、Reducerでそのケースを考慮に入れる必要があります。

つまり、ここでそのアクションを本当にブロックするのです。それで、アカウントがアクティブでない場合、現在の状態を返すだけだと言うことができます。

この場合、これは機能しない。だから、ここにあるこれはもう機能しない。

だから、それが機能しないことがわかる。しかし、機能しないのは、口座を開設することでもある。

そして、`or`、実際には`end`も言う必要がある。それで、アカウントがアクティブでなく、アクションタイプが`open account`と異なる場合、単に状態を返します。

そして、それが現在のケースです。それが現在のケースです。だから、私がここをクリックすると、アカウントはアクティブではなく、タイプは`open account`ではないので、何も機能しません。

しかし、もちろん、アカウントはまだアクティブではありませんが、アクションタイプは実際には`open account`です。

そして、ここにあるこれは実行されず、先に進むことができます。そして、私たちの口座は実際に開設できます。

よし。それで、これらはあなたが心に留めておく必要がある非常に重要な事柄であり、それが、これらの課題を行うことが非常に重要である理由でもあります。

なぜなら、プロジェクト自体でこれらのすべての事柄を網羅することは不可能だからです。だから、このように、より異なる状況で最もよく扱われる他のいくつかのエッジケースが常にあります。

しかし、とにかく、今度はここでいくつかの他のアクションをディスパッチしましょう。これはローンをリクエストするためのものです。

ローンをリクエストする。そして、ここでは5,000ユーロ、ドル、その他の通貨などです。

では、ローンの指示を見てみましょう。ローンは、まだローンがない場合にのみ顧客がリクエストできます。

そして、その条件が満たされた場合、それはローン状態と残高に追加されます。よし。

つまり、ここにあるガード句は、基本的に、すでにローンがある場合、つまり`state.loan`がゼロと等しい場合は何もしない、つまり現在の状態を返すということです。

この`Reducer`関数は常に何らかの状態を返す必要があることを思い出してください。何も返さないことはできません。

もしそうした場合に何が起こるか見てみましょう。次にそれを実験しますが、他の場合、つまりまだローンがない場合は、ローンはアクションペイロードと等しくなるべきです。

そして、もちろん、最初に状態自体が必要であり、次に残高も更新されて、`state.balance`プラスここにあるペイロードになります。

よし、では試してみよう。ローンをリクエストすると、美しい。

両方に追加された。しかし、もう一度クリックすると、ここに`null`が返される。

そして、それはアプリケーション全体を壊します。なぜなら、今ここにあるこのオブジェクト、つまり`youth Reducer`から返された状態が`null`だからです。

そして、それはあり得ない。それは常に何らかの状態である必要がある。では、もう一度試してみよう。

ええ、これでうまくいきます。これはもちろん必要ありません。そして、実際には次のものをすぐに実装しましょう。

ローンが支払われた場合、ゼロに戻り、残高も下がります。

`state.balance`、こんな感じです。そして、ここで、何らかの値を渡す必要があると思いますか？

ペイロードがないアクションのようなものですか。まあ、それは実際には必要ありません。なぜなら、ローンが何であったかはすでに知っているからです。

それは`state.loan`です。そして、そのローンは単に銀行口座から削除されます。

では、それをここでディスパッチしましょう。これは現実世界のローンのようなものではありません。

長期間にわたって支払うものです。それはすべて一度に支払わなければなりません。

最初に言ったように、非常に単純化されています。ローンを支払います。

さて、ここにいくつか問題があり、ええ、そこにあります。

今、私たちは何度もクリックすることができ、そして実際にはこの関数、あるいはこのアクションが何度も何度も呼び出されるのを防いでいません。

しかし、2回目にクリックするとローンはすでにゼロになるため、何も起こりません。

そして、現在の残高から単にゼロが引かれます。したがって、これは全く問題ありません。

問題だと思う唯一のことは、ここにある口座のお金が一人ぼっちのときよりも少ない場合です。

その場合、マイナスの残高になり、理論的には許可されるべきではありません。しかし、まあ、私たちの銀行では許可されています。

マイナスの残高があるときにできる唯一のことは、口座を閉じることです。

どこかでそう言ったと思う。ええ、そうすると、ユーザーや顧客は口座を閉じることができません。

よし、そして閉じることと言えば、ここでリストアクションを実装しよう。

そして、これは、ローンがなく、残高がゼロの場合にのみ口座を閉じることができると言っている。

では、ここでそれをやろう。実際には、これは必要ない。

まず、条件をチェックする必要がある。そして、いつものように、ガード句を使う。

条件が満たされない場合に状態を返すガード句だ。ローンがゼロより大きく、現在残高がある場合、つまり残高がゼロと異なる場合、もう一度状態を返すだけだ。

しかし、そうでなければ、口座を閉じて、アクティブではなくなります。

つまり、基本的には初期状態です。残高もローンもなく、口座はアクティブではありません。

さて、最後に、ここでそのアクションを`dispatch`する必要があります。タイプは`close account`です。

では、見てみましょう。まあ、そこにはいくつかの問題があります。なぜなら、私たちはマイナスの残高で口座を閉じることが許されるべきではないからです。

では、ここで何を間違えたのか見てみましょう。そして、ここは`or`であるべきです。同時に`end`ではありません。

ローンがある場合、または残高がある場合は、何もしないでください。つまり、この日付を返すだけです。

そして、これは、ローンがなく、残高がゼロの場合、初期状態を返すことができ、口座が閉じられることを意味します。

では、もう一度試してみましょう。口座を開設して、今度は閉じられません。

たくさんのお金を引き出しましょう。ああ、それは多すぎました。

それで、今度は私も閉じられませんが、ええ、今度は機能します。

素晴らしい、それで実際に終わりました。そして、今、前に言ったように、もしあなたが望むなら、この演習で少し夢中になることができます。

そして、ここにいくつかの操作を追加したり、いくつかの入力フィールドを含めたり、あるいは本当にあなたが望むものを何でもできます。

そのアナロジーで行ったように、口座番号を使用することもできます。そして、もしあなたがそれらのいずれかを行うなら、それを私に見せてください。

それで、Q&Aでのあなたの進捗状況を見ることができます。そして、もしあなたがこのような`use Reducer`の課題をもっと欲しいなら、なぜならまだ少し難しいと思うかもしれないからです。

それなら、Q&Aセクションで教えてください。そして、これで、次のセクションに進みましょう。そこでは、初めて外部ライブラリを使用します。

そして、それはReact Routerです。そうすれば、初めての本物のシングルページアプリケーションを構築できます。